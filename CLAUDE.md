@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-12 | **Multi-tenancy Phase 3 step 11 — per-Organization outbound email. PHASE 3 IS NOW COMPLETE.** **The step's premise was false, confirmed by survey before building**: the spec says "switch outbound email from however it currently sends", but **nothing in this repo sent email at all** — no dependency, no module, no send site (`auth.service.ts:309` and `sla-registry.config.ts:27` both documented the gap; `CommunicationLog` only RECORDS that a communication happened). Greenfield, not a switch. New tenant-scoped `OrganizationEmailIntegration` (one mailbox per office, OAuth refresh token **encrypted** by the same field-level service as national IDs under a new `oauth` purpose), live **Microsoft Graph** and **Gmail** adapters, the OAuth authorization-code flow, and `GET/POST /admin/email-integration/*` (`email.integration.read` / `.manage`, +2 permissions). **The governing rule, borrowed from Part B: a message that was not sent is never reported as sent** — `SENT` / `NOT_CONFIGURED` / `CREDENTIAL_REJECTED` / `SEND_FAILED`, and **the registry NEVER falls back**: no mailbox, REVOKED/ERROR, an unconfigured provider, or a token that will not decrypt all yield the null provider, never a shared platform sender. A 401/403 sets ERROR ("reconnect"); a 5xx is retried and stays ACTIVE — collapsing the two would tell an administrator to do the wrong thing. **§6.3 ("sensitive content never in the body") is structural, not conventional**: `OutboundMessage` has no attachment and no HTML field, and the send path accepts only a **template key + typed params**, never caller-supplied body text — so violating §6 is a reviewable edit to one file rather than a one-line mistake anywhere. Every template renders **Arabic and English**, in the recipient's own `languagePreference`. **Password reset now actually sends** — the gap open since A.1. Its failure is deliberately NOT surfaced to the caller: `/auth/forgot-password` returns the same shape either way, and reporting a mail failure for one address but not another would reintroduce the account enumeration that shape prevents. **A migration lesson that applies to EVERY new table from here on**: migration `20260928100000` installed RLS policies for the **118 tables that existed when it ran** and cannot cover one added later. The application layer picks new tenant tables up automatically from the Prisma DMMF; **the database layer does not.** This migration adds its own policy and RAISEs if it did not take → **119 policies**. (Step 10's three new tables correctly have none — they are §3.1 global.) **Verification**: typecheck 6/6, lint 3/3, build 3/3, unit api **2847/2847** · web 16/16 · db 15/15; api e2e **434/434 across all 72 files**. | **Phase 3 is complete (steps 10 + 11). Do NOT start Phase 4 or any later phase without an explicit go-ahead.** **The two mail adapters have NEVER run against a real tenant** — no office has connected a mailbox, and a live OAuth consent cannot be faked. Their HTTP behaviour (token exchange/caching, retry, error classification, request + MIME shapes, Arabic subject/body encoding) is covered by 19 unit tests against a stubbed `fetch`; **whether Microsoft and Google accept those payloads is not**. Same standing as Part B's commercial screening adapter — verifying it is the first task of whoever connects the first mailbox, and the two places to adjust are `sendMail`'s body shape and the MIME builder. **Part V item 10 is deliberately left UNTICKED**: the per-office sender, the no-fallback rule and the mailbox row's isolation are all proven, but no message has ever been delivered. **9 of 12 items are ticked**; the two genuinely unbuilt are subdomain-scoped JWT (Phase 4) and bulk import (Phase 5). **Not built**: a client portal (so §6.3's "authenticated link" works for staff, but an external client or insurer has nowhere to authenticate), and any UI — endpoints only, screens belong with Phase 4. New env vars, all optional: `EMAIL_MS_*`, `EMAIL_GOOGLE_*`, `APP_BASE_URL`, `EMAIL_TIMEOUT_MS`, `EMAIL_MAX_RETRIES`. See `docs/per-tenant-email.md`. |
| 2026-09-12 | **Multi-tenancy Phase 3 step 10 — the insurer registry split into global master data + per-office relationship.** `Insurer` was one table holding both a company's IDENTITY and one office's commercial relationship with it — two things with opposite sharing rules. New GLOBAL `InsurerMaster` / `InsurerFormTemplate` / `InsurerFormField` (no `organizationId`, **no RLS policy**, §3.1 like `Role`/`Permission`); `Insurer` keeps its scoping and now carries only what differs per office — the §10.2 three contacts, credit terms, rating, `isActive`. **The inversion worth guarding**: every other tenancy rule here points the other way, so **adding `organizationId` to `InsurerFormTemplate` would pass every isolation test in the suite and silently destroy the sharing §5 exists for** — hence the new Part V item-8 test, the only one in `tenant-isolation.e2e-spec.ts` asserting a row IS visible across offices (**9 of 12 items now automated**). New module: `GET/POST /insurer-masters/:id/form-templates` (`insurer.master.read` / `insurer.form.map`, +2 permissions). A re-map is a **new version**, never an edit — other offices may already have submitted against v1; versions are allocated by the DB unique constraint with a retry, not by read-then-trust. `/current` returns **null, not 404** when nobody has mapped that insurer+line: §5 falls back to the generic Needs-Assessment/RFQ fields. **Migration `20260929100000` is hand-authored and that is not cosmetic** — Prisma's diff wanted to `ADD COLUMN insurerMasterId NOT NULL` in the same statement that `DROP`s `name`, which both fails on existing rows and destroys the data the masters are built from, plus the usual eight raw-SQL index drops. Real order: add-nullable → backfill → a `DO` block that REFUSES to continue on any unmapped row → constrain → drop. Verified: 3→3 masters on `db`, 1,768→1,768 on `db-test`, zero orphans, zero duplicate `(organizationId, insurerMasterId)`, Arabic names intact, insurer ids unchanged so every FK still resolves. **The bug it found, and the lesson: `typecheck` cannot see inside `$queryRaw`.** Every TypeScript read of `insurer.name` failed the build; ONE raw-SQL `ins.name` (the insurer-payables report) compiled cleanly and died at runtime with `42703`. Only e2e caught it. Related: a SQL comment inside a `Prisma.sql` literal must not contain backticks — they open a nested template expression. **And a silent one**: five repositories each had a private `INSURER_IDENTITY_SELECT`; a stale `name: true` did NOT fail the build, because Prisma's `Exact` resolves an unknown `select` key to `never`. Now one shared definition — and since its helpers flatten the join back to `{ id, name, nameAr }`, **the wire format is unchanged and the web app needed no changes**. **Verification**: typecheck 6/6, lint 3/3, build 3/3, unit api **2811/2811** · web 16/16 · db 15/15; api e2e **415/415 across all 71 files**. | **Do NOT start step 11 or any later phase without an explicit go-ahead** — Phase 3 step 10 is done, step 11 (per-tenant email) is not started. **Step 11's premise is false and this is confirmed, not assumed**: the spec says "switch outbound email from however it currently sends", but **nothing in this repo sends email at all** — no mail dependency, no module, no send site; `auth.service.ts:309` and `sla-registry.config.ts:27` both document the gap, and `CommunicationLog`/`Interaction` only RECORD that a communication happened. It is a greenfield OAuth mail subsystem, and the user has chosen a **full build including live Microsoft 365 / Google Workspace adapters**, which cannot be verified end-to-end without real credentials (the same known-unverified state Part B's commercial screening adapter is in). **There is still no Insurer CRUD and no mapping screen** — API and tests only, a deliberate scope decision; screens belong with Phase 4's UI work, where they also need Arabic. **Before changing any column on a table raw SQL touches**, grep for it as SQL (`grep -rn '"Insurer"'`), not only as a TypeScript property — typecheck will not save you. |
| 2026-09-12 | **Multi-tenancy Phase 2 step 9 — the Part V checklist, run as real tests across two Organizations.** New `apps/api/test/tenant-isolation.e2e-spec.ts`: **19 tests**, a second office stood up for the run, both offices holding a customer with the **same legal name** — the case where a leak would be least obvious. **Eight of the twelve multi-tenancy items are covered** (1, 2, 3, 4, 5, 6, 9, 12); the other four need surface area later phases build and are annotated as such in the spec, not silently skipped. Items 2/3/4/5 open their own connections as `ibms_app` **and** as the owner and compare what each can see — asserting them through the API would only prove the application layer works, which is the other layer. **A structural fix, found by reviewing step 8's own diff rather than by a failure**: the store field tracking "is a scoped transaction open" was a BOOLEAN, which answers the wrong question — a query may reuse an ambient transaction's connection **only if that connection's `app.current_org_id` matches the Organization the query is filtered by**. It now holds the org id (`scopedTransactionOrg: string | null`), making the mismatch unreachable rather than fixed per instance. Reviewing THAT change exposed a second case in the same function: `$transaction` pinned `organizationId ?? ''` even where `set_config` was deliberately skipped (auth bootstrap, global-models-only transactions), which would have invited a query inside to reuse a connection RLS still regards as unscoped; it now pins only when the variable was actually set. **The general shape worth carrying forward: a "can I reuse this?" check must compare the property that makes reuse safe, never merely observe that something is open.** **The "lock, then do separate work" sweep step 8 asked for is done** — exactly two lock sites exist: `UserRepository.withRoleLocked` (the known defect, already fixed) and `InvoiceRepository`'s instalment lock, which does all its work INSIDE `tx` and is correct. No others. **Item 6 audit over all 88 `updateMany`/`deleteMany` sites**: 79 already act on their count; of the nine that discarded it, the four revocations now RETURN it (a legitimate zero exists for each, so it is reported, not asserted), and five are genuinely count-blind delete-then-recreate/sync patterns. What makes the class unreachable is the structural fix, not per-call assertions. **Two fixture traps any later phase writing e2e will hit**: `Customer.contactPhone`/`contactEmail` are not columns (`…Enc`, written through the service's encryption); and **`AuditLogEntry` cannot be deleted at all** — the Part 10.3 immutability trigger rejects every DELETE, so audit rows written into a second Organization pin that `Organization` row forever through its FK. Cleanup suspends user triggers for one transaction via `SET LOCAL session_replication_role = replica` — the owner-role bypass that trigger's own migration documents as residual risk, and which **`ibms_app` genuinely cannot perform**, being NOSUPERUSER: step 8's runtime role closes most of that gap as a side effect. A third hazard was designed around rather than discovered — signup refuses to guess an Organization once a second exists (the step-7 guard), so every account is created before office B is stood up, and office B is removed at the **start** of the run as well as the end, because a crashed run leaving it behind would break `makeUser` in every later spec file. **Verification**: typecheck 6/6, lint 3/3, build 3/3, unit api **2800/2800** · web 16/16 · db 15/15; api e2e **398/398 across all 70 files, zero failures**. | **Do NOT start Phase 3 without an explicit go-ahead** — Phase 2 is now complete (steps 7-9). **Four Part V multi-tenancy items remain genuinely unverifiable today** and are not gaps to chase: subdomain-scoped JWT rejection (Phase 4, §4.10), `InsurerFormTemplate` sharing (Phase 3, §5), per-tenant email sender (Phase 3, §6), legacy bulk import (Phase 5, §7). **On this host**: the 398/398 figure is a single uninterrupted `npm run test:e2e` (exit 0, 1326.97s), run from the prompt with `!` after seven attempts from inside the session were each killed; it matches a seven-batch foreground pass test for test. **A full e2e run here takes ~22 minutes and 35 files alone exceed a 10-minute foreground timeout** — run it with `! npm run test:e2e`, or as foreground batches of ~10 files, and never pipe it through `sed`/`grep`, which buffers the output so a killed run leaves an empty log. **When writing an e2e that needs a second Organization**, reuse this suite's shape: create every account first, stand the second office up last, and make teardown self-healing at the start of the run. |
| 2026-09-11 | **Multi-tenancy Phase 2 step 8 — PostgreSQL Row-Level Security, the independent second layer.** Spec §1 asks for two layers that fail independently; step 7 built the first, this is the second. **118 policies**, one per tenant-scoped table, keyed to `current_setting('app.current_org_id', true)` with `WITH CHECK` mirroring `USING` so writes are governed too. The table list is derived from `schema.prisma` by the same rule the runtime extension uses, so the policies cannot cover a different set of tables than the application layer does. **The detail that silently defeats all of it**: Postgres exempts a table's OWNER from its own policies. The tables are owned by `ibms`, which also runs migrations and the seed — so the API now connects as **`ibms_app`**, a role that owns nothing, has no SUPERUSER and no BYPASSRLS, and holds only SELECT/INSERT/UPDATE/DELETE. `npm run db:provision-app-role` creates it (deliberately NOT a migration: a role is cluster-level, needs CREATEROLE, and its password is a credential); the migration GRANTs to it and REFUSES to run if it is missing. New `APP_DATABASE_URL`, wired through `.env.example`, docker-compose and CI; if unset the API still boots but logs a loud error that layer 2 is inert. **Also folded in**: `SecurityConfig` stops keying on a fixed `"default"` row id and is keyed by `organizationId` — the same single-tenant assumption step 5 removed from the unique constraints. **Four things this found that nothing else could.** (1) **Raw SQL was protected by nothing.** `$queryRaw` has no `where` for the application layer to rewrite, so those ten call sites (full-text search, `FOR UPDATE` locks, the watchlist containment query) were never covered by layer 1 at all — RLS caught it the moment it went live, as four search e2e tests went from unfiltered to returning nothing. They now run inside the same session-variable transaction. **Any future raw-SQL path must too.** (2) **Every request 401'd**: the auth bootstrap deliberately skips layer 1, but RLS fails closed, so unscoped reads on `ibms_app` returned nothing. Resolving WHICH Organization a caller belongs to is inherently a cross-tenant read, so the bypass now also selects the connection — owner inside `auth-bootstrap`, `ibms_app` everywhere else, both driven by the same closed union. (3) **A concurrency bug**: `inScopedTransaction` was a mutable flag on the shared request store, so when a request issued queries in PARALLEL (every dashboard does) the first to open its RLS transaction set the flag for all of them; the siblings skipped opening their own session and were rejected with `42501`. Now scoped to the transaction's own async subtree. It failed CLOSED — the write was rejected, never misfiled. (4) **The lazy `PrismaPromise` trap, a second time**: handing `work` straight to `storage.run` returns the unexecuted promise and closes the store before the query runs. Both sites now `await` inside, commented as load-bearing. **DECIDED — one transaction per query, not per request.** `SET LOCAL` is transaction-scoped, and must be: a bare session-level `SET` can leave a STALE org on a reused pooled connection and be in effect for the next request, which is a real cross-tenant read rather than a fail-closed one. Measured cost +4ms / ~3.7x on a trivial query; explicit `$transaction` blocks pay it once. Per-request would amortise it but holds locks longer — rejected as trading a measured, tolerable cost for an unmeasured risk. **Do not revisit without load-test numbers** (`docs/multi-tenancy-rls.md`). **Verification**: 118 policies on both databases; owner reads all rows (migrations/seed keep working), `ibms_app` reads ZERO with no org set and the full set with it; cross-org INSERT rejected (`42501`), cross-org UPDATE by primary key affects 0 rows; `ibms_app` confirmed to have no SUPERUSER/BYPASSRLS and own nothing. +8 unit tests → api unit **2796/2796**, web 16/16, db 15/15; `typecheck` 6/6, `lint` 3/3, `build` 3/3; api e2e **379/379 across all 69 files, zero failures**. | **Phase 2 step 9 is the only thing left in this phase — do not self-select it.** It is the Part V multi-tenancy checklist across two real Organizations, including the direct-ID-guess probe (`GET /customers/{id}` on another org's real id must 404, not 403) and the "disable the app filter, RLS still returns nothing" proof. Three of its items are already verified directly against the database (raw-SQL fail-closed, the `ibms_app` role attributes, the cross-org INSERT/UPDATE behaviour) but not yet as automated tests. **Operational**: `APP_DATABASE_URL` must be set in every environment or layer 2 is inert — the API logs an error but still boots. `npm run db:provision-app-role` needs a role with CREATEROLE; in CI and a fresh `docker compose` the default `ibms` is the cluster superuser, so it just works. On a database where `ibms` was hand-created as a plain role it is not — this dev box needed `ALTER ROLE ibms CREATEROLE` once. |
| 2026-09-11 | **Multi-tenancy Phase 2 step 7 — the application layer actually enforces tenant isolation now.** Spec §1 asks for two independent layers; this is the first. **RLS (step 8) is NOT built, so this is currently the ONLY thing between two offices** — which is why it fails closed rather than passing a query through. **`tenantScopeExtension`** (a Prisma client extension on the one `PrismaService` every repository already goes through) injects `organizationId` into every query against every tenant-scoped model, and REFUSES to run one when there is no Organization in context. No call site passes the org — spec §1: "a developer should not be able to write a query that skips this." **The model list is read from the Prisma DMMF at startup, never hand-kept** (118 models today): §8 warns "a missed table is a real isolation hole", and a hand-maintained list is exactly how a table gets missed. **Context** is an `AsyncLocalStorage` store opened by middleware BEFORE the guards — an interceptor is too late, because the JWT guard itself reads `UserSession`/`User`/`SecurityConfig` while working out who the caller is. `JwtStrategy` fills the store in via `adopt()` the moment the session resolves. The six anonymous auth routes bypass filtering ONLY for the lookup that identifies the caller, then `adopt()` that user's org so every write after it is scoped; `UnscopedReason` is a closed union with one member, so adding a bypass is an edit to a type. **All 14 background jobs** now run once per ACTIVE Organization through `PerOrganizationRunner`, each resolving THAT office's own service account, with per-org failure isolation. `watchlist-sync` is deliberately NOT converted — it touches only the three global watchlist models (§3.1), so the public sanctions lists are synced once for everyone, not once per office. **The temporary `@default` is gone** from all 118 columns (migration `20260927100000`), and a write that escapes the extension now fails loudly — proven: `23502 null value in column "organizationId" violates not-null constraint`. **Two judgment calls worth knowing.** (1) Dropping the default made `organizationId` required in Prisma's create types — 126 compile errors. Fixing those by passing the org at each call site would have rebuilt the "every developer remembers" model the spec rejects, so the field is declared `@default(dbgenerated())` instead: Prisma treats it as database-supplied and stops demanding it, while the DATABASE KEEPS NO DEFAULT (verified — `migrate diff` emits nothing for it). Type-level relief, zero runtime weakening. (2) The 68 e2e spec files imported the raw client and would have bypassed the extension for ~450 fixture writes; they now import `test/tenant-prisma.ts` (one import line each) so fixtures exercise the same path the API does. **Three real bugs found by building and testing it**: `createManyAndReturn` was missing from the operation sets, so `PolicyRepository.attachDocuments` slipped past unstamped (caught by e2e as a 500) — fixed, and **unknown operations now fail closed too**, because a missing WRITE was caught by NOT NULL while a missing READ would have leaked silently; `PrismaPromise` is LAZY, so the test client's wrapper received an unexecuted promise and closed the context before the query ran (the `await` inside it is load-bearing and commented as such); and `Prisma.TransactionClient` describes the RAW client, so every "client or tx" helper rejected one of them until `TenantTransactionClient` was derived from `$transaction`'s own signature. **Verification**: +28 unit tests (extension 21, runner 7) → api unit **2786/2786**, web 16/16, db 15/15; `typecheck` 6/6, `lint` 3/3, `build` 3/3; api e2e **378/379, the one failure the documented TOTP-enrolment timing flake, 20/20 in isolation**. | **Phase 2 is NOT done — steps 8 and 9 remain, and do not self-select them.** Step 8 is the PostgreSQL RLS policies, the independent second layer: until it lands, a raw SQL query or a bug in the extension has nothing behind it. The approach is already decided (2026-09-11): a separate non-owner `ibms_app` role the API connects as, with `ibms` kept as owner for migrations/seed — `ibms` owns all 126 tables and Postgres exempts table owners from RLS unless it is FORCEd, which would also hit migrations. That needs a second `DATABASE_URL`, plus docker-compose/CI/`.env.example` changes. Step 9 is the Part V multi-tenancy checklist across two real Organizations. **Still single-tenant in practice until Phase 4**: login resolves a user ACROSS all Organizations, because the subdomain that would identify the office is not read yet (§4.10). `AuthService.signup` therefore resolves the platform's SOLE Organization and THROWS if a second exists — a deliberate refusal that blocks onboarding a second office through a path that cannot tell which office an account belongs to. |
Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
