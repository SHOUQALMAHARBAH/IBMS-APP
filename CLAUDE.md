@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-16 | **The Manager's sidebar, restructured — and the premise behind the task was wrong.** The brief said both broad roles get "one long flat list of 45/38 items". **The counts are exactly right and the flatness is not**: `AppNav.tsx` already had nine `NAV_GROUPS` with translated headers and permission filtering. Resolving the seeded `PERMISSIONS` grid against it gives **Manager 43 gated items + Home + Security = 45**, **Executive 36 + 2 = 38** — on the nose, which is the check that the parse is faithful. The real defects were narrower: **nothing collapsed**, there was **no search**, **two groups carried 25 of the Manager's 43**, and **three of their nine groups held exactly one item**. **(1) Taxonomy 9 -> 11, largest group 14 -> 8.** `Sales & clients` (13) split into New business / Clients / Policies; `Insights & reporting` (14) into Dashboards / Performance & analysis; the one-item `Claims` group dissolved into the latter. **One taxonomy filtered per role, never a per-role tree** — what varies per role is ORDER (`GROUP_ORDER_BY_ROLE`, Manager only so far). Item order inside the six untouched groups is deliberately unchanged. Manager still totals 43 and Executive 36, item for item: **the regrouping adds and removes no destination**, because that would be a permission decision. **(2) Collapse state is an external store, not `useState` + a mount effect.** Reading `localStorage` in an initialiser is the hydration bug already fixed in `LanguageProvider`; adopting it in a mount effect instead is a **lint error** here — `react-hooks/set-state-in-effect` — whose own message names the way out. `nav-open-groups.ts` is therefore a `useSyncExternalStore` source: `getServerSnapshot` renders the SSR default, `getSnapshot` takes over after hydration, **no effect and no setState anywhere**. Only groups the user actually toggled are stored, so a group added later is open when you are in it rather than inheriting a stale `false`. **(3) Native `<details>`/`<summary>`** — keyboard and screen-reader semantics free, and **the disclosure marker flips for RTL with no code**, verified in the Arabic screenshots. Not an accordion: opening one group does not close another. **(4) Search folds Arabic** (`lib/i18n/fold.ts`): alef variants, alef maqsura, ta marbuta, diacritics, tatweel. A raw `includes()` would tell a user who typed a module's name correctly that it does not exist. Match count renders through `tPlural`. **(5) `navRetention` EN 'Retention' -> 'Customer retention'** — it sat beside 'Retention & Disposal' (the PDPL schedule) and a search for "retention" made the collision worse. **AR needed no change**: it already read الاحتفاظ بالعملاء. **Three defects found by running it, not by reading it**: my own blast-radius claim ("zero specs locate nav links") was **wrong** — a one-line regex missed chained locators in `home.spec.ts` and `policies.spec.ts`; the new search box collided with four pages' own search boxes because **`getByLabel` is substring-matching** and "Search modules" contains "Search"; and the group items rendered **outdented from their own heading** because a list-item marker sits outside the box. **Verification**: typecheck 6/6, lint 3/3, build 3/3; unit **api 2927 / web 57 (from 43) / db 15**; **`npm run test:a11y` 66/66**; web e2e **296/296 (from 288)** — +8 new `sidebar-manager.spec.ts` tests. Screenshots captured in all 16 combinations of light/dark x AR/EN x collapsed/expanded/search, per role. **The Executive followed in a second commit** — the mirror order (Dashboards, Performance & analysis, Finance ... Clients, New business last) plus the one hoist, and **9 groups rather than 11**: the role holds nothing in Privacy or Operations, so neither renders. Its spec caught a fixture trap worth remembering — a hand-rolled `{}` for `/kpi-dashboard` crashed the page into **Chrome's own "This page couldn't load"**, which reads like a sidebar failure and is not one; the spec now anchors on `/customers`, whose fixture shape this suite already proves, and reads group order by **`href` attributes rather than `innerText`**, because a collapsed group's links have no rendered text. Final web e2e **302/302**. | **`getByLabel("X")` is a SUBSTRING match** — adding any control whose label contains an existing one's label breaks four specs at once with a strict-mode violation. Check before naming a new control. **A closed `<details>` removes its contents from the accessibility tree**, so `getByRole` finds zero — assert DOM presence by `href` and visibility by role; a collapsed item and a permission-hidden item are NOT the same thing and must not be tested the same way. **`allInnerTexts()` does not auto-wait** — reading it before `/auth/me` resolves returns `[]` and the assertion fails for the wrong reason. **Never read `localStorage` in a `useState` initialiser, and do not reach for a mount effect either** — `useSyncExternalStore` is the pattern this repo now uses; see `nav-open-groups.ts`. **Directive §5 is NOT satisfied and that is deliberate** — 43 grouped entries are still 43 top-level entries. The prune that would satisfy it moves PAGES, needs client sign-off, and is recorded in README § Known gaps. Do not self-select it. **Both roles are now ordered; every OTHER role still falls back to declaration order** — that is expected, not a regression. **Per-role item order is a `hoist` list, not a second taxonomy**: `NAV_ORDER_BY_ROLE` carries `groups` and an optional `hoist` of hrefs lifted to the top of their own group, because `/dashboards/executive` is the first screen an Executive opens and the last a Manager does, and it cannot be both eighth and first in one global list. Add exceptions there, never a per-role item tree. |
| 2026-09-15 | **The hero gradient finally reaches the screen it was sampled from — and a spec that was never testing the build it was pointed at.** **(1) The auth screens.** `--nav-gradient-from/-to` (`#273a4e -> #33516d`) was measured out of the approved login comp, yet only the sidebar ever used it; login / signup / forgot / reset were a white card on grey. They now carry it, via **two ADDITIVE exports** in `auth-form.styles.ts` — `authPageStyle` and `authCardStyle`. Additive is the whole point: `pageStyle` and `cardStyle` there are imported by **86 authenticated screens** as well as the four `(auth)` ones, so editing them would have painted navy behind the entire app. Same two tokens, no new ones, and the **same `180deg` the sidebar uses** — vertical carries no physical direction, so it needs no RTL mirroring where a diagonal would have to be flipped for Arabic. **(2) The dark-theme boundary, measured rather than eyeballed.** On the new ground the card separates at **8.42:1 in light** but **1.00:1 in dark** — `--surface-card` `#1e2733` on a `#131a21`-`#1b2735` gradient has no surface separation at all, so its edge rests entirely on the border, and `--border-default` gives that **1.35:1**, under the 3:1 WCAG 1.4.11 wants of a boundary that carries meaning. **Two approaches were priced and rejected with numbers**: darkening the gradient cannot work — even a pure-black ground reaches only **1.31:1**, because two dark surfaces cannot separate by luminance — and no existing surface token works as a lighter card (`--surface-hover` reaches 1.14:1). `authCardStyle` therefore borders with **`--ink-muted`**, the lightest token that clears it: measured **4.99:1** in the final render. `--ink-secondary` (7.11) and `--brand-300` (7.31) also clear but read as a bright ring drawn around the card. **(3) `--shadow-lg` was tried and reverted.** It moved dark separation 1.09 -> **1.12** — imperceptible; the comment justifying it was contradicted by its own measurement, so it is gone rather than shipped. **Verification**: `tsc --noEmit` 0, lint 0, build 3/3; **`npm run test:a11y` 66/66, exit 0**; web e2e **288/288, exit 0**; **0 hydration errors** across light/dark x AR/EN, `dir` correct in each. Contrast sampled off the FINAL render, not carried over from the sidebar: heading **13.34:1** light / **12.71:1** dark. | **`pageStyle` and `cardStyle` in `auth-form.styles.ts` are shared with 86 authenticated screens — add an auth-only export, never edit those two.** That file's own header warns about it and it is easy to miss. **A gradient for this app is vertical (`180deg`).** A diagonal would need mirroring for RTL; vertical sidesteps it entirely, and matches the sidebar. **axe cannot see any of this.** Its contrast rule is text-only: 1.4.11 non-text contrast is not in the default ruleset, so a card boundary or a control border can sit at 1.3:1 with the suite fully green. Measure it by hand when a surface changes. **A THIRD dark-mode gap is now tracked, and is NOT addressed**: input borders measure **1.35:1 light / 1.37:1 dark** against the card — `--border-default` on `--surface-card`, **pre-existing and app-wide**, untouched by this change. It is a stronger 1.4.11 candidate than the card edge, because those *are* controls. It joins the two already recorded — `--ink-muted` on `--surface-hover` (4.34, zero consumers) and `--nav-ink-muted` on `--nav-item-active` (2.91, never renders). Do not self-select any of the three. **Found incidentally, fixed in the commit after this one**: `part-h-detail-pages-screenshots.spec.ts` was the **only spec of 75 that hard-coded `http://localhost:3000`** in its `page.goto` calls, so under `PLAYWRIGHT_BASE_URL` its four tests silently exercised whatever was on :3000 instead of the build under test — and passed only because a dev server happened to be up. |
| 2026-09-15 | **Plural rules, because `count === 1 ? '' : 's'` is a wrong answer for four of Arabic's six cases — and a hydration bug the same bilingual work had been hiding.** **(1) The last English in the app was interleaved with interpolations** — **32 sites across 17 files**, not the 29 previously recorded; a bare-JSX-text scan structurally cannot see them because the text node itself contains `{`. Only **14 were count-dependent**; the other 18 were unit suffixes, fixed range labels and date sentences needing plain keys. **(2) `lib/i18n/plurals.ts` is a new, deliberately SEPARATE namespace.** `Intl.PluralRules('ar')` selects between **six** CLDR categories (zero / one / two / few / many / other) where English selects between two. Plural values are objects of forms, so widening the flat dictionary to `string \| PluralForms` would have made every existing `t()` call site's return type a union and broken the merged-dictionary tests; keeping `PluralKey` and `TranslationKey` as separate unions instead means `t()` cannot resolve a plural key to `[object Object]` and `tPlural()` cannot take a key with no forms — **both are compile errors**. `other` is the only mandatory form, and a missing category falls back **straight to `other`, never to a neighbour**: borrowing `few` for a missing `many` hides the gap behind a form that is grammatically wrong for that count anyway. The two sentences carrying two counts compose two `tPlural()` calls rather than inflecting a compound. **`LOCALE_BY_LANGUAGE` is now exported from `format.ts`** rather than duplicated — two copies of `'ar'` vs `'ar-JO'` would drift, and the drift shows up as a plural form not matching the numeral beside it. **(3) Four dashboards were printing `{rawValue} JOD`** — an ungrouped number with a hand-written suffix, bypassing `formatMoney` entirely. Now `JOD 37,500.000` like every other figure; 7 spec assertions updated to match. **(4) A hydration failure on the sign-in screen, reported from a real browser.** `LanguageProvider` read `localStorage` in its `useState` initializer, so the first client render could pick `'EN'` while the server — which has no localStorage and no locale cookie — rendered `'AR'`, and React discarded the server HTML. **The flaw predates this branch**; it was invisible while the four `(auth)` screens were hard-coded English, because both sides then rendered the same literal regardless of language state, and it surfaced the moment the 403/auth commit made them call `t()`. The initializer now returns the SSR default and a mount effect adopts the stored preference. **Verification**: typecheck 6/6, lint 3/3, build 3/3; web unit **43/43** (from 19 — +24 plural tests pinning the Arabic boundaries 0, 1, 2, **3–10**, **11–99**, 100+ and that six counts produce six genuinely distinct strings); **`npm run test:a11y` 66/66, exit 0**; web e2e **288/288, exit 0**. Playwright ran against a private `next start` via `PLAYWRIGHT_BASE_URL`, leaving the developer's own dev server untouched. The hydration fix additionally carries a **negative control**: the old initializer restored temporarily reproduces the exact error on `localStorage='EN'`, and all three states are clean once fixed. | **Never express a count with a ternary — use `tPlural()`.** `'s'`/`''` cannot express Arabic, and the ternary is invisible to every gate: `tsc` sees two strings, and an English-only test passes on both branches. **Never read `localStorage` (or anything else the server cannot see) in a `useState` initializer.** It is a hydration mismatch waiting for the first component that renders translated text, and it stays silent until then. The SSR-renderable default goes in the initializer; the client-only value goes in a mount effect. **A plural key's `other` form is mandatory and is the only fallback** — do not add a neighbour-category chain. **Money renders through `formatMoney`, always** — a hand-written currency suffix on a raw value is both unformatted and inconsistent with every other figure on screen. **Two environment notes, not code ones**: `next dev` and `next build` both own `.next/`, so running them concurrently produces torn writes in `.next/dev/types/*` that surface as syntax errors in a *generated* file — clear the directory (gitignored) rather than hunting the code. And after `wsl --shutdown`, Docker and Postgres come back on their own but **`npm run dev` does not**; the API takes roughly 90 seconds to bind :4000 after `tsc` reports 0 errors, so a missing port is not yet a failure. |
| 2026-09-15 | **`policy.ts`'s 98 orphaned keys, audited one at a time — and a sixth of them were not debt at all.** The working assumption was dead weight. **15 of the 98 were missing wiring**: the string each was written for was still hard-coded in the component that owns the key. Six had drifted in wording and were corrected **to the text already on screen**, never the other way round — `policyPlaceButton` "Place the policy" -> "Place policy", `policyCheckButton` "Check the policy" -> "Run check", `endorsementCreateButton` "Create endorsement" -> "Request endorsement", and three more. Five matched exactly and just needed calling. **Four were the labels for a `<select>` rendering raw snake_case tokens** — `bank_transfer` / `cheque` / `card` / `cash` were their own option text, and `financeMethodBank` and friends had been written for precisely that and never referenced. **The audit surfaced a real bug**: `claimNotifyingButton` ("Notifying…") sat unreferenced while the claim-notify button's busy state called `t('policyIssuingButton')` — which reads **"Recording…"**. Notifying a claim showed the policy module's label. **The other 83 are genuinely dead and are deleted**, after confirming zero references anywhere including `e2e/`: 33 never-built copy, 17 headings superseded by `pold*`, 11 claim status/type labels, 11 written for a tabbed billing/collection UI that does not exist, **9 `policyStatus*` superseded by `policyState*`** — five of which name statuses absent from the `PolicyStatus` enum, the remnants of the very defect `lib/policy/policy-status.ts` was created to fix — and 6 finance/commission labels. **Scope was widened deliberately**: the same components held **28 hard-coded strings with no orphan at all** (`Remove`, `Attach`, `Closure`, `DISCREPANCY — Delivery blocked`, the delivery-method select's four English labels), so **26 new keys** were added rather than leave those files half-fixed. **And the last raw enum on those screens is gone**: `FinanceSection` printed `{invoice.status}` verbatim, so a new `INVOICE_STATUS_LABEL_KEY` covers all six `InvoiceStatus` members. That one had a trap — `financeCollectedLabel` is **already "Collected"** on the same screen, so the new status label collides with it and `getByText("Collected", { exact: true })` matches twice; the status element carries `data-testid="invoice-status"` and `rfq.spec.ts`'s six assertions now check that field's text rather than hunting the page for a string that was unique only by accident. **Verification**: typecheck 6/6, lint 3/3, build 3/3; dictionary tests 13/13; **`npm run test:a11y` 66/66, exit 0** — held; web e2e **288/288, exit 0**. `policy.ts` AR 247 / EN 247, exact parity, **0 unreferenced**; app-wide **61, down from 159**. | **An unreferenced key is a question, not a verdict — audit before deleting.** One in six here was a translation waiting for its call site, and the cheapest way to lose a translation is to bin the key because nothing references it *yet*. **When a key's wording has drifted from the screen, the screen wins**: correct the key, because the visible string may be a locator — `Run check`, `Notify claim`, `Quality-control check`, `DISCREPANCY — Delivery blocked` and `Change` are all asserted by `rfq.spec.ts`. **Render an enum through a `Record<Enum, TranslationKey>` map, never raw** — `POLICY_STATUS_LABEL_KEY`, and now `INVOICE_STATUS_LABEL_KEY`, `RECEIPT_METHOD_LABEL_KEY`, `DELIVERY_METHOD_LABEL_KEY`. A new member should be a compile error, not a token a user reads. **A unique-looking string is not a stable locator.** `getByText("COLLECTED")` worked only because the raw token happened not to collide; translating it put two "Collected"s on the screen. Prefer a testid on the element under test. **A host note, not a code one**: C: hit **0 bytes** mid-run — typecheck died `ENOSPC`, a build crashed `0xC0000409`. `docker_data.vhdx` is **41 GB** and never auto-shrinks; only Playwright artefacts and logs were reclaimable here, and an elevated `diskpart` compaction is still outstanding. Every figure above is from a clean run afterwards. **The full api e2e suite will need Docker up and more headroom than this machine currently has.** |
| 2026-09-15 | **Hint text and accessible names — the last two places English was still hiding, and one of them no test could see.** Three clusters, all of them copy the earlier bilingual passes never claimed. **(1) Placeholders.** A previous row put the figure at thirteen; that was an undercount **from the scan, not the tree** — the regex was anchored to a letter (`placeholder="[A-Za-z]`), so it skipped every numeric example. There are **27**. Thirteen carried English prose and now read from the dictionary; **fourteen are format examples and are deliberately left alone** (`"17500.000"`, `"+962-7-..."`, a UUID sample) because `lib/i18n/format.ts` pins Arabic to `'ar'`, never `'ar-JO'`, **specifically so numerals stay Western** — verified against Node's ICU, where `(17500).toLocaleString('ar', …)` returns `17,500.000`, byte-identical to `en-GB`. An AR key there would be the same string as its EN twin. **(2) Thirty template-literal `aria-label`s were English** — `` `Confirm access for ${name}` ``, `` `Document ${i+1} file name` `` — across 18 files, now interpolating through `translate()`'s existing `params` argument. **axe cannot catch these**: `color-contrast` and the label rules check that an accessible name *exists*, not what language it is in, so all 66 `@a11y` specs passed over them. Neither could the functional suite, which asserts visible text. **(3) Four ageing-bucket labels** on `/financial-report` (`"1–30 days"`…) rendered English between two siblings that were already `t()`-driven. **Reuse beat invention where the concept was already owned**: three placeholder sites took existing keys rather than new ones — including `opMitigationActionLabel`, which `operational-pi-risk` **already used as a placeholder 200 lines above** the site that hard-coded the same English. Two aria-labels matched existing copy exactly (`complaintsTextAria`, `customersViewProfileAria`) and were **deliberately NOT reused**: generic wording shared by two domains is not shared ownership, and the DSR and prospects screens got their own keys. **Every EN value is byte-identical to the English it replaced** — `screening-matches.spec.ts` locates a control by `getByLabel("Review reason for Ahmad Khalid Yousef Al Hashimi")`, and an accessible name is a locator as much as it is copy. **Verification**: typecheck 6/6, lint 3/3, build 3/3; dictionary tests 13/13; **`npm run test:a11y` 66/66, exit 0** — held; web e2e **288/288, exit 0**. **2,749 keys per language** (+44), exact AR/EN parity, 0 collisions, and **159 unreferenced before and after** — no orphans introduced. | **An `aria-label` and the visible label of the same control must be in the same language.** A screen reader and the screen disagreeing is the defect this closed, and **no gate in this repo can detect it** — axe checks a name exists, Playwright asserts visible text. It is a review question, not a test one. **Numeric placeholders stay untranslated on purpose** — see the `'ar'`-not-`'ar-JO'` note in `lib/i18n/format.ts`; do not "finish the job" by adding AR keys that duplicate their EN twins. **Keep an accessible name byte-identical when you move it into the dictionary** unless you mean to update the spec that locates by it. **One category remains and is NOT started — do not self-select it**: English text interleaved with interpolations, which a bare-JSX-text scan structurally cannot see because the text node contains `{`. A filtered pass finds **29 sites across 14 files** — unit words (`JOD` on four dashboards, `minutes` on `/settings/security`, `days`), pluralisation fragments (`{n} invoice(s) across {m} customer(s).`, `entr{y\|ies}`, `line{s}`) and a handful of full sentences. Pluralisation is the real work there: Arabic has more plural forms than the `s`/`no-s` ternaries these sites use, so it needs a plural rule, not a key. |
Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). This paragraph predates
Domain B #12–22, Domains C–H and Parts D–F, all of which have since been built —
**root `README.md` § Scope status is the authority, not this summary.** Part F's
bilingual UI is complete: all 93 screens — the 89 under `app/(app)/` and the four
`(auth)` screens that render before sign-in — read from
`apps/web/lib/i18n/translations/`, including the 403 permission-denied branch
(see § What's New). Part G's final verification
checklist has not been run as a formal, evidence-attached gate. See § Known gaps for
each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web

# Demo data for two Organizations, through the real HTTP API — DEV DB only,
# never .env.test. Sign in with the password, then pair an authenticator at
# Settings -> Security. See apps/api/scripts/README-SEED-DEMO.md.
npm run seed:demo -w api
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
