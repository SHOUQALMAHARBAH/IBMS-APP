@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-26 | **The owner ruled on the two umbrellas Phase 4 left open, and one of them turned out to gate two different things.** `sla.policy.manage` SPLIT (routine — `sla.policy.regulatory` already stands apart for the decision that carries the regulatory weight, so splitting the CRUD verbs around it takes nothing from it). **`user.manage` NOT SPLIT, and the reasoning is the part that must survive**: it anchors the last-administrator lockout guard at four routes, that guard exists because an office losing every administrator cannot repair itself from inside the system, and the benefit on the other side is view-without-edit on one screen. **IMPROVEMENTS § 3.15 records the ORDER if it is ever revisited — the lockout guard is re-proven FIRST and the split follows, never the reverse**, because a split landing first and leaving the guard to be re-verified later is exactly how a safety control gets weakened by a tidy-up. **FOUR successors, not three, because the umbrella gated two ENTITIES**: its fifth route is `POST /sla/holidays`, which adds a public holiday to the business-day calendar — not an SLA policy. Mapping that onto `sla.policy.create` would be the lie Phase 1 refused when it split `insurer.relationship.manage` five ways rather than pretend an insurance line was an insurer. The two jobs genuinely differ: a holiday is clerical and factual, an SLA duration is a policy decision with regulatory weight, and an office can now grant one without the other. `sla.policy.read` already existed as the fourth action and is held by FIVE roles rather than the umbrella's three, deliberately, and is untouched — the holiday READ stays on it, because viewing the calendar is part of understanding how a deadline is counted and a code nothing needs is worse than a shared one. **AND THE SPLIT FOUND A REAL DEFECT (§ 1.57).** `SlaHoliday` is **0 rows on dev and 0 on db-test**, nothing seeds it, and its only writer has **no web caller** — so an office cannot enter Eid, while the arithmetic genuinely consults the table. **The failure direction is SAFE and that is the whole reason it is P2**: `business-days.util.ts` says in its own header that without a calendar a deadline is *"a lower bound (never later than the true legal deadline)"*, so the system chases staff EARLY. The real cost is a FALSE BREACH pointing the wrong way — two holidays inside a ten-working-day window make the true deadline two days later, so the breach reports and any PDPL evidence drawn from them over-report the brokerage missing deadlines it actually met. **Four plants, each killing two tests from both directions**: the holiday route re-gated onto a policy code, the off switch onto the update, and both web controls onto each other. **And the from-empty check ran BEFORE the commit this time** — a throwaway database, 110 migrations from nothing, then the seed, 216 permissions: the routine the sibling migration's CI failure earned. | **216 codes, 20 five-state families over 56 codes, 160 toggles.** The mirror of the commit before it: `payment-channel` turned a toggle INTO a family, `sla.holiday.create` adds a lone CRUD verb and lands in the toggle list, while `sla.policy` was already a family and simply grew from 2 members to 4. Families do not move; the other three numbers do. **Run the from-empty check on any migration that reads existing rows** — create a throwaway database, `migrate deploy`, then `db seed`. A guard comparing against what it FOUND rather than what it was OWED cannot run before the seed, and both local databases are already seeded so neither can see it. **`user.manage` is closed, not pending** — if a future change wants it, § 3.15 names what has to be proven first. **Still open on four-action**: nothing. Phase 3 is § 3.14 and is a product decision, not a permission code. |
| 2026-09-26 | **The permission that says where client money goes was one checkbox for three acts — and the measurement is what decided which umbrella to split.** Four-action Phase 4, migration `20261029100000`. Phase 4's brief is "deactivate codes where a real deactivation scenario exists", so the first job was finding them: **21 `*.manage` codes remain, and SIX gate a deactivation alongside a create.** Five are NOT split here and the reasons are the content: `retention-case.manage` and `risk-register.manage` gate a `:id/close`, which is a terminal WORKFLOW state and not the deactivation of a configured entity — the distinction that keeps `claim.close` its own code rather than a `claim.deactivate`; `email.integration.manage` **has no web caller at all** (§ 1.44), so splitting it would add codes nothing can exercise, which the enforcement guard from the commit before this one now refuses; and `sla.policy.manage` and `user.manage` are real candidates deliberately deferred — **`user.manage` is the anchor of the last-administrator lockout guard**, read at four routes into it plus the segregation signal, so splitting it is a decision about a safety control rather than a tidy-up. **`payment-channel.manage` went first because of consequence**: a payment channel is where client money is SENT, and adding one is not the same capability as listing them. Its own description already read as three things — "add / disable ... and list it when recording a receipt or remittance". Now `.read` / `.create` / `.deactivate`, and **no `.update`**, because the controller is create/read/disable and a fourth code for symmetry is what Phase 1 declined three times. **NOBODY LOSES A CAPABILITY, AND HERE IT WAS PROVABLE BEFORE IT WAS WRITTEN**: measured first — the umbrella is held by FINANCE alone, and so are `receipt.record` and `remittance.record`, the two actions whose screens need the list — so no role needed the read and lacked it. Then measured after: db-test 1 umbrella grant → 3 successors; **dev 2 → 2 each, because dev has a SECOND OFFICE whose Finance role the seed would never have reached**, which is the whole argument for a migration over a seed change. **The migration's own assertion was PROVEN, not written**: a rolled-back transaction recreated the umbrella, dropped one successor grant, and ran the assertion verbatim — `ERROR: four-action phase 4: 1 (role, successor) grants were owed and not written`. **The separability spec's argument applies verbatim and is why two more cases exist**: every seeded role received all three successors, so NO seeded account can tell them apart — the exact condition that once left a Phase 1 plant invisible with the whole vendor suite green. So the api builds a role holding ONE code, and the web needed a new mock (`mockAuthWithCodes`) because no ROLE NAME can express "can see the list, cannot add a bank account" — which is precisely the state an office creates the moment it uses the Role screen. **FOUR PLANTS, each killing only its own test**: the list re-gated onto the create (1 dies), the disable onto the read (2 die, from both directions), the add form onto the read, the disable button onto the create. **And the refusal named the umbrella IN BOTH LANGUAGES** — the trap recorded when an Arabic message still said `insurer.relationship.manage` after only the English was fixed. It now names `payment-channel.read`, because the load is what failed and telling somebody they lack the ADD permission sends them to ask for the wrong grant. | **213 codes, 20 five-state families over 54 codes, 159 toggles.** The interesting arithmetic: a split that ADDED two codes REMOVED a toggle, because the umbrella was a toggle (one CRUD verb is not a family) and three successors qualify. Those four numbers are pinned separately in `permission-matrix.test.ts` for exactly that reason. **Regenerate both generated files after any permission change** — `npm run db:test:fixture:permissions` and `npm run db:permission-descriptions`; the fixture diff should be +2 net grants and nothing else, and reading it line by line is the rule. **Five copies of the grid had to move together**: the seed, the grid spec, the matrix pins, the web fixture, and the owner's description file. **`npm run test:e2e` config is `./test/vitest-e2e.config.ts` from `apps/api`** — there is no `vitest.e2e.config.ts`, and guessing cost one esbuild resolve error. **Still to come on four-action**: `sla.policy.manage` and `user.manage` (the lockout-guard decision), and Phase 3, which § 3.14 now shows is not a permission code at all. |
| 2026-09-26 | **Three checkboxes on the Role screen removed nothing, and the file the owner writes Arabic into had no generator behind it.** Phase 2's honesty pass, MEASURED rather than recalled: of 211 permission codes, **12 sit on no route guard, and nine of those are read by a SERVICE** — the `.all-owners.read` family widens a visibility filter, `customer.national-id.reveal` gates one FIELD of one response, `access-recertification.review.routine` tiers a reviewer pool. That is enforcement, just not at the route. **THREE were read nowhere in the application at all.** The sharpest is `refund.raise`, described as "the maker side" of a maker/checker pair: an administrator who unchecks it believes she has taken away the ability to raise a refund, and she has not — a refund is created inside `POST /endorsements/:id/calculate-adjustment` under `endorsement.apply`, because the refund and its commission reversal must move together. `claim.delete` promised "disabled by default, logged privileged override only", a control mechanism that exists NOWHERE; the capability this product actually has is `claim.discard`. **Nothing is exploitable** — `assertDifferentActors` and the CHECK constraints still refuse every self-approval — and that is exactly why it survived: the defect is that the screen makes a promise the code does not keep, which no test of behaviour can see. **NOT DELETED, and the reason is a rule this repo already made**: a `RolePermission` row is the record of what an office once granted, the same argument Phase 3 used to give `Role` no DELETE; and the house treatment for a capability that is not ready is to keep it visible and SAY so, which is what the mode screen does with COMBINED. So each description now OPENS with `NOT YET ENFORCED` and says what does gate the action. **The guard runs in BOTH directions**, which is what stops the list rotting: a code nothing reads must be declared, AND a declared code that gets wired must leave the list — otherwise `refund.raise` would gain a route while still telling the administrator it does nothing. Three plants, one per direction plus the description itself, each killing only its own test. **AND THE OWNER'S INPUT FILE HAD NO GENERATOR — the second time in this repo.** `docs/permission-catalogue-for-descriptions.txt` said "Generated from the seeded database, so this IS what the Role screen renders", was hand-written once at **186 codes against a catalogue of 211**, and 25 codes were therefore invisible to the person being asked to describe every permission — all of four-action Phase 1, all four discard codes, the duty-segregation mode. `npm run db:permission-descriptions` now generates it, reusing the SCREEN'S OWN `buildMatrix` rather than re-deriving the [5-STATE]/[toggle] grouping, because the copy is the one that drifts. **TWO DEFECTS IN MY OWN GENERATOR, both found by running it rather than reading it.** (1) The count of Arabic lines SHE has written was part of the file, so writing one line made `--check` report STALE until a developer regenerated — a gate going red because the owner did the work it exists to support. The bytes now depend only on the catalogue and on her own lines, so `render(catalogue, harvest(file))` equals `file` and the gate stays quiet while she works; proven end to end by writing a real Arabic line into the real file, watching the gate stay green, regenerating, and reading the line back intact. (2) The "regenerating would DELETE what you wrote" warning asked the HARVEST what had been written — so breaking the harvest returned nothing, there was nothing to report as dropped, and **the worst failure printed the mildest message**. Proven by planting a harvest that keeps nothing: only a generic "STALE". It now reads the file independently, and the plant makes it name the exact Arabic sentence that would be lost. A safety check may not be built out of the component whose failure it exists to catch. **And a plant of mine APPLIED and killed nothing** — it inserted dead code before the storing line instead of disabling it — § 1.51(d) on my own work, caught because I had named which test should die and it didn't. | **A new permission code must be READ by something before it is declared** — four-action Phase 1 already refused to declare three codes on that ground, and `packages/db/prisma/seed-data/permission-enforcement.inventory.spec.ts` now enforces it. If a code genuinely must exist before its route, declare it in `UNENFORCED` with a real reason AND open its description with `NOT YET ENFORCED`; the guard checks the description, not just the list. **Wiring one up means REMOVING it from that list in the same commit**, or the screen keeps telling the administrator it does nothing. **`npm run db:permission-descriptions` after any permission change** — `verify.sh` gates the `:check` form, and both variants read DB-TEST so write and check cannot disagree (the fixture generator's dev-vs-db-test split made its own gate unsatisfiable for a while). **Never run the write variant when `--check` says it would DROP lines** — that message means the harvest is broken and regenerating destroys the owner's Arabic. **A spec under `scripts/` is a spec nothing runs, AND a spec that imports across the repo root does not build**: `turbo run test` runs per-package scripts with no root vitest project (so a `scripts/*.spec.ts` is never invoked — § 1.51(b)), and `turbo prune web --docker` keeps only web's workspace dependencies, so a web spec importing `../../../scripts/...` typechecks on a laptop and fails inside the image — which is what CI caught, after six local gates went green. So the generator is SPLIT: everything decidable without I/O lives in `apps/web/lib/admin/permission-descriptions-input.ts` beside the matrix it already depended on, tested by `apps/web/test/permission-descriptions-input.test.ts`, and `scripts/generate-permission-descriptions-input.ts` is a thin prisma-and-fs shell over it. **Put the logic in the workspace, not in `scripts/`.** **The three unenforced codes are still granted and still on the screen** — deliberately, and withdrawing any of them is a product decision, not a tidy-up. |
| 2026-09-26 | **The mode was declarable and the screens could not carry a reason — for one commit. That window is now closed with ONE control, not twelve.** Lifting the shipping gate made a future wall a live one: fifteen routes required a `combinedDutyReason` from an approver who was also the maker, and one web control could send it. **`components/ui/CombinedDutyReasonField.tsx` is the field, the ten-character floor, AND `needsCombinedDutyDeclaration`** — the condition that decides whether to ask at all, exported because the button's `disabled` and the field's presence must be the same answer. Twelve textareas would be twelve places for the WORDING to drift, and the wording is the load-bearing part: it has to say the person raised this AND that the office allows them to approve it, or a mandatory box in front of an approval reads as an accusation. What differs per screen is only which column names the maker — `resolvedByUserId`, `approvedByUserId`, `overrideRequestedByUserId`, `assessedByUserId`, `draftedByUserId`. **THE CONDITION IS NARROW AND BOTH HALVES ARE ASSERTED, on the same row.** Asking on every approval would put a mandatory box in front of the hundreds of ordinary two-person approvals this product is mostly made of; asking on none is the 422. So it asks only when the office declared COMBINED, the approver is the maker, and the second half is unrecorded — and `combined-duty-declaration.spec.ts` drives ONE complaint closure twice: a COMBINED office gets a box, is still refused on NINE characters (the floor is ten, and a screen must not send a request it knows will 422), and sends the TRIMMED reason; a SEGREGATED office on that same row gets no box and posts `{}`, the body it always posted. **Two plants, and they kill DIFFERENT tests, which is what proves the halves are independently observed**: dropping the mode check (`always-asks-for-a-declaration`) killed the recommendation approval and the claim-settlement second approval in `rfq.spec.ts` — two ordinary two-person approvals, neither of which is about this feature — while the COMBINED test passed; dropping the maker check (`never-asks-for-a-declaration`) killed the COMBINED test alone. **B.7 FOUND A VIOLATION IN CODE WRITTEN THE SAME DAY**: two of my own twelve controls called the condition TWICE with identical input where the other ten compute it once, so the field and the button each decided separately whether a reason was required. Functionally identical today and two places to disagree tomorrow — fixed rather than recorded, which is what the pass is for. **And the doc-currency grep on the feature's NOUNS** ("nowhere to type", "cannot carry", "no web control") found four stale claims across README, both design docs and two prior What's New rows, every one of them written by me within the last two days and every one now instruction-shaped: *an office that declares COMBINED will find its claim settlement refused*. Corrected in place, not deleted. | **All twelve approve screens carry a declaration. The only pair left unwired is `AccessRecertificationItem`**, pending the owner's answer in `docs/decision-reviewing-your-own-access.md` — its row IS the decision, written by an INSERT with no later approval step to intercept, so the condition as drafted does not fit the mechanism. **Adding a thirteenth approve control? Import `needsCombinedDutyDeclaration` — never re-derive the condition, and never write a second textarea.** Give it the column that names the maker for THAT pair and the column that records the second half; the floor, the wording and the disabled button are already decided. **A screen that needs the office's mode reads `/auth/me`'s `dutySegregationMode`**, which stays OPTIONAL on `MeResponse` — ~80 spec files build that mock, and every reader defaults to SEGREGATED, which is what a caller that does not know must assume. **An absence assertion on a control needs an anchor from the SAME render**: the SEGREGATED test asserts the close button is ENABLED before asserting the reason box is absent, or an unhydrated page satisfies the absence and the test passes proving nothing. **Nine characters, not zero, is the assertion that pins a length floor** — a test that only types nothing cannot tell a ten-character floor from a non-empty check. |
| 2026-09-26 | **THE SELF-APPROVAL REPORT EXISTS, SO THE GATE IS LIFTED — and the gate, its refusal and the test that asserted it were removed in this same commit, together.** Part 4 step 6, which is the commitment the whole mode rested on: the owner accepted applying it uniformly, including to a person reviewing their own access, on the stated mitigation that every such act would surface in a report, flagged at the top — and that report, measured, had no reader at all. `GET /internal-controls/combined-duty-acts` now lists every declared act with the actor by name, the pair NAMED BY ITS CONSTRAINT, the reason, the hat, and the office's own mode and declaration date. **Rendered ABOVE the silent-self-approval scan** on the same screen, because that scan should always be a table of zeroes (the CHECK constraints refuse silent self-approvals) while anything in this report is a real act somebody performed. **THE FIRST ORDERING RULE, PROVEN BY CONTENT**: access self-reviews sort ahead of every transaction act whatever their dates and render FLAGGED — a position alone is something a reader re-sorts away. The proof puts a self-review of ACCESS and a self-approved REFUND in the office with **the refund deliberately NEWER**, and asserts both present AND the access one first; a report sorted by date would pass a test that only checked the access row was in there somewhere, and planting exactly that killed it ("expected 15 to be less than 14"). **Reading the report is itself audited — counts only, never a name**: a lookup that copied the report's contents into the log the report is about would pollute what is being searched. **And lifting the gate made a future wall a live one**, so the refund approve control now asks for a reason when — and only when — the office has declared COMBINED and the approver is the person who raised it. **Two findings about WHERE a claim can honestly be tested.** The empty report is asserted on the SCREEN, not in the api e2e: db-test is cumulative and the combined-mode spec leaves its acts behind as legitimate history, so "no acts in this office" is a state that database cannot be in, and an assertion on it would break on run ORDER. And a mock whose SHAPE differed from the endpoint's (`pairs` for `byPair`, `runAt` for `generatedAt`) crashed the existing section and surfaced as **Chrome's own "This page couldn't load"** rather than a React error — the third time this project has been bitten by that exact symptom, and it cost three wrong diagnoses before the saved error-context snapshot named it. | **COMBINED mode can now be declared.** The gate is met; `SELF_APPROVAL_REPORT_EXISTS` no longer exists. ~~**Fourteen of the fifteen approve screens still cannot carry a combined-duty reason** — the refund one can. An office that declares COMBINED today will find a claim settlement or a KYC approval refused with "give a reason" and nowhere to type it~~ — **CORRECTED by the row above**: all twelve carry one, through `CombinedDutyReasonField`. The window lasted one commit. **A new screen section reads from `/auth/me`'s `dutySegregationMode`**, which is OPTIONAL on `MeResponse` deliberately: ~80 spec files build that mock and a required field would fail every one of them over a value none of them are about — every reader defaults to SEGREGATED, which is what a caller that does not know must assume. **A Playwright mock must match the endpoint's SHAPE, not approximate it**: a wrong field name crashes the component and the browser shows its own error page, so copy the shape from the sibling spec that already mocks that endpoint. **Still open on Part 4**: the fourteen approve screens, and `AccessRecertificationItem` pending the owner's answer in `docs/decision-reviewing-your-own-access.md`. |
| 2026-09-26 | **An office can now declare how it separates duties — and COMBINED is refused in CODE until the report exists, not in a document.** Part 4 steps 4 and 5, shipped together because the owner approved them as one unit: a settable mode without screens that can carry a reason is a door opened onto a wall. **`SELF_APPROVAL_REPORT_EXISTS = false`** in `duty-segregation-mode.service.ts` makes the shipping gate real — declaring COMBINED returns 403 naming the missing report, an e2e test asserts that refusal, and the flag, the refusal and the test are deleted in ONE commit when step 6 ships. **`duty-segregation.mode.declare` goes to OFFICE_ADMINISTRATOR ALONE, and the omissions are the design**: Compliance, Executive and the external auditor hold `internal-controls.view`, which is the self-approval report, so whoever DECLARES the mode is not whoever REVIEWS the acts it permits — the same segregation principle one level up, applied to the control that weakens a control. The READ accepts either code, which is the one place `PermissionsGuard`'s OR semantics are what you want; the WRITE names one. **NO idempotent short-circuit, and my first version had one**: declaring SEGREGATED in an already-segregated office looks like a no-op and is not — "segregated because nobody ever chose" and "segregated because the administrator confirmed it on this date" are different facts, and the skip threw away the first explicit declaration an office ever makes. Caught by a test asserting the declaration stamps who and when. **`/auth/me` carries the mode** for every authenticated caller, because a screen that cannot know it either asks everybody for a reason or asks nobody and 422s. **Step 5 — the act ON the record**: one `combinedDutyActView` shape for all fifteen pairs (actor, when, reason, the pair named by its constraint, the HAT, and whether the hat was ambiguous), projected onto the endorsement's refund block so a reader sees that nobody else signed it without going to find a report. **THREE GUARDS FIRED ON THEIR BUILDER in one step.** The i18n collision check refused my `dutySeg*` keys — Part 5's readiness list already owns that prefix — so the screen's keys are `dutyMode*`. The e2e helper's own "a mistyped code grants nothing" assertion caught `customer.read`, which is not a permission. And the matrix pin moved 210 → 211 and said so. **Four plants, each stated before running**: the gate flipped to true (the gate test died, 403 → 200), the audit call replaced (the audit assertion died at 0 rows against 1 — added FIRST, because without it the plant had nothing to kill), the screen's permission check replaced with `true` (the read-only test died), and the fifteenth pair's escape column dropped from its write (201 → 500 from the CHECK). | **COMBINED still cannot be declared, and that is now enforced by the service rather than by nobody having built the screen.** The gate lives in one constant with one refusal and one test; when the report ships, all three go in the same commit. ~~**The approve screens still cannot carry a reason** — fifteen routes accept `combinedDutyReason` and no control sends one, which is fine while COMBINED is unreachable and is the next thing that must close if that changes.~~ **CLOSED** — it changed, and it closed: see the row above. **A new permission moved the pins again**: 211 codes, 160 toggles, 479 grants in the web fixture — regenerate with `npm run db:fixture:permissions`, never hand-edit. **Use `dutyMode*` for the mode screen and `dutySeg*` for the readiness list** — two adjacent features, two prefixes, and the i18n guard enforces it. **The office administrator declares; Compliance/Executive/the auditor read** — if a future role needs both, that is a decision about the control, not a convenience. **Still to come on Part 4**: step 6, THE SELF-APPROVAL REPORT — the gate, with access-recertification self-reviews as its first ordering rule, and `AccessRecertificationItem` still unwired pending the owner's answer in `docs/decision-reviewing-your-own-access.md`. |
| 2026-09-26 | **All fifteen maker/checker pairs now pass through one engine, and lint caught two of them not carrying what it returned.** Part 4 step 3 complete: 15 of the 19 call sites resolve through `DutySegregationService`, 1 is blocked on an owner decision, and 3 are application-only. **THE ACTOR IS A USER ID, NOT A SESSION OBJECT** — most call sites take a bare `actorUserId`, so threading an `AuthenticatedUser` would have meant changing seventeen service signatures plus every controller and test that calls them, to carry two fields the engine reads for itself through the same `getRoleRefs` the session is built from (which also means a role revoked earlier in the same request cannot be recorded as the hat). **THREE APPLICATION-ONLY PAIRS, NOT THE TWO THE PLAN RECORDED**: the third is `NeedsAssessment.reject`, found by reading the WRITE rather than the guard — rejecting writes no `approvedByUserId`, so that pair's CHECK can never fire on the reject path and the rule "the rejecter is not the capturer" lives in application code alone. It still goes through the engine with its returned id DISCARDED, because a one-person office that could neither approve nor reject an assessment she captured is the trap Part 4 exists to remove. **A SHARED TEST DOUBLE, because fifteen hand-written mocks would have broken fifteen tests silently** — every one of those service specs asserts that a self-approval is refused, and a local `mockResolvedValue(null)` in each would have made all fifteen pass on the mock rather than the code (§ 1.51(d) with the guard and the double swapped). `duty-segregation.double.ts` refuses exactly as a segregated office does, through the same `assertDifferentActors`. **AND `no-unused-vars` FOUND A REAL DEFECT**: two call sites — the settlement second approval and the commission override — resolved an act and never passed it to the write, which in COMBINED mode is a 500 from the CHECK. A lint rule caught the exact mistake a plant had already proved fatal on the refund site, on two sites nobody reviewed. **A plant on the FIFTEENTH pair, because fourteen had no combined-path proof**: the ordinary-path assertions pin the escape column as `null`, which catches a spurious act id and says nothing about the column being filled when one is declared. So a SECOND pair is now proven end to end — `NeedsAssessment.review`, chosen because that table carries TWO escape columns and the test asserts the review fills the reviewer one and leaves the approver one null. Dropping it turned the 201 into a **500**, with the engine's own `updateMany` named in the trace. **That plant was refused as STALE on its first attempt** — `prettier` had reformatted the line it named, and `plant.mjs` said so and exited non-zero rather than matching nothing, which is § 1.51(a) and (e) both working. | **Thirteen of the fifteen pairs have no combined-path e2e** — they have the ordinary-path `null` assertion, the shared double, and a database constraint that answers a missing escape column with a 500 rather than a silent self-approval. The 500 is what makes the gap survivable; it is not a reason to leave it. **Use `segregatedOfficeDutySegregation()` from `duty-segregation.double.ts` in any new service spec** — never a local `mockResolvedValue(null)`, which would make that file's own self-approval assertions pass on the mock. **A route that records a checker decision extends `CombinedDutyDeclarationDto`** rather than declaring its own `combinedDutyReason`: one definition of the field and its floor. **Steps 4 and 5 ship TOGETHER** (owner-approved): fifteen routes now accept a declaration that no web control sends, so an office able to declare COMBINED before the screens can carry a reason gets a 422 and nowhere to type — a door opened onto a wall. **`AccessRecertificationItem` stays unwired pending a decision**: its constraint fires on an INSERT (the reviewer is assigned when the cycle opens, and `reviewerUserId` is NOT NULL with no null guard), so in a one-person office the cycle cannot start and `decide()` is never reached — wiring it would change nothing for the case the condition exists for. The three options are put to the owner in plain language in `docs/decision-reviewing-your-own-access.md`. **Still to come on Part 4**: steps 4+5 as one unit, then step 6 — THE SELF-APPROVAL REPORT, still the shipping gate. |
| 2026-09-26 | **A rule I wrote into a document was asserted nowhere, and one README limitation had become false.** `docs/discard.md` § 3 states what decided all seven of the discard's reader fixes — a withdrawn record stays in its own register and leaves every derived view — and nothing tested either half. One test now withdraws a claim and reads two surfaces with the same token: its own list, where the row comes back carrying its discard block, and the customer's 360° timeline, where its id is absent while the policy's is present. **The absence half is anchored on a positive read from the SAME response**, and the anchor is not decoration — written once without the bearer header, the request 401'd, and "the assertion failed" and "the request was never authorised" look identical from outside. **A branch that would have let it pass vacuously was removed**: the 360° read had been tolerating a non-200 by asserting 403 instead, which would have satisfied an absence assertion with a permission-denied body. **PLANT**: removing the timeline's `discardedAt: null` put the withdrawn claim's id straight back in, and the test died naming the id it found. **And the doc-currency grep found a claim the discard had falsified**: README's #22 row said a stuck in-flight cancellation blocks re-raising permanently with "no void/withdraw endpoint" — true when written, false since the discard shipped one commit earlier, and it is the exact limitation the feature exists to remove. Corrected rather than deleted, because the old limitation is what explains why the feature exists. | **Grep the feature's NOUNS after shipping it, not only the files it touched** — "cancellation", "withdraw" and "void" found a false README claim that the diff of the discard commit never touched. **A rule stated in a doc is not a guard**: `docs/discard.md` § 3 now says which test asserts it and which plant proved that test can fail. api e2e discard 8/8 (+1). |
| 2026-09-26 | **One person can now perform both halves of a refund approval — through the real API, in an office that declared the mode, only by saying why, and with the roles they wore recorded.** Part 4 step 3a: the engine plus 1 of the 19 call sites. **`assertDifferentActors` stayed a pure function and is still the refusal**; what gained the mode is `DutySegregationService.resolve()`, returning the escape-column value the caller's write must carry — `null` for every ordinary two-person act, an act id for a declared combined one. **A service shared across modules, which this codebase otherwise never does** — the one established exception is `WorkflowTransitionService`, an ENGINE fifteen modules import from its own `@Global()` module because every status change must pass through one implementation; nineteen call sites in fourteen modules record a checker decision and "may one person do both halves, and what must be recorded" has to have one answer. **The ordinary path costs nothing**: two different people, or no checker yet, returns before any database read, and a unit test asserts the office is NOT read on that path — a cost assertion, since that is the path every approval in the product takes and the reason the mode is not read inside fifteen triggers. **THE HAT is why a new repository read exists**: `findRolesGrantingCode` returns WHICH of the actor's roles grant the checker code, a question the cached authorization read cannot answer because it flattens provenance away deliberately — and when two roles grant it, the act records `multipleGrantingRoles` rather than picking one and looking certain. **The act is written BEFORE the caller's write and is not rolled back if that write loses a race** — the escape column is an FK so the act must exist first; the alternatives are a mutable evidence table or threading a transaction client through nineteen repositories, and over-recording a control event beats under-recording one. **ALL FIVE of the plan's plants are now run, four as permanent tests** — plant 4 is a test of its own: a Placement officer without `refund.approve` declares a reason in a COMBINED office and is still 403, and the refusal must NOT mention segregation, because telling that person to find a second signature would be the wrong instruction. **Two more plants on this step's own code**: the call site made to drop the act id turned the 201 into a **500** — the CHECK constraint refusing the service's mistake, which is the backstop existing rather than being described — and the engine made to ignore the mode killed the segregated test at 422 (the reason gate, one step before the trigger; the trigger's own backstop is proven by the schema spec, not by that run). **And a global count in my own new spec was the cumulative-db trap**: `combinedDutyAct.count({ organizationId })` had to be 0, which the combined-mode spec legitimately makes false — a cross-spec coupling that would have broken on run ORDER. Scoped to its own row, and a sibling assertion that "no office has ever declared a mode" was deleted rather than kept, because an assertion a later step must delete is a liability, not a guard. | **The mode is still not settable** — no endpoint, screen or service writes `dutySegregationMode`, so nothing in production reaches any of this. **14 pairs and 18 call sites remain**, and they are a mechanical repeat of the worked example in `endorsement.service.ts#approveRefund`: resolve through the engine, pass the returned id into the repository write, and let the repository put it in THAT pair's own escape column (`docs/duty-segregation-mode.md` step 2 lists all fifteen column names). **A route that gains a combined-duty declaration takes it as an OPTIONAL body field** — `ApproveRefundDto` is the pattern — because an ordinary two-person approval sends nothing and must behave exactly as before; a DTO cannot see the office's mode, so the requirement is decided in the service. **A test that puts an office into COMBINED mode must reset it at BOTH ends** (`beforeAll` and `afterAll`, plus a `finally` around the work): db-test is shared, and an office left COMBINED makes a self-approval legitimate for every later spec in the run, surfacing as an unrelated failure somewhere else. `duty-segregation-combined.e2e-spec.ts` also asserts, as its last test, that the office is SEGREGATED again. **Still to come on Part 4**: the other 18 call sites, step 4 (the mode screen and its audited change), step 5 (the act visible on the record), step 6 (THE SELF-APPROVAL REPORT — the shipping gate, without which the mode may not be released). |
| 2026-09-26 | **The database can now express "this office declared that one person does both halves" — and refuses every way of abusing it. Nothing can set the mode yet, deliberately.** Part 4 step 2 of 7, migration `20261028100000`; the plan and its rejected alternatives are `docs/duty-segregation-mode.md`. `Organization.dutySegregationMode` is a two-value enum defaulting to SEGREGATED, so **every existing office reads SEGREGATED and nothing changed for anybody** — asserted by the migration itself. The 15 maker/checker CHECK constraints stay CHECK constraints, on the same tables, with the same names and the same column pairs; each gains ONE disjunct — its own escape column being non-null — and that column is an FK to a new `CombinedDutyAct` carrying the actor, the pair (named by the CONSTRAINT, so the two cannot drift), a mandatory reason, the roles worn, THE HAT (the subset that actually grant the checker permission) and a `multipleGrantingRoles` flag for when the honest answer is "we cannot tell". **ONE trigger, on `CombinedDutyAct` alone**, refuses an act whose office is not COMBINED. So the database refuses a SILENT self-approval (the CHECK, as before) **and** a DECLARED one in a segregated office (the trigger) — deleting every line of application code cannot produce either, which is the property the design was chosen for over fifteen triggers and over denormalising the mode onto fourteen tables (that one dies on mode TIGHTENING: the cascade re-validates and fails on every historical row, so an office could enter the mode and never leave). **A column per CONSTRAINT, not per table**: `NeedsAssessment` carries two pairs, and one shared column would let a declared combined REVIEW excuse a self-APPROVAL. **The 15 predicates were generated from the database's own `pg_get_constraintdef` output**, because they do not share a shape — `DataSharingApproval` guards only the checker side for NULL, `AccessRecertificationItem` guards neither — and re-deriving them from a template is how one comes back looser than it went in. The migration's `DO` block asserts, per constraint, that it still names BOTH original columns AND its escape column: a predicate loosened to `TRUE OR (escape IS NOT NULL)` would satisfy a check that only looked for the escape column and would refuse nothing. **My own comment claimed that assertion before it existed** — caught by re-reading it, and the assertion was written rather than the comment softened. **Four of the plan's five plants are run, and three are now permanent tests** (`duty-segregation-schema.e2e-spec.ts`, 7 tests): plant 1's property proven STRONGER than the plant (the test writes a self-approving Refund with raw Prisma — no service in the path at all — and the database refuses it, so removing the application check cannot reach a weaker state); plant 2 run by dropping `Refund_maker_checker_distinct` on db-test, which killed TWO tests independently — the undeclared-self-approval one and the registry-vs-`pg_constraint` one at 14 against 15 — and the leaked row it let through had to be deleted before the constraint would go back, which is the constraint working; plants 3 and 5 permanent. Plus the three claims in one rolled-back transaction: refused undeclared, act insertable once COMBINED, the same write accepted because it declares itself, read back inside the transaction. | **COMBINED mode is NOT usable and must not be described as available**: no endpoint, no screen and no service sets `dutySegregationMode`, so the only way into it is a manual `UPDATE`. The control exists before anything can rely on it, which is the intended order. **The mode may not SHIP before the self-approval report exists and works** — that gate is stated at the top of `docs/duty-segregation-mode.md` and is owed to a commitment made to the owner, not a preference. **Two migrations to `migrate resolve` on both databases** if you are catching up (`20261028100000`, plus the three discard ones from the commit before). **A test that changes an office's mode must roll it back** — db-test is cumulative and an office left COMBINED makes a declared self-approval legitimate for every later spec in the run; the one test that needs the mode does it inside `$transaction` and throws, and creating a second Organization is the WORSE alternative (a second live office makes `signup` refuse, which has already cost one run 17 unrelated failures). **Prisma's default `onDelete` for an optional relation is `SetNull`, and the database says `RESTRICT`** — all 15 relations declare `onDelete: Restrict` explicitly, or `db:divergence` flags exactly the class this repo was bitten by before. **Still to come on Part 4**: step 3 (the application layer — `assertDifferentActors` gains the mode and the act, 19 call sites), step 4 (the mode screen), step 5 (the act visible on the record), step 6 (THE REPORT — the shipping gate), step 7 (the remaining plant: withholding the checker permission in a COMBINED office must still be 403). |
| 2026-09-25 | **A record raised in error can be withdrawn without being committed first — and the state was added to four models whose READERS were then measured one by one.** Class B piece 1, `docs/discard.md`. **The trap, measured before building**: there was no route out of any pre-commitment state in four modules, and the sharpest case is the endorsement — the only exit from a wrongly raised one was to walk it to `APPLIED`, changing a real policy, its premium and its commission, then correct it with a second endorsement. **To undo the mistake you had to commit it first.** Now `Policy`, `Claim`, `Endorsement` and `Recommendation` carry `discardedAt`/`By`/`Reason` with an all-or-nothing CHECK per table, a mandatory 10-character reason, and four new permission codes (206 → 210) each granted to exactly the roles that can RAISE that record. **The status is deliberately NOT rewritten** — a discarded Policy is `PLACEMENT_CONFIRMED` forever — because a status is a position in a workflow and "raised in error" is a statement about the whole record. **The cost of that choice is the interesting half, and it is § 1.40's shape with the halves swapped**: every existing query reasoning about a pre-commitment status began answering with withdrawn records the moment the columns existed. All **31 reads** over the four models were enumerated; **seven were wrong** — the insurer-deactivation open-obligation count (a figure written into an AUDIT ROW), the open-claims ageing report (`status != CLOSED` holds forever, so a withdrawn claim ages without limit), both halves of the insurer responsiveness ratio, both halves of the 360° timeline, and the one that matters most. **THE TRAP REAPPEARED ONE LEVEL DOWN, IN THE DATABASE**: `Endorsement_one_live_cancellation_per_policy` is a partial UNIQUE predicated on `status <> 'CLIENT_NOTIFIED'`, and a discarded endorsement can never reach `CLIENT_NOTIFIED` — so withdrawing a wrongly raised cancellation would have blocked every future cancellation of that policy **permanently**, with applying the unwanted cancellation as the only exit. Found by the measurement, not by a failing test: nothing would have failed until somebody discarded a cancellation and then needed a second one. The predicate now excludes discarded rows and **asserts itself in its own migration's `DO` block**, because `db:divergence` does not see partial indexes; the assertion was run against the un-widened index first and printed the real predicate in its refusal. **"Cannot advance" lives in TWO places, deliberately**: the engine, for the three entities whose every forward move passes through it (one guard, not three — a terminal-state rule copied three times is three chances to forget one), and `Recommendation`'s own service, because it has no `status` column and drives the *Opportunity's* transitions instead; `discard.config.ts` records `advancesViaWorkflowEngine: false` against it so nobody assumes the engine covers all four. Both refusals NAME the discard — "cannot transition from X to Y" would be true and would send the reader hunting a status problem that is not there. **`AuditAction.DISCARD` is new**: `DELETE` would be a lie (the row is still there) and `UPDATE` would make withdrawing a wrongly raised endorsement indistinguishable, in a filtered audit list, from correcting its premium. **One screen control for all four** (`DiscardControl`), whose reason field IS the confirmation — no "are you sure" step to click through without reading — and whose Arabic never says «إلغاء», which in this product means a cancellation endorsement on a live policy. **Six plants, each preceded by naming which test should die and why that surface can observe it**: the reason made optional (died — and the CHECK constraint caught it as a 500, so the database backstop is real), the engine guard removed (a discarded policy was ISSUED), the recommendation's own guard removed (its separate guard proven separately, which the engine plant could not do), the commitment rule bypassed, the endorsement discard made to touch the policy (premium read back as 135000 against 90000), and the web control made to ignore its permission. **And a SIXTH way a proof can be absent, § 1.51(e), found by that fifth plant**: `plant.mjs` verified "the anchor text is gone", which is right for a substitution and wrong for an INSERTION — so it wrote the file, re-read it, and exited NON-ZERO saying the plant had not applied, with the plant live in the tree. A loud failure that misdescribes what happened is not safer than a quiet success; it is the same defect with the halves swapped. The invariant is now "the file on disk is exactly what this plant intended", `--self-test` has a case for it, and restoring the old condition makes exactly that case fail. | **Read `docs/discard.md` before adding a query over `Policy`, `Claim`, `Endorsement` or `Recommendation`** — and answer its § 3 question for your query: *can a row in the discarded state reach this, and is the answer it gives still true?* The state is pinned per entity, so this is decidable rather than a guess. **A discarded record stays in its own register and leaves every derived view** — dashboards, ageing reports, performance ratios, the 360° timeline, any "is one of these in flight" check. It stays in its own list for the same reason a deactivated insurer does: hiding it reads as deletion. **Check every partial index and unique constraint on a model you add a terminal state to** — a permanently-stuck row occupying a partial-unique slot is a permanent block, and `db:divergence` cannot see it, so the assertion belongs in the migration's own `DO` block. **A plant may INSERT** now, not only substitute, which is how you prove a test observes a side effect rather than a changed branch; `node scripts/plant.mjs --self-test` covers it. **A unit fixture that omits a column the model has is a fixture that lies**: `assertNotDiscarded` refuses to advance anything whose discard state is not exactly `null` — a row loaded without it is a row whose state nobody knows — so seven recommendation unit tests failed until the fixture stopped omitting the three columns. That is the fixture being wrong, not the guard. **Two new migrations need `migrate resolve`d on both databases** (`20261027110000` adds an enum value, which cannot be used in the transaction that adds it, hence its own migration). **Still owed, in the owner's order**: Part 4's implementation — gated on the self-approval report existing and working — then Phase 2's two lies, Phase 3's update-where-a-real-scenario-exists (customer first), Phase 4's deactivation, the recorded debts (§ 1.44, § 1.47, § 1.40, § 1.52, § 1.53, the three weak assertions on main), then B.7's formal consistency pass. |
| 2026-09-25 | **One control for finding a named thing, and the audit trail can be asked about a person.** `EntitySearch` replaces `CustomerPicker` at all ten call sites and the picker is DELETED in the same commit. A SOURCE decides how to search, what to call the thing, and what to show beside a name; everything else lives once — Enter searches without submitting the surrounding form, a native `<select>` carries the ARIA/focus/keyboard behaviour in both reading directions, and "nobody by that name" and "the search failed" stay separate claims. **The audit screen got both halves it was missing**: the column headed "User" rendered a raw uuid at the person being asked to review who did what, and the `userId` filter the API has accepted all along had no control — so "what did this person do" was unaskable. Names are resolved ONE query per page on the distinct actors, and NOT stored: `actorRoleNames` is stored because a revoked role is unrecoverable, a name is a lookup. **`GET /audit-trail/actors` is its own endpoint, measured not preferred**: `audit-log.read` is held by COMPLIANCE, EXTERNAL_AUDITOR and the two administrator roles, `user.manage` by only the administrators — so sourcing the picker from `/admin/users` would have 403'd for the audit trail's primary readers. It returns only people who actually appear as an actor (a read-only auditor cannot enumerate staff through a filter control) and writes NO audit row, because a lookup that writes to the log pollutes what is being searched. **TWO OF MY OWN MISTAKES, BOTH FOUND BY MEASURING.** An English reader saw `Ahmad Al-Test — نشط · REG-1`: sources translated at FETCH time, `t` was captured at mount before the language resolved from `/auth/me`, and the line never re-localised — found by PRINTING the option text in a throwaway spec, and fixed by making option data language-free (`DetailPart` is literal text or a key) and translating at render, which deleted the `eslint-disable exhaustive-deps` rather than justifying it. And my Enter test COULD NOT FAIL: it ran on `/dsr`, whose required fields make the browser block submission either way, so planting the missing `preventDefault` left all seven tests green. Rewritten against the audit browse form, which has no required fields and an observable submit. Also corrected a claim I had written three times — `actorName` cannot be null in practice, because `userId` is NOT NULL and `AuditLogEntry_userId_fkey` is ON DELETE RESTRICT (measured against the database, not read off the schema, which has been wrong about an `onDelete` before); the test now asserts that refusal instead of trying to create the state. | **Need a picker? Add a source to `ENTITY_SOURCES`** — never a second picker. A source returns `{id, name, detail?}` where `detail` is an array of literal strings or `{key}` objects, and the COMPONENT translates: returning an already-translated string pins the language to first render, which on this Arabic-first platform shows Arabic to an English reader. **Rebuild before every Playwright run after touching web source** — the `webServer` serves the last build, and this cost three confusing failures and one wrong diagnosis in a single session (third occurrence recorded in this repo). **A mock's SHAPE must match the endpoint**: `listDsrs` returns an ARRAY and a paginated object crashed the page, unmounting the form under test. **A test on a form with required fields cannot prove anything about submission** — HTML5 validation blocks it regardless, so pick a host form that can actually submit. **Still unreachable from the audit screen**: `action`, `from` and `to` (IMPROVEMENTS § 1.52), recorded rather than built. |
| 2026-09-25 | **A proof is only a proof if its absence is loud — three mechanisms were silently absent at once, and CI had been red for five commits.** **(a) A plant that never applied.** `process.argv[2]` is empty when node runs with `-e` (no script path occupies argv[1]), so a plant selector was `undefined`, no branch matched, no file was touched, and the suite went green. **A plant that silently does nothing is indistinguishable from a guard that works** — both print a green suite, and that output is then used to justify "this guard is proven". Audited across the week from the session record: **121 plant runs, 67 showing a test die, 52 setup/revert steps, exactly 3 that reported everything passing** (2026-09-21T19:48, 2026-09-22T06:38, 2026-09-24T20:43) — each noticed and re-run within a minute. **(b) A plant DEFINED and never invoked**, which the "all passed" signal cannot catch because nothing ran: `plant-ua.mjs` defined four names and the loop listed three, while the commit message claimed four. The missing one — no KMS round trip inside the person-and-account transaction — was run and the guard SURVIVES, but it was unproven for the whole time it was called proven. **(c) A guard file CI ran, failed on, and nobody read.** `permission-only-gates.e2e-spec.ts` caught four wrong route gates on `477965d` immediately, naming them (`expected [ 'GET /admin/departments', …(3) ] to deeply equal []`), and again on `a7a977d`. Then `395a0a3` created `Employee_branchId_idx` in a migration without declaring it in `schema.prisma`; `db:divergence` went red EARLIER in the same job, short-circuited it, and **the api e2e suite stopped running at all for three commits** — the first failure hidden behind a newer red while every local gate stayed green. The gate was right: `DROP INDEX "Employee_branchId_idx"` is what the next `migrate dev` would have generated. **And running the database gates found two more of mine**: `org-units.e2e-spec.ts` leaked two roles per run (6 runs → 12 roles, 60 grants) so the fixture generator read TEST roles as the seeded grid; and the generator read EVERY role in the default office while its write variant reads DEV and `verify.sh` checks DB-TEST — dev held two roles the owner made by hand, so the gate could never pass, and **the committed web-e2e fixture already contained a role literally named `SHOUQ`**. | **Plants go through `node scripts/plant.mjs <plants.json> <name>` and nothing else.** It refuses a missing name, an unknown name, an absent `from` (the code moved — the plant is stale), a `from` matching more than once (ambiguous site), and a no-op replacement; then it RE-READS the file and will not print `PLANTED` unless the bytes changed. `--revert` restores from the backup taken at apply time. `node scripts/plant.mjs --self-test` is a plant ON the plant mechanism (11 cases, including the original bug: no name → exit non-zero) and is a `verify.sh` gate. **`bash scripts/check-ci.sh` is now the LAST gate in `verify.sh`** — it reads the CI conclusion for HEAD and fails on a red, unfinished, or absent-though-pushed run, printing the `gh run view --log-failed` command for each; no `gh` is also a failure, because "I could not check" is the state that caused this. It passes quietly when HEAD is unpushed. **Run the cheap database gates locally even when skipping the suites**: `db:validate`, `db:test:checksums`, `db:test:divergence`, `db:test:privileges`, `db:test:fixture:permissions:check` take seconds and are what caught all of this. **A new migration index/constraint must be declared in `schema.prisma` too**, or `db:divergence` fails in the direction that bites. **An e2e that creates a Role must delete it by ID in `afterAll`** — a comment in a sibling file is not a teardown. **The fixture generator reads `isSystem` roles only** now, so an office using the Role screen cannot break a test fixture; `db:test:fixture:permissions` (write, from db-test) exists alongside the dev variant. |
| 2026-09-25 | **A person and their login are now ONE act, and the read that answers before the page exists is now unwritable.** **Plan A**: registering someone who needed an account was two screens in a fixed order — record the employee, then provision the account and name the HR record in a DROPDOWN OF EXISTING EMPLOYEES, which by definition could never contain the person being registered, while demanding a department and a branch no screen could create. Three reasons for one sentence: "the button is broken". The login is now a checkbox on the person form at `/employees`, and one Save sends ONE request to `POST /admin/users` with an `employee` block that writes both rows in **one transaction**. Never two calls from the browser: a second call that fails leaves a person who half exists with nothing on screen to say which half — proven by driving the rollback through the collision an administrator actually hits (an email already in use) and then reading the database to find the person the transaction had already written **gone**. **Why that route and not `POST /employees`**: the account half carries the rules that must not fork (password policy, role resolution, access window, segregation signal, P2002→409) and `UserAdminService` already injects every repository the pair needs, whereas composing it from the employee side would have meant a second copy of those rules in a module that cannot inject `PasswordService` at all — this codebase shares repositories across modules, **never services**, with no exception anywhere. **The second permission cannot be declared on the route**: `PermissionsGuard` ORs its codes (`required.some`), so `@RequirePermissions('user.manage','employee.create')` would let either one through alone; checked in the service, and a caller that sends a person without the actor's roles gets a loud programming error rather than a silent bypass. Measured against the grid first, because a form the owner cannot use is the defect that keeps recurring: OFFICE_ADMINISTRATOR holds both, **MANAGER holds `employee.create` and NOT `user.manage`** — so "a Manager records people and cannot hand out logins" is a real state the screen renders with a sentence, not an edge case. **Department and branch appear ONCE** and feed both rows, which makes the department-disagreement 409 both link paths raise *unreachable* rather than caught. **`User.fullName` stays stored and NOT NULL** (an audit row holds a userId and nothing else) but is never typed twice — composed from the four parts, and sending `fullName` beside a person is a 422; its type became `fullName?`, which is what `@ValidateIf` had always meant, with the service re-deriving it rather than trusting the decorator. The four ENGLISH name parts are optional as a SET and compose to **NULL, not ''** — transliterating an Arabic name is a judgement about a real person and nothing guesses one. **The picker covered TWO cases and only one was replaced**, so deleting it outright would have left `employeeId` reachable by nothing — the unreachable-API-surface problem § 1.44 had to measure across four other endpoints; the second case (someone recorded weeks ago) moved to that person's own page, and `GET /employees/:id` now returns `account: {id,email}｜null` so the screen never offers a second login and is then refused by a unique constraint it could have known about. **AND THE READ THAT LIED FOUR TIMES.** `count()`, `evaluateAll()` and `allInnerTexts()` do not auto-wait, and `toHaveCount(0)` passes on its FIRST poll: before hydration they answer 0/[] and the assertion passes. Four wrong readings in one session, in four files, every one written by someone who knew about the trap and had left a comment about it — a comment is not a control. `e2e/support/anchored.ts` now does the anchoring once (`anchoredCount`, `anchoredAttributes`, `anchoredTexts`, `expectNone` — whose anchor is REQUIRED, because an empty set cannot vouch for itself), the three raw reads are an **eslint error in every e2e spec**, and `test/e2e-anchored-reads.test.ts` enforces in the fast gate what the ban cannot express: every absence assertion has something positive asserted before it. Measured before building — 490 tests, 90 asserting absence, exactly **4** unanchored — which is why the ban covers the READS and not `toHaveCount(0)`. **It immediately found a test that had been lying for 107 commits**: `navbar.spec.ts` asserted "the notifications slot announces no control", true at `c5fafb2` and made false 13 commits later by `9bd396d` building the bell, with nothing failing because the absence assertion ran first. | **Register a person at `/employees`, not at `/settings/users`** — that screen is now for an account with no person behind it (the external auditor), role changes and deactivation. `docs/first-run.md` § 8 is corrected in both languages, including the two rows that would have sent the owner to the wrong screen. **Playwright: never call `count()`, `evaluateAll()` or `allInnerTexts()` in a spec** — eslint refuses, and `e2e/support/anchored.ts` is the exemption. When the expected count is zero use `expectNone(target, anchor)` with an anchor OUTSIDE the set. **A page-wide `getByRole('alert')`/`('status')` is a strict-mode violation**: Next renders its own route announcer with `role="alert"`, so scope to `main` — second occurrence this session. **`permission-only-gates.e2e-spec.ts` is the guard for "a route's gating permission moved" and the four-action pilot left it red** with four wrong gates and nothing running it; re-guard a route and run that file. Its first test now also collects 5xx, since it was collecting only 403 and a route that got past the gate and then died counted as a pass. **A two-character needle in base64 is a coin toss** — asserting `not.toContain('99')` on a ciphertext went red on `v1:J992Z4...`; assert the whole secret is absent AND the envelope prefix is present. **A plant that silently fails to apply reads exactly like a guard that works**: `process.argv[2]` is empty under `node -e`, and three plants reported PASSING before that was noticed. **Still owed, in the owner's order**: Plan B (one name-search component), Part 3 Phase 1 (the cheap four-action codes + splitting the four umbrellas), Part 4 (duty segregation as a declared per-office MODE — its own plan first), Part 5 (the two honesty fixes), then Class B Piece 1, the discard, whose approved shape is a partial unique index on `opportunityId WHERE discardedAt IS NULL` plus the same treatment for `recommendedQuotationId`. |
| 2026-09-23 | **The system was handed to its owner, and the front door was measured rather than described — `docs/first-run.md`.** The walkthrough was **performed before it was written**: stack started, browser opened, administrator signed in, authenticator paired, populated screens reached. Writing from the code would have produced a confident and wrong document, because **the first sign-in had no signpost at all**. `MfaRequiredGuard` refuses every route until TOTP is paired, `/settings/security` is the only way through, and nothing said so — each screen reported the refusal in its own words and the insurer screens told an administrator who HELD `insurer.read` that she did not. Fixed in the one place that reaches all 68 screens (`app/(app)/layout.tsx`, a banner keyed on `mfaEnabled` and suppressed on Security itself) plus a shared `isMfaEnrolmentError` reading the `code` the API always sent and the web never read. **The recorded MFA defect was real and is diagnosed**: ten `demo.*` accounts held a credential whose secret the seed generated for its own API calls and never showed anyone, so every code she typed was genuinely wrong server-side while being correct in her authenticator — *that* is "correct code rejected". `npm run demo:release -w api` repairs it in 2.5 s. **The demo password left the repository**: `DEMO_PASSWORD` is now required with no default, `ensureActor` re-hashes on reuse (it only hashed on create, so a rotated password printed credentials that did not work), and — found while wiring it — **the run had been writing the password in plaintext into `seed-demo-report.json`**, which is gitignored but still a file; the field is gone and the stale report deleted. **Four measurements changed the work.** (1) The seeded near-duplicate pair `al-yarmouk insurance` / `Yarmouk Insurance` does **NOT** collide: `canonical_name_key` is token-SORTED and strips the Arabic article `ال` but keeps a Latin `al` as its own token, so the directory would have shown TWO entries under a doc promising one. Replaced with a measured-colliding pair, then **proven end to end** — both spellings planted into the two offices inside a rolled-back transaction, the `InsurerDirectory` view returning **1** row for 2 `Insurer` rows. (2) The walkthrough's "try this and watch it refuse" step is now asserted BY THE SEED (`proveDuplicateSpellingIsRefused`), which fails the run if the 409 does not come naming the attempted spelling — an instruction nobody re-checks is exactly the kind that rots into "it just let me save it". (3) The administrator holds **29 codes and none of them read a customer**, so "enter existing customers" is not its job on this build; the doc routes that to `demo.sales` with a measured per-account table. (4) Office B's `OFFICE_ADMINISTRATOR` is **4 grants behind** office A's (22 vs 26, missing `insurer.relationship.manage`), closed by re-seeding but warned about by nothing. | **Read `docs/first-run.md` before explaining this system to anyone.** It is AR-first, answers what-to-type through what-to-do-when-it-breaks, and states the gaps plainly (no second office from inside the app; `customer.bulk-import` and both `email.integration.*` codes have no screen; no WebAuthn, which is why one banner never clears). **`DEMO_PASSWORD` is now mandatory** — `npm run seed:demo -w api` refuses without it, in both windows if you use two. **A local Playwright run reuses whatever is already on :3000** (`reuseExistingServer: !process.env.CI`), so a leftover `npm run dev` makes the suite test a DEV build — its Dev Tools button adds a second "Next" that turned `getByRole('button', {name: 'Next'})` into a strict-mode violation in `customers.spec.ts`, a failure CI can never reproduce. Kill the dev server before running e2e. **A banner in the shell is a change to 80 spec files' worth of pages**: every one of them mocks `mfaEnabled: false`, which is a state the real API would refuse every screen for — the fiction was harmless until the shell started reading the flag. Measured before adopting: no text collisions, 32/32 green on the nav+list batch. **Verification**: typecheck 6/6, lint 3/3 (one pre-existing warning), api unit 25/25 on the touched module, `auth-mfa-session.e2e-spec.ts` **26/26** — which is what proves `body.code` exists where the new web helper reads it, the assertion there having been a `JSON.stringify(...).toContain(...)` that would have stayed green if the discriminator moved into `message`. New `mfa-enrolment-banner.spec.ts` 5/5 and `insurers.spec.ts` 9/9 (+2), with **three plants**: each half of the banner's condition dropped in turn (one test dies each time) and the helper reverted to ignoring the code (the MFA message test dies, the permission one still passes). |
| 2026-09-22/23 | **Insurer management, and the verification machinery it exposed as missing — 30 commits.** **The feature**: an office can register an insurer no global catalogue lists (both paths through one endpoint, 409 on collision, 404 not 403 cross-office), describe it with a managed 32-line bilingual vocabulary plus office-side additions, deactivate it as a recorded act carrying TWO live impact counts (`IN_FORCE_POLICY_STATUSES` and `OPEN_OBLIGATION_POLICY_STATUSES`, separately named, one definition serving screen and audit row), and find a company another office registered through a read-only cross-office **directory whose boundary is structural** — a `SECURITY DEFINER` view with a key allow-list test, so relationship columns are unreachable rather than un-selected. **ONE canonical name key now lives in the database (F13)**: an `IMMUTABLE` SQL function backs a `STORED GENERATED` column, the directory's `GROUP BY` and the per-office unique index. Measured before the fix: `"Al-Yarmouk Insurance"` and `"al yarmouk   insurance"` registered into one office as TWO rows and the directory showed ONE entry. The TypeScript mirror survives only for mid-registration suggestions, pinned by a 39-name parity table. **That key is CTYPE-free by construction, and then Arabic-correct.** `lower()` and `[^[:alnum:][:space:]]` are destroyed by a C ctype — measured, every Arabic letter becomes punctuation and every Arabic name keys to the EMPTY STRING — so both became enumerated folds. Then the allowance `؀-ۿ` turned out to be the whole Arabic BLOCK, which is not letters: **Arabic punctuation was KEPT while ASCII punctuation was stripped**, so `"شركة، التأمين"` and `"شركة التأمين"` were two different companies, and `"شركة ١٢٣"` vs `"شركة 123"` likewise. Now enumerated letter subranges, Arabic-Indic digits folded both ranges, presentation forms normalised via NFKC (innermost, so the tatweel it EMITS is still stripped), and the mark strip widened. Assertions live in the migration, where the property matters; they caught the author twice. **Four free-text `insuranceLine` columns CONVERTED — backfilled, and then the WRITERS** (§ 1.40 measured the gap between those two: the migration changed no writer, so for three days every new row was unmapped — 6 and 4 on two models it had mapped 100%) on the models that must MATCH each other — 962/962, 4,449/4,449, 4,994+632-parked, 287/287 — mapping derived from a vocabulary survey, `motor` PARKED because it is ambiguous between compulsory and comprehensive cover and guessing reprices business, the original string retained, nothing funnelled into "Unclassified". **AND THE GATES THE WORK EXPOSED.** `verify.sh` had a gate named "drift check" running `migrate status`, which compares neither checksums nor `schema.prisma` — measured: it printed "up to date!" over a real drift. Three real gates now exist (`db:checksums`, `db:divergence`, `db:privileges`), and between them they found **five drifted migrations** (only one known), **six schema divergences** all of which were the database being STRICTER than the declaration including an `ON DELETE RESTRICT` the schema called `SetNull`, and — the serious one — that `GRANT ibms TO ibms_app` leaves `rolsuper` FALSE while handing the runtime role the owner's privileges, and **Postgres exempts a table's owner from its own RLS policies**: planted, an unfiltered cross-office read returned **13,774 rows where 1 was expected** with every existing assertion green. **The audit log now stores the role the actor HELD** — the one gap that cannot be backfilled, because a role held in the past and never written down is unrecoverable. **Five of the six free-text `insuranceLine` columns carry a line FK, are backfilled, AND are written** — identity flows along the chain (programme line resolves a catalogue code; RFQ and Policy COPY it; the rate table REFUSES an unresolvable line rather than writing NULL) and `commission.repository.ts` now matches a Policy to its governing rate on the FK, not on `mode: 'insensitive'` free text — the four MUST models taking BOTH line FKs (office-scoped) against `InsurerFormTemplate`'s global-catalogue-only one, each with a composite `(officeLineId, organizationId)` tenant FK proven by a rolled-back cross-office plant. **And the commission VARIANT axis landed with the defect § 1.25 predicted**: `UNIQUE (insurerId, insuranceLineId, variantKey)` does NOT constrain two rows whose variant is NULL — the COMMON case — because Postgres treats NULLs as distinct, on the table deciding what the broker is paid. `NULLS NOT DISTINCT` fixes it, proven by three plants including one that initially proved the WRONG constraint. The variant stopped being a prediction when the conversion found **six insurers each carrying two simultaneously-open agreements**, every one the pair `Property All Risks` + `Property All Risks (Fire)` — two strings, one line id, kept apart today only by an index keyed on the string. **The directory can now be asked the question it exists for**: `GET /insurer-directory?lineCode=MOTOR_COMPREHENSIVE`. It shipped searchable by company NAME only, which answers "is this company on the platform" and never "who writes this cover" — the use case was missing, not merely unfiltered. An unknown code is a **422 naming it**, never an empty page, because `[]` is indistinguishable from "nobody writes this" and looks like an answer. The first draft also matched office-added lines by canonical NAME; measuring killed it — the colliding set is empty on both databases (0 either side), both write paths already refuse the collision, and the fallback would have put name similarity back on a matching path and returned companies whose recorded line was not the one asked for. An e2e asserts that empty set instead. **And Q9 landed — an office can map its OWN copy of an insurer's form — by NOT doing what `IMPROVEMENTS.md` § 1.26 predicted.** That entry said to make `InsurerFormTemplate` office-scoped; re-deriving it (which the entry demanded) showed that would have withdrawn Part I §5's sharing promise SILENTLY, because `applyTenantScope` filters every model carrying `organizationId` and the scoped set is derived from the DMMF — a row every office reads and a table the extension scopes are mutually exclusive. So Q9 is a SECOND model, `OfficeInsurerFormTemplate`, hanging off the office-scoped `Insurer` — the same split that already existed one level up. `insurerMasterId` stays NOT NULL on the global table and `assertGlobalLine` stays with it; the new table needs no `insurerMasterId` at all, which is how the "a local company's form is unrepresentable" gap dissolves instead of being patched. A `GENERATED` COALESCE key was written for its uniqueness and REMOVED after a temp table proved the two-nullable-uniques shape already refuses the duplicate. `GET /insurers/:id/form-templates/resolved` answers "which form do I submit against" naming its `source` — OFFICE beats SHARED, and an override is never an edit. | **Run `bash scripts/verify.sh` — it now has six database gates, three of them new.** `db:checksums` (every applied migration's hash vs its file, whole set — `migrate resolve` stamps a hash and any later edit drifts it), `db:divergence` (the diff is EXACTLY 12 named statements, failing in BOTH directions), `db:privileges` (five role attributes AND membership in no role — the membership half is the one that matters). **`db:divergence` has a measured BLIND SPOT: it does not see PARTIAL indexes or CHECK constraints.** Two live UNIQUE partial indexes on `CommissionAgreement` have never appeared in its output, so a future migration could drop either and the gate stays green — where a partial index carries a real invariant, assert it in its own migration's `DO` block (reading `pg_index`) or in a test. **`migrate dev` is still not the way to add a migration**, and the reason CHANGED: not checksum drift any more but that `schema.prisma` deliberately holds less than the database, so `migrate dev` would generate a migration dropping four GIN indexes and un-generating five columns. Hand-write, apply, `migrate resolve`, then run `db:checksums`. **Open the draft PR on commit one** — `ci.yml` triggers on `push: branches: [main]` and `pull_request` only, so a feature-branch push runs NOTHING, and 24 commits once sat on one laptop with no copy anywhere. **Targeted locally, FULL in CI** (14.1 min there vs 75 min on a quiet laptop) — and keep CI for being the only environment that starts from NOTHING, not for being fast. **An exclusion assertion can pass because the row was never on the page** — planting a neutered filter showed `not.toContain(x)` going GREEN while the page filled with 200 unrelated rows and the fixtures fell off it. Assert the POSITIVE form ("every returned entry carries the line") plus a non-empty page; a filter test that only names what it excludes cannot tell "correctly excluded" from "never returned". **A prediction about a future commit usually gets the OBJECT wrong, not the mechanism** — § 1.26 named the right inversion and the wrong table. An entry that says "re-derive" survives being wrong; one that says "do these three things" gets followed. **`ensureRole` UPSERTS**, so a mistyped role name in an e2e is CREATED with no grants and every request 403s — cost a full run, and left a permissionless role in the cumulative test database. **api e2e specs share one database and cannot run concurrently**: a second Organization live in another run makes `signup` refuse, which surfaces as unrelated 500s. **db-test has crossed a HARD limit and a `testTimeout` bump can no longer reach it**: at 45,939 ACTIVE users (5.9M audit rows), `rbac`'s access-recertification `startCycle` now 500s on Prisma's 120s INTERACTIVE-TRANSACTION cap — inside the application, so the request dies at 120s however long the test waits. Not a product defect (no office has 45,939 users), and the same commit proves it: CI ran `rbac` **10/10 in 14.7 seconds** while this laptop failed 4/10 with single tests at 143–261 seconds, because CI builds its database from NOTHING. **Resetting the local e2e database is now STEP ZERO of starting a branch**, beside the draft PR (`verification-contract.md`) — minutes on a schedule instead of a day mid-feature, twice discovered as an emergency. `prisma migrate reset` refuses to run unattended for an AI agent and needs the user's explicit consent for that run; do NOT route around it with a manual `DROP DATABASE`. **Take any measurement that depends on the current contents FIRST** — a reset makes some gates answer ZERO for the wrong reason, which is how § 1.40 was found. **And the reset immediately found a real defect, which is the argument for doing it on a schedule**: `RoleRepository.findActiveUserIdsWithPermission` had NO `orderBy` while `pickReviewer` takes `[0]` of it and README called that "the first eligible member" — so which reviewer a subject got was decided by the query PLAN, on a segregation-of-duties control. It passed at 46,153 users and failed at 23. Now `orderBy: [{ grantedAt: 'asc' }, { userId: 'asc' }]`, a total order (§ 1.42). **The class: a consumer taking `[0]` imposes an ordering requirement nothing type-checks** — put the order, and the reason, in the query. **The insurer feature has SCREENS now** — `/insurers` list + register + detail, the first insurer UI in the app (74 nav links and not one of them was for insurers). Three properties are asserted rather than assumed: the list shows deactivated insurers BY DEFAULT (they keep their policies and are the row an administrator most needs to find — hiding them reads as deletion); the detail page keeps COMPANY facts and this office's RELATIONSHIP terms in SEPARATE headed sections, proven by asserting each fact is under its own heading and absent from the other; and the deactivation step shows all five impact counts with the two policy figures APART, proven by planting a sum and reading `Received: "5"`. Nav sits beside `/vendors` under Operations — both are counterparty registers, while "New business" is a pipeline of stages. **A nav entry must sit in a group every holder of its gating permission can see** — I put `/insurers` in Operations beside `/vendors` (both counterparty registers) and CI refuted it: `sidebar-executive.spec.ts` asserts an Executive sees NO Operations group, and an Executive holds `insurer.read`. The rule is not what KIND of thing a screen is but WHO holds its permission; both insurer entries moved to New business, where every holder already works. **Read the FLAKY count in a green CI run, not only the conclusion** (§ 1.47): the count went 2 → 2 → **4** across three consecutive runs, two of which concluded SUCCESS. All four are the CLAIMS block of `rfq.spec.ts`, and a cascade was ruled out (each builds its own context) — they share a SHAPE: ~16 sequential interactions before one assertion, against a 30s budget. A growing flaky count is a different problem from a stable one. **And `/insurer-directory`** — its own nav entry and its own permission, never a tab on `/insurers`, because `insurer.directory.read` is separate from `insurer.read` so an office can be given the market without its own panel. A refused line code renders as a REFUSAL with the API's sentence, NEVER as an empty page; proven by planting the swallow and watching the error vanish while the empty state appeared. Two empty states, because "no company matches your search" and "nobody writes this line" are different claims. **MEASURED (§ 1.44), because the insurer feature sat unreachable for weeks with every gate green: four API surfaces have NO web caller** — `admin/email-integration` (6 routes), `POST /imports/customers`, and the two known insurer gaps. The office administrator holds `email.integration.read`, `email.integration.manage` and `customer.bulk-import` with no way to exercise any of them. Recorded, not acted on. `auth/sso` and `orgs` are correctly absent (a provider callback and subdomain resolution are not nav destinations), and `customers/:customerId` was a false positive — a parameterised prefix cannot be grepped for literally. **§ 1.45: a cast that silences the compiler usually means the question was good** — `as never` on a concatenated translation key hid the fact that there were no structure labels in EITHER language. A total `satisfies Record<Union, string>` map is the form that cannot express it. **§ 1.46: the i18n guard caught a genuine REPEAT of what it was built for** — a dictionary created and merged but not registered, silently excluding 88 keys from the parity and collision checks, exactly as `auth.ts` once did. First time one of these guards has fired on its own stated scenario, which is the evidence the class earns its cost. **`translations.test.ts` is the guard that caught my omission**: it asserts every dictionary file on disk is REGISTERED and that each keeps exact AR/EN parity, so a new screen's dictionary cannot be half-wired. **A server-side refusal is only half a change — read the SCREEN that calls it.** Tightening the commission rate resolver would have turned `/commission`'s free-text line field into a 422 for anyone who typed, and two defects fell out of reading it: the resolver matched `code`/`nameEn` only, so on this ARABIC-PRIMARY platform the Arabic line name was a 422 (every test in that file was English — that is how it survived); and the screen's Arabic placeholder was worded differently from the seeded Arabic name, so following the hint had ALWAYS produced an unmatched rate. The field is now a `<select>` valued by CODE — language-independent submission, label follows the reader — with office additions filtered out because the API cannot resolve them, pinned by an option-count assertion. **The signup refusal named a route that could never help** — "provision users through POST /admin/users, which names the Organization explicitly", when `ProvisionUserDto` has no `organizationId` and the route only ever writes into the CALLER's office. Removed, not reworded: it now says a second office cannot currently be onboarded through the application, names Phase 4, and names the only two writers of `Organization` (both developer tools, neither reachable over HTTP). Pinned by a spec asserting the message does NOT mention `/admin/users`. § 1.11's 500 stays open by prior decision — and it caused its THIRD expensive diagnostic today. **A migration that converts rows and leaves the WRITERS is the shape to watch for** — it decays monotonically, and nothing fails while nothing reads the column. The four writers now populate the line FK and the money crossing (policy → governing commission rate) matches on it; proven by writing a row through each API path and reading the STORED row back, plus three plants. **§ 1.43: a baseline is a MEASUREMENT** — ask "does anything outstanding rest on this data?" in its yes/no form, and read what a reset breaks as a finding rather than noise. Both of today's findings came from there. **A generator's output is not evidence about the generator**: mine produced valid TypeScript that passed `tsc` and had silently dropped an export 86 spec files import — only reading the first `git diff` could catch that. **`npm run db:fixture:permissions` now exists** — the web e2e's permission grid said "GENERATED … regenerate rather than hand-edit" for months with no generator behind it; `verify.sh` gates the `--check` form. Read a generator's first diff: mine silently dropped an export 86 spec files import. **Read `IMPROVEMENTS.md`'s new opening principle** before the numbered entries: *prefer the form that cannot express the mistake over the form that requires you to notice it.* |
| 2026-09-19 | **Custom office-scoped RBAC, PHASE 3 of 5 — an office can now define its own roles, and two Highly Confidential reveals stopped riding on the permission that lets you read the record.** Nine commits; 177 permission codes became 182. **`/settings/roles` is the screen the whole rework exists for**: the office's own catalogue, a matrix over all 182 codes grouped by module, create/rename/retire/reactivate. `Role` gained `status` and `isSystem`; there is deliberately **no DELETE** — the `UserRoleAssignment` and `RolePermission` rows pointing at a role ARE the record of who held what and when — and `status` is enforced in every query that DECIDES access, with **FOUR routes into the last-administrator lockout guard** now guarded under the same per-office advisory lock — revoking a grant and deactivating the user (Phase 2), retiring a role, and **removing `user.manage` from a role via the matrix**, which I missed on the first pass and found by asking what else the new screen can take away: unchecking a box does not look like an access action, but an office that ends up with nobody holding `user.manage` cannot grant it back. **`isSystem` GRANTS NOTHING**, proven by a test that holds an `isSystem` role with zero grants and gets 403 everywhere; planting `if (user.roles.length > 0) return true` in `PermissionsGuard` is what proved that test can fail. **Every office got an `OFFICE_ADMINISTRATOR`** (22 codes, `isSystem`, strict MFA), granted to every user who could already administer users — asked as a CAPABILITY, never by role name — and **in addition to** their legacy role, never instead. Its grants are a verified strict subset of the legacy administrator's, which is what makes the migration a provably empty per-user effective-permission diff. A NEW office gets that role and **no business roles at all**; nothing in the application creates an Organization (the only two writers are the seed and the demo script), so the rule lives in both writers plus a test asserting an Organization with no active route to user administration is a defect. **The reveal splits differ deliberately.** `employee.manage` → `employee.read`/`.create`/`.update` + `employee.national-id.reveal` (per ROUTE: its DTO accepts only that field); `customer.national-id.reveal` split out of `customer.360-view.read` **per FIELD**, because the same endpoint reveals a phone number a Sales Officer needs for their own customer — a route-level gate would have stopped an officer phoning their own client. Compliance alone holds both; the administrator and the Manager both lose the employee one, which is the point. KYC proven intact end to end. **THREE REAL DEFECTS THE WORK FOUND.** (1) **The AC1 measurement exposed grant drift in the demo office**: the first diff was empty on db-test but had ONE row on dev, because `seed.ts` grants permissions to the DEFAULT office's roles only — so every code added after a demo office was created never reached it, and Office B's administrator role was 3 codes behind the grid (25 across all eight of its roles). Fixed at the source with a mirror-from-the-default-office helper; with the drift closed the dev diff is empty too. (2) **`/settings/users` had no web e2e at all, and its grant dropdown was broken**: the options carried the role NAME while `grantRole` posts that value as `roleId`, which the API validates with `@IsUUID()` — every grant an administrator made from that dropdown was a 400, and the default (set from the first catalogue id) matched no option. Found by writing the test the screen never had. (3) A lint rule caught the catalogue fetch setting state synchronously inside an effect; the house async-IIFE pattern is what it wants. **Two decisions**: the matrix **warns and saves** on classify+co-sign (a small office may want that, and `assertDifferentActors` still refuses a co-sign by whoever recorded it), and the two MFA attributes **are** editable behind a step-up challenge — the first consumer of `@RequireStepUp()` anywhere — because not exposing them would leave every custom role demanding MFA forever. **Verification**: typecheck 6/6, lint 3/3, build 3/3; unit **api 2998 · web 69 · db 36 (+12)**; i18n parity 58/58 (+53 keys, both languages); api e2e **83/83 spec files, 562 tests** across eight batches with the ran-file list diffed against the spec list (new `national-id-reveal-split` 5/5, `office-administrator` 6/6, `last-administrator-lock` 15/15); web e2e **505/505 (433 + 72 @a11y)**, new `roles.spec.ts` 11/11 and `users.spec.ts` 7/7. **AC1: per-user effective-permission diff across the administrator migration EMPTY on both databases** — 36,688 users on db-test, 31 on dev. Every new guard was proven by planting the regression and watching it fire: fourteen plants across the two reveal gates, the grid invariants, the orphaned office, the 23rd grant, the `isSystem` bypass, both UI reveal gates, the segregation warning, the step-up gate, the original dropdown bug restored, the fourth lockout route un-guarded, and an office's own role marked `isSystem`. | **A strict-subset grant set is what makes a "grant everyone this new role" migration provably safe.** Adding `insurer.relationship.manage` to the administrator would have broken that property, which is why it is deferred with the insurer backlog rather than for tidiness — `permissions.spec.ts` now asserts the subset so it cannot quietly stop holding. **The seed grants to the DEFAULT office only.** Any code added after an office was created never reaches that office's roles; the demo script now mirrors the default office's grants, and a future org-provisioning path must do the same or its offices drift silently. **An untested screen is where a half-migrated API lands.** `/settings/users` survived the move to id-addressed assignment with a name-valued dropdown because nothing exercised it; both admin screens now have specs. **Select controls by a `data-*` hook, not by index or label** — the matrix sorts by module then code, so an index follows the catalogue, and every role label appears in the fieldset, the table and every row's dropdown (five matches for one `getByText`). **`getByRole(name)` still matches case-insensitive SUBSTRINGS**: the matrix heading "Permissions for Claims Triage Desk" matched a module heading of "claims". **Two role-name sites remain by design** (SLA escalation routing + the bell recipients that follow it, and the Executive nav order) — they answer "which business function owns this", and the honest fix is Phase 4's routing table. **The permission cache is per-process and that now matters more**: editing a role used to mean editing the seed and redeploying, and is now a button — recorded in `permissions.service.ts`, with the fix named (a shared invalidation channel, not a shorter TTL). **Still to come**: Phase 4 (the unified screen's org-structure half, subdomain resolution, the business-function routing table), Phase 5 (migration rehearsal, dropping the `RoleName` enum type and the pre-migration rows). Insurer CRUD remains specification-only. |
| 2026-09-18 | **Custom office-scoped RBAC, PHASE 2 of 5 — no authorization decision reads a role NAME any more, and the two controls that failed OPEN are gone.** Ten workstreams over ten commits; 167 permission codes became 177. **The MFA pair is now a Role ATTRIBUTE, not a permission** — `Role.requiresMfaAlways` / `requiresHardwareToken`, defaulting to the STRICT value, because a permission could be granted away from the very Role screen Phase 3 adds. The migration backfills only the roles that must be RELAXED, so anything it does not recognise fails closed. **`@RequireRoles` and `RolesGuard` are deleted outright**: all 19 routes (not 17 — I had miscounted) already carried an equivalent `@RequirePermissions`, and each of those seven codes is granted to exactly one role, which is what makes the removal provably zero-delta. Cross-owner visibility became seven `<family>.all-owners.read` codes — named that way, not `.view-all-owners`, because `permissions.spec.ts` proves the External Auditor read-only BY CONSTRUCTION from the `.read`/`.view` suffix and widening that check would have traded a real safety property for a nicer code. The segregation signal keys on 13 checker PERMISSIONS and stays DETECTIVE; the reviewer pool keys on `access-recertification.review` with a second `...review.routine` code preserving its two tiers; `incident.classify` splits three ways; role names validate as bounded strings. **FOUR DEFECTS THE TESTS FOUND THAT READING DID NOT.** (1) `seed.ts` upserts grants and never REMOVES one, so narrowing `incident.classify` in the grid left every existing database with Executive Management still holding it — reseeding does not revoke. Migration `20261004120000` withdraws exactly that grant, scoped to the legacy role name; the seed was deliberately NOT made to prune, because under custom roles an office's own grants are precisely the rows absent from the grid. (2) `pg_advisory_xact_lock` returns `void` and Prisma cannot deserialize a void column — `$queryRaw` made every administrator revoke a 500; `$executeRaw` is the call. (3) A lock guard that threw SYNCHRONOUSLY out of a Promise-returning method. (4) A new e2e created a second Organization and leaked it, producing 17 failures in other files — `signup` refuses with two orgs, and the teardown could never have worked because `AuditLogEntry` is immutable by trigger. **TWO OF MY OWN TESTS COULD NOT FAIL, and planting found both.** The reviewer tiering test passed against a flattened pool because the mock listed the routine reviewer first and `pickReviewer` takes the first eligible id either way; the concurrent-revoke race test passed with the lock REMOVED, on I/O timing alone. Both were fixed — the mock now lists the fallback reviewer first, and the lock has a deterministic serialisation test that cannot pass by luck. **Verification**: typecheck 6/6, lint 3/3, build 3/3; unit **api 2994 (+11) · web 69 · db 28 (+4)**; api e2e **79/79 spec files, 527 tests** across seven batches with the ran-file list diffed against the spec list; contract 4/4; web Playwright **414/414**; a11y **70/70**. AC1 gate: per-user effective-permission diff before vs after, **0 unexplained differences** on both databases — every change is one of the 10 added codes or the 1 deliberate withdrawal. Seed verified on an EMPTY database twice, and the role-permission grid is **byte-identical (382 grants, one md5) across seeded-from-empty, dev and db-test**, which is what proves a migrated database and a freshly seeded one are indistinguishable. | **An `md5` gate is the wrong shape for a phase that adds permissions.** AC1 as written asked for byte-identical effective permissions; this phase adds 10 codes by design, so that could never pass and reporting it as met would have been false. The honest gate is every difference CLASSIFIED — 0 unexplained — which is what was run. **The web e2e has a THIRD copy of the permission grid** (`apps/web/e2e/fixtures/role-permissions.ts`); its header says regenerate rather than hand-edit, and a stale copy broke 4 Playwright tests in three files. Regenerate it from the seeded database whenever a grant moves. **Four role-name decisions REMAIN, deliberately**: SLA escalation targets, the bell routing that follows them, and the Executive nav order — they answer "which business function owns this", not "who may do this", and the honest fix is a routing table in Phase 4. **Phase 3 can now proceed**: nothing fails open, a custom role can be created and assigned, and a custom administrator role is guarded. Before it, decide two things this phase deliberately did not: whether `role.manage` / `permission.manage` should split into `.read` + `.manage` (they gate read-only GETs today and the matrix will read wrong), and whether role assignment should move from name-addressed to id-addressed. **`OFFICE_ADMINISTRATOR` is still unseeded** — the 20-code proposal is in the Phase 2 plan artifact and awaits confirmation; `user.manage` is already the anchor three workstreams key on. Insurer CRUD remains specification-only, as instructed. |
| 2026-09-18 | **Custom office-scoped RBAC, PHASE 1 of 5 — roles became per-office data, and authorization stopped resolving by name. Two latent cross-tenant leaks were live the moment the constraint changed, and a third bug locked every user out of everything until it was found.** `Role.name` was globally `UNIQUE`; it is now `@@unique([organizationId, name])`, plus `organizationId`/`nameAr`/`nameEn` and RLS. That constraint change is what made the old lookups a leak rather than a style problem: `PermissionRepository.findCodesForRoles` filtered `role: { name: { in } }`, which once two offices can each define a "Manager" returns BOTH rows and hands the caller their **UNION**; and `PermissionsService` cached under `[...roleNames].sort().join(',')`, serving one office's answer to the other for up to a minute. Both now key on `roleId` (uuids, unique across every office), and `AuthenticatedUser` carries `roleIds` beside the names it keeps for display. **The third bug is the one worth reading.** The first migration gave `RolePermission` an RLS policy scoped through a JOIN to `Role`, specifically to avoid denormalizing `organizationId` where a drifted copy would be the row granting access. That policy can never be satisfied: `tenantScopeExtension` sets `app.current_org_id` **only for models that CARRY `organizationId`**, so `current_setting` was always NULL, every permission read returned empty, and users authenticated fine and then 403'd everywhere — `/auth/me` showed `roles: ["SALES_RELATIONSHIP_OFFICER"]` beside `permissions: 0`. A second migration adds the column and answers the drift worry **structurally** instead: a composite FK `(roleId, organizationId)` → `Role(id, organizationId)` makes a disagreeing grant fail to INSERT, proven by attempting one. **MIGRATION preserved access exactly.** The 11 shared rows are ADOPTED by the lowest-id organization using each and COPIED for the others — so the adopting office's `UserRoleAssignment` rows never move — and an office gets only the roles it actually used: `default` keeps 11, `demo-office-b` gets exactly its 8, with no DPO/Executive/External-Auditor row it never had. The blocking gate was a per-user effective-permission diff before vs after: **empty, 31/31 users, byte-identical md5**. **Verification**: typecheck 6/6, lint 3/3, build 3/3; unit **api 2983 (+13) · web 69 · db 24**; api e2e **75/75 spec files** green across seven batches (`tenant-isolation` 33/33, +6 new), contract 4/4.  **CI then caught a second thing every local gate had missed: `prisma/seed.ts` was typechecked by NOTHING.** `packages/db/tsconfig.json` sets `"include": ["src"]`, so the seed and its seed-data modules were outside the program — `npm run typecheck` passed 6/6 across the monorepo while the seed still wrote the pre-migration `Role` shape and could not run at all. It failed on the one CI step no local script covers, on a database fresh enough to need a CREATE rather than an upsert-existing. A new typecheck-only `packages/db/tsconfig.seed.json` (a second config, because the build's `rootDir: "src"` makes widening the existing `include` a TS6059 error) is now wired into `db`'s `typecheck` — and it immediately caught a real defect in the very fix that motivated it: the edit that added `nameEn`/`nameAr` to `RoleSeed` had dropped `description`, which ran fine because types are erased. Verified by dropping and recreating a scratch database, migrating and seeding it from empty, twice. | **An RLS policy on a table the extension does not scope is not defence in depth — it is an outage waiting for a deploy.** The scoped set is derived from the DMMF as "carries `organizationId`", so a policy reading `app.current_org_id` on any other table matches nothing, forever. `tenant-scope.extension.spec.ts` now guards this: every RLS-protected model must be in `TENANT_SCOPED_MODELS`. **`tenant-scope.extension.ts` itself was never touched** — adding the column is what brings a model under Layer 1, which is the whole argument for deriving the set from the schema. **Two lists still FAIL OPEN and are Phase 2's first job**: `ALWAYS_MFA_ROLES` and `PRIVILEGED_ROLES` match on role name, so a custom role silently qualifies for the trusted-device MFA skip. Not yet exploitable — no custom role can exist until Phase 3's Role screen — so **Phase 2 must land before Phase 3**. The five visibility lists fail CLOSED by contrast, and the recertification reviewer pool would refuse to start a cycle for a custom-role-only office. **`maker-checker.util.ts` is untouched and needs no change**: 62 call sites compare user IDs, backed by 17 DB CHECK constraints, so multi-role cannot weaken separation. **`RoleName` the enum TYPE is deliberately kept** (nothing references it) — dropping it and the legacy rows is deferred a release so rollback stays cheap. **The seed is now typechecked — keep it that way.** `packages/db` runs a second `tsc` against `tsconfig.seed.json`; every remaining phase touches both the schema and the seed, and without it a broken seed reaches CI looking locally green. **Also run `npm run db:format` before pushing any schema change**: CI's gate is "formatting leaves no diff" and no local script runs it, which cost one red round-trip on a single line of field alignment. **Verify a seed change on an EMPTY database**, not the dev one — an upsert against rows a migration already created exercises the update path and never the create path, which is exactly the half that was broken. Still to come: Phase 2 (convert the 12 role-name-keyed decision points), Phase 3 (`status`/`isSystem`, Role CRUD + matrix, OFFICE_ADMINISTRATOR), Phase 4 (unified User/Employee screen), Phase 5 (migration rehearsal + the remaining tests). |
| 2026-09-17 | **The backup restore drill had failed every scheduled run since 2026-08-31 — three stacked faults, each hidden behind the one before it, and the third was the drill lying about what it verified.** **Fault 1 — the seed crashed.** Nothing in this job ever ran `prisma generate`: `npm ci` has no postinstall hook, and unlike `ci.yml` this workflow runs no turbo task (turbo's graph builds `@ibms/db`, which is where `ci.yml` quietly gets its client). `RoleName` was `undefined` and the seed died on `Cannot read properties of undefined (reading 'SALES_RELATIONSHIP_OFFICER')`. Fixed with a standalone `npx prisma generate` — no dotenv wrapper and no `DATABASE_URL` (generate only reads the schema); `npm run db:generate` would NOT work here because that script is `dotenv -e ../../.env` and this workflow only writes `.env.test`. **Fault 2 — there was no passphrase.** The repository has **zero** Actions secrets, so `BACKUP_DRILL_ENCRYPTION_KEY` was never set and the drill step would have exited 1 even once the seed was fixed. Fixing only fault 1 would have moved the red one step later. Now generates an EPHEMERAL per-run passphrase, secret still overriding when present. **Fault 3, the real find — the verification compared ESTIMATES and called the drift data loss.** It summed `pg_stat_user_tables.n_live_tup` after an `ANALYZE`, with a comment asserting that made the comparison "exact, not approximate". `ANALYZE` SAMPLES; `n_live_tup` stays an estimate. Proved rather than assumed, on ONE database with no restore involved: estimate **4,142,466** vs true count **4,141,630** — an 836-row phantom. Replaced with an exact per-table `count(*)` via `query_to_xml`, placed AFTER `END_EPOCH` so verification time is never charged against the RTO target; verified there are no partitioned or inherited tables (0 and 0, 130 base tables) that could double-count. **Verification**: the drill now runs locally end to end — **PASS**, 4,141,630 original vs 4,141,630 restored, 638s against the 900s RTO target, exit 0. | **Fixing the first failure in a stack tells you nothing about the second.** Three weeks of red were three different bugs; the third only became visible once the first two were fixed and the drill could reach its own summary. Check what comes AFTER the failing step before calling it fixed. **A comment asserting a property is not the property** — "ANALYZE makes this exact" was wrong, and one query against a single database disproved it in seconds. **Do not weaken a check that might be catching something real**: the estimate-vs-exact measurement is what established the FAIL was the metric and not the restore, and the fixed drill then matched that exact number on both sides. **The repo has NO Actions secrets** — any workflow written to require one has never run. **An ephemeral passphrase is the right key for this drill**, not a missing long-lived one: it proves the round trip against a throwaway seeded database and keeps a real credential out of a job with no real data to protect. |

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). This paragraph predates
Domain B #12–22, Domains C–H and Parts D–F, all of which have since been built —
**root `README.md` § Scope status is the authority, not this summary.** Part F's
bilingual UI is complete: all 93 screens — the 89 under `app/(app)/` and the four
`(auth)` screens that render before sign-in — read from
`apps/web/lib/i18n/translations/`, including the 403 permission-denied branch
(see § What's New). Part G's final verification
checklist is complete — all seven items closed and guarded. See § Known gaps for
each built item's deferred edges.

**Custom office-scoped RBAC is IN PROGRESS — Phases 1, 2 and 3 of 5 have landed.** The
fixed 11-role catalogue is gone as an architecture: `Role` carries `organizationId`, a
role name is unique per office rather than globally, and authorization resolves
permissions from `roleId` — never from a role name, because two offices may each define
a "Manager" with different grants. `Permission` stays a single global catalogue; only
the grants are per-office. The legacy 11 survive as ordinary per-office rows carrying
their old machine names, so nobody's access changed. An office now DEFINES its own roles
at `/settings/roles`, every office has an `OFFICE_ADMINISTRATOR`, and a new office gets
that role and nothing else.

Before touching anything role-related read **`docs/office-scoped-rbac.md`** — it
carries these four constraints in full, the things that will bite, and the Phase 4
hand-off:

1. **A role NAME is not an identity.** `PermissionRepository.findCodesForRoles` and
   `PermissionsService` both take ids, deliberately — a name-keyed lookup returns every
   same-named role across every office and hands the caller their union. The same
   applies at the API boundary: role assignment is id-addressed
   (`{ roleId }` / `{ roleIds }`), and the by-name repository lookups were deleted, not
   left dormant.
2. **An RLS policy is only enforceable on a model carrying `organizationId`**, because
   that is how `tenantScopeExtension` decides whether to set `app.current_org_id` at
   all. A policy on any other table matches nothing, forever.
3. **`Role.status` has to reach every query that DECIDES access**, not just the screen.
   A retired role that still satisfied the authorization read or the
   last-administrator count would keep granting everything it ever granted.
4. **`isSystem` grants nothing.** It stops the Role screen renaming, retiring or
   re-granting a row and is read nowhere else. A flag called "system" on a Role is
   exactly where an `if (isSystem) allow` bypass gets smuggled in; there is a test
   whose only job is to prove it does not exist.

Phases 4 and 5 (the unified screen's org-structure half, subdomain resolution, the
business-function routing table that retires the two remaining role-name sites; then
migration rehearsal and dropping the `RoleName` enum type) are not built.

**Insurer CRUD HAS shipped** — API and screens, with `insurer.relationship.manage`,
`insurer.read`, `insurer.directory.read` and `insurer.office-form.map` granted. This
paragraph said it was "deliberately deferred" for the whole branch that built it, which
is the doc rot `meta/lex/definition-of-done.md` names: a deferral is true when written
and nobody revisits it. What survives of that deferral is the property it protected —
those codes sit OUTSIDE the office administrator's strict-subset-at-migration set and are
listed explicitly in `ADDED_AFTER_THE_MIGRATION`, so a fifth cannot slip in without a
decision.

**Before touching anything insurer-related read `docs/insurer-management.md`** — the
eleven constraints in full, including the two that are easiest to break by accident: the
canonical name key lives in the DATABASE and the TypeScript mirror is for suggestions
only, and an insurer row holds two kinds of fact where only the company-level half may
ever leave the office. Its two siblings go deeper on the global catalogue
(`docs/insurer-master-registry.md`) and the cross-office directory
(`docs/insurer-directory.md`).

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # per-office roles + the global permission catalogue — RBAC needs these
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web

# Demo data for two Organizations, through the real HTTP API — DEV DB only,
# never .env.test. DEMO_PASSWORD is REQUIRED and has no default: the script
# refuses to start without one, because the password it used to carry was a
# literal in this repo. Sign in with it, then pair an authenticator at
# Settings -> Security. See apps/api/scripts/README-SEED-DEMO.md.
$env:DEMO_PASSWORD = (Read-Host "Choose a demo password")   # PowerShell
npm run seed:demo -w api

# Releases the demo logins from an MFA prompt nobody can answer — the repair
# for "I entered the correct code and it says the code is wrong" on an account
# the seed enrolled with a secret it never showed anyone. Passwords untouched.
npm run demo:release -w api
```

**Handing the system to someone who does not read code: `docs/first-run.md`** — the
bilingual (AR-first) first-sign-in walkthrough, walked against a running stack rather than
written from the source. It carries the measured per-account permission table, so if the
seed's role grants move, re-measure it rather than editing it by hand.

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
