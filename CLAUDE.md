@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-14 | **Sixteen demo accounts nobody could sign into, and the reason the seed said `0 failures` anyway.** Logging in as the Manager demo account landed on an MFA *challenge* — a six-digit prompt — rather than the enrolment screen, on an account that had never been enrolled by a human. **It is not a login bug**: `AuthService.login` branches on `user.mfaEnabled`, never on role policy, and `ALWAYS_MFA_ROLES` governs only the trusted-device skip, so the routing is right and `auth.e2e-spec`'s "blocks MFA-gated routes until enrolled, then requires a code at login" proves it. The accounts were simply left enrolled: `enrolMfa` gives every actor a TOTP secret generated in-process and shown to nobody, and `releaseActorsForHumanLogin` — which exists to switch that back off — **never ran against this data at all**. The seed finished 22:43 on 2026-09-13; the release was committed at 23:50, 67 minutes later. The report is the proof it would otherwise have run: `errors: []`, `actor: 16`, both offices present in `summaries`, and an office only lands there if `seedOrganization` **returned** — past the point the call now sits. All 14 stale credentials carry `lastUsedAt == createdAt` at 19:39–19:41Z: written by the seeder, never touched again. **So the honest count is zero of sixteen released, not one** — `demo.sales@office-b` was cleared by hand at 19:51Z while the fix was being written, which is the only thing that made it look like a loop that stuck once. Remedied by a scoped transaction over exactly the 14 locked rows (**SELECT 14 / DELETE 14 / UPDATE 14**, no re-seed), deliberately excluding `demo.sales@office-a`, whose credential was created 2026-09-14 10:37 and last used 14:44 — a real human pairing, separable from the seeder's because the seeder's `lastUsedAt` never moved. **Three holes in the script are what let a silent lockout ship, and all three are closed.** The release ran as the last statement of `seedOrganization`'s happy path — an actor is locked from the moment `enrolMfa` returns, so any throw in the ~70 lines between strands eight real accounts, and the caller catches office-level failures and carries on, logging one line about an office and never "nobody can sign in"; it now runs after the whole seed. It keyed on the in-memory `Actors` map, which holds only actors whose entire create-login-enrol attempt succeeded — and `enrolMfa` is **two** HTTP calls, so an actor that enrolled and then failed to verify owns a credential the map never recorded, while the "could not provision" guard throws and the other seven go unreleased too; it now finds accounts by **email**, via a single `actorEmail()` shared with `ensureActor` so a rename cannot leave the release hunting for names nothing answers to. And nothing checked the end state; a post-condition now re-reads the database and **fails the run, naming accounts**, if any demo actor is still `mfaEnabled`, owns a credential, or owes a password change. **Verification**: typecheck 6/6 with `tsc --listFiles` confirming the script is genuinely in the api program; api e2e **35/35** across `auth.e2e-spec` + `auth-mfa-session`; all 16 accounts probed live — 15 return a session and `MFA_ENROLLMENT_REQUIRED` on every real screen, `demo.sales@office-a` correctly challenges; and a full first-login round trip on an always-MFA role (session → 403 `MFA_ENROLLMENT_REQUIRED` → enrol+verify → **200 on a real screen** → next login challenges), restored afterwards. | **A demo account is handed back with MFA OFF and the run now fails if it is not** — sign in with the password alone, then pair an authenticator at **Settings → Security**; every other screen 403s `MFA_ENROLLMENT_REQUIRED` until you do, which is §4.3.2 working. **Never key a cleanup on the in-memory record of what succeeded** — it is exactly the set that excludes the half-finished cases the cleanup exists for. **A run that reports `0 failures` is not evidence the end state is usable**; that is what the new post-condition is for, and its absence is the whole of this defect. **The two new functions were typechecked but never EXECUTED** — the script has no dry-run path and the only way to run them is a full seed, which would reset `demo.sales@office-a` and wipe a real paired authenticator; run a minimum-scale seed against **db-test** if you want them exercised before trusting them. **`scripts/` is outside the api lint glob** (`{src,apps,libs,test}/**/*.ts`), so `npm run lint` never sees this file: it carries **210 pre-existing prettier errors at HEAD** and `--fix` reformats the whole thing (759 insertions), which is why this change leaves the formatting alone — lint it directly with `npx eslint scripts/seed-demo.script.ts` and compare the rule mix, not the count. |
| 2026-09-14 | **The whole app speaks Arabic — 89 screens, one dictionary per domain — and the four-state rule stops being asserted in English.** Every page under `app/(app)/` is now bilingual: **89 of 89** (the four `(auth)` pages were already done), against **2,410 keys per language** in exact AR/EN parity. Copy lives in **nine new namespaced dictionaries** under `lib/i18n/translations/` covering **seven page clusters** — customer service (6 pages), PDPL (9), finance (6), compliance (11), dashboards (14), operations (16), detail pages (16) — with the remaining 11 pages served by the `rfq`/`customers`/`leads`/`policy` dictionaries that already existed. Arabic is **written, not translated from the English**; where no sourced term exists it is marked in place rather than guessed (`complaints.ts` carries a `TODO(arabic-terminology)` on the Insurance Dispute Resolution Committee's name, the only place it appears). **Enum labels follow the policy-status precedent**: a new `lib/lead/lead-status.ts` replaces `leads/[id]`'s own `Record<string, string>`, which listed four of the five statuses — `DISQUALIFIED` rendered as the raw token, in Arabic too, the identical defect the policy detail page had; 16 `Record<Enum, TranslationKey>` maps now cover the enums a user reads, so a new member is a compile error. **The most useful thing the pass found is what the existing tests were not testing.** `rtl-layout.spec.ts` asserted the **English** column headers `Source`/`Completed` on a page it had itself switched to Arabic — it passed only because the screen was English-only, which is the very thing that test exists to deny; `four-state-screenshots.spec.ts` proved its AR loading and empty captures with English `"Loading…"`/`"No prospects yet."`; and three specs asserted raw enum tokens (`ESCALATED`, `post_issuance`, `lapse_risk`) as if they were labels. All five are corrected to the Arabic or the label. **A Playwright trap came out of that**: `getByRole`'s `name` match is **case-insensitive**, so a bare `"ESCALATED"` still matches the new `"Escalated"` label and proves nothing — `complaints.spec.ts` now asserts the label *and* `{ name: "ESCALATED", exact: true }` → `toHaveCount(0)`. **One state is deliberately NOT closed and is recorded, not hidden**: the 403 permission-denied branch is still hard-coded English at **54 sites across 48 pages**, even though this pass added the AR+EN keys for exactly those messages — all **54 `*NoPermission` keys are defined and unreferenced** (`tmNoPermission`, `dpolNoPermission`, `consNoPermission`, … plus `commonNoPermissionGeneric`). **Verification**: typecheck clean, lint clean, web unit 16/16; **web e2e 287/287 in ONE uninterrupted run, exit 0, 3.2 min**; api e2e **486/486 across all 74 files, 0 cached, exit 0** — unchanged, as a web-only change should leave them. | **Copy goes in the domain dictionary, never in the page.** A page-local `Record<string, string>` of labels is the defect `lib/policy/policy-status.ts` and now `lib/lead/lead-status.ts` exist to prevent — `tsc` cannot see it, because both sides are `string`. **The next commit wires the 403 branch**: the keys are already written and named per screen, so it is a substitution at 54 sites plus the ~25 stragglers this pass missed — `audit-trail` is the worst (five visible `<label>`s in English while the `aria-label` beside each is Arabic, so a screen reader and the screen disagree), then `Record a new …` headings on `employees`/`vendors`/`bcp-dr-plans`/`information-assets`/`documents` and `Timers`/`Workflow`/`Entity` on `sla-dashboard`. **`customer-service.ts` re-declares four `commChannel*` keys that `rfq.ts` already owns** and spreads later, so `rfqs/[id]` silently renders "Phone call"/"Customer portal" instead of its own "Call"/"Portal" — the merge in `translations.ts` claims prefixes make collisions impossible and these are the first four; rename the customer-service set when the 403 work lands. **Do not treat `policy.ts`'s 161 unreferenced keys as this pass's doing** — they were already orphaned at HEAD, because the policy screens read `pold*` from `detail-pages.ts`; that is pre-existing debt to fold in deliberately, not a regression to chase. |
| 2026-09-14 | **A Policies list page — the Policy Checking Officer finally has somewhere to land, and `GET /policies` had to grow to give it one.** Permission-gating the sidebar exposed that the role whose entire job is Process 20 QC saw **3** items and could reach a policy only by drilling through a customer or an opportunity. The obvious fix looked like one page; **the endpoint could not back it.** `PolicyService.list` threw **422 unless exactly one of `opportunityId`/`customerId` was supplied** — a scoped sub-query for a detail view, not a list — and `PolicyRepository` had no cross-customer finder at all. So: a new `findManyForActor` with **owner, status and search all in the SQL `where`**, `ListPoliciesQueryDto` widened with `status`/`search` shaped exactly like `ListCustomersQueryDto`, and an unscoped branch in `list()` (both existing branches untouched; two scopes at once is still refused). **Visibility is the same rule, expressed as a query filter**: `POLICY_CROSS_OWNER_ROLES` (Placement/Manager/Executive/Policy Checking) pass `ownerUserId: null` and work the whole book, everyone else is pinned to Customers they own. It **must** be in the query — filtering after a capped read is the `DpoWorkspaceService` defect, where the cap silently decides what the caller cannot see. **Capped at `POLICY_LIST_TAKE = 200`, deliberately not paginated**: 0 of 50 list DTOs in this repo carry a page param and only 2 of 94 repositories use `skip`, so inventing offset pagination here would make this the one list screen that behaves differently. **`PolicyView` gained `customer: { id, legalName }`** — identity only, never the Customer row — because a list that cannot say whose policy a row is, is not usable, and search already matches customer legal name. The page itself is the `customers/page.tsx` template, not a new one: submit-triggered search, the same four states in the same order, the same card grid. **A second bug the work surfaced**: `policies/[id]/page.tsx` held its own `Record<string, string>` status maps listing `PLACEMENT_REQUESTED` and `CHECKED` — **neither is a real `PolicyStatus`** — while omitting `CHECKING_IN_PROGRESS`, `DISCREPANCY`, `VERIFIED` and `ACTIVE`, so the state every completed policy ends in rendered as the raw token `ACTIVE`, in Arabic too. Both maps are replaced by one shared `Record<PolicyStatus, TranslationKey>` (`lib/policy/policy-status.ts`), so a new status is now a **compile error** rather than a token on screen. **Verification**: typecheck 3/3, lint clean, build 3/3, unit api **2927/2927** · web 16/16 · db 15/15; **web e2e 287/287 in ONE uninterrupted run, exit 0, 1.9 min**; api e2e **486/486 across all 74 files, cache bypassed, exit 0** (483 + the 3 new list tests). | **Policy Checking goes 3 -> 4 sidebar items and now lands on `/policies`.** The list is capped, not paged — if a book outgrows 200 rows the answer is a narrower filter, or a deliberate decision to introduce pagination **across all 50 list screens**, not just this one. **Never add a status label to a page-local `Record<string, string>`** — use `POLICY_STATUS_LABEL_KEY`; the typing is the only thing that kept the two policy screens from drifting apart again. **Two Playwright traps cost a full suite run each and are now recorded**: the config sets `reuseExistingServer`, so a `next start` left over from a killed run gets adopted — rebuild `.next` under it and every page you just changed serves a 200 shell whose client bundle 404s, which reads exactly like a broken component (9 failures, all on the rebuilt pages). Check `:3000`'s `StartTime` against `.next/BUILD_ID` before a run. Separately, a long session can exhaust Windows handles until every child process dies at spawn with **`0xC0000142`** — 285 of 287 Playwright workers crashed that way while `tsc` invoked directly still exited 0; it is a host symptom, never a code signal. **The two remaining directive gaps are NOT started — do not self-select them**: bilingual coverage (6 of ~30 domains) and the Manager/Exec sidebar at 45/38 links, which needs secondary pages moved inside parent records. |
| 2026-09-14 | **The frontend gets a real design system, and the sidebar stops showing every role every link.** Three things, in dependency order. (1) **Design tokens** (`apps/web/app/globals.css`): colour, spacing, type scale, radii, elevation, focus ring, light + dark, RTL logical-property conventions and reduced-motion. **The palette is SAMPLED from the approved login comp, not invented** — brand `#3b6790`, hero gradient `#273a4e -> #33516d`, surface `#f4f6fa`, ink ramp `#26303e / #525e6f / #828c9a` are literal measured values. Plus `components/ui/`: Button, Card + PageHeader, Table, Field (+ TextInput/Select/TextArea), Badge, EmptyState, **ErrorState** and Skeleton. ErrorState is a sibling of EmptyState rather than a `tone` prop, deliberately: an error is not an empty list, it needs a retry, and conflating them is how a failed request ends up reading "No data". **Tailwind was considered and rejected** — retrofitting it over 1,116 inline style objects across 92 pages is a mechanical rewrite with real regression risk, and tokens buy the same thing. (2) **The two chokepoints.** `lead/lead.styles.ts` (imported by **83** pages) and `auth/auth-form.styles.ts` (**87**) had become the app-wide design system by accident, because pages copied each other — a customers page importing `cardStyle` from a *lead* folder is normal here. Both now read tokens and hard-code nothing, **export names and shapes unchanged**, so ~92 pages inherit the design without being edited one by one. (3) **The sidebar renders from permissions.** All **69** links now gate on `user.permissions` from `/auth/me`, grouped under 9 bilingual headings; every code was derived MECHANICALLY from the destination route's own `@RequirePermissions`, never guessed. Per-role counts went from 69-for-everyone to: Policy Checking **3**, Admin 9, Claims 12, DPO 14, Auditor 14, Placement 15, Finance 16, Sales 27, Compliance 30, Exec 37, Manager 44. **Two findings the gating produced.** **A lockout, caught before it shipped**: the obvious permission for `/settings/security` is `security-config.read`, which is **ADMIN-only** — but that page is pure self-service MFA enrolment, and `MfaRequiredGuard` 403s every other screen until a user enrols, so gating it would have stranded **ten of the eleven roles** with no route to unblock themselves. It renders UNGATED in the account footer; Home is the only other ungated item. **A live production bug**: `complaints/page.tsx` held ROLE NAMES in `LOG_ROLES` but passed them to `hasAnyPermission`, which compares permission CODES — it can never match, so **the complaint log form and its acknowledgement-PDF button were invisible to every role in the running app**. §10.4 converted the two sibling constants (`ESCALATE_ROLES`/`CLOSE_ROLES`) and missed this one; `tsc` cannot see it because both sides are `string[]`. Fixed to `['complaint.log']`, seeded to exactly the five roles previously listed, so intent is preserved rather than changed. A codebase sweep found no other instance. **Verification**: web lint + typecheck + build + unit **16/16**, 0 cached; **web e2e 280/280 across all 74 spec files in ONE uninterrupted run, exit 0, 3.1 minutes**. | **New screens compose `components/ui/` primitives and read tokens — never hard-code a colour, and never add a third `.styles.ts` design system.** The two chokepoints exist to carry their 170 existing consumers; their export names and shapes must stay stable or dozens of pages break at once. **Never gate `/settings/security`** — see the lockout above; it is the same class as the demo-seed `mfaEnabled` trap. **`apps/web/e2e/fixtures/role-permissions.ts` is GENERATED** from `packages/db/prisma/seed-data/permissions.ts`: 71 of 74 Playwright specs mock `/auth/me`, and the sidebar renders off `permissions`, so every spec now returns a realistic set via `permissionsForRoles(roles)`. Regenerate it when the seed grid changes — hand-editing recreates the duplicated grid §10.4 exists to prevent. `hasPermission`/`hasAnyPermission` now **fail closed** on a missing `permissions` field rather than throwing, since they run in the render path of every authenticated screen. **Three directive gaps remain open and are NOT started — do not self-select them**: bilingual coverage is 6 of ~30 domains; Manager/Exec still see 44/37 links, which needs secondary pages moved inside parent records (a per-module refactor, not a nav change); and **there is no Policies LIST page** — `app/(app)/policies/` holds only `[id]/`, which is why Policy Checking sees 3 items, even though the API already serves `GET /policies` under `policy.read`. |
| 2026-09-14 | **`npm run seed:demo` — demo data for two Organizations, driven through the real HTTP API.** Seeds employees, leads at every funnel stage, ~500 customers, and full sales-to-policy pipelines (risk profile → needs assessment → programme → opportunity → RFQ → quotation → comparison → recommendation → client decision → placed/issued/checked/delivered policy) plus invoices, claims, complaints, incidents and vendors — every row through the same endpoints, DTO validation, permission checks and workflow rules a real user's click goes through, so nothing it creates could not have been created by hand. Full run: **1,292 rows, 0 failures, 219s**; all 30 pipeline policies reach `ACTIVE`. **Six defects were found by EXECUTING it and fixed before it was committed, and the two that mattered were invisible to review** — the DTO field names, enum values and all 26 actor→permission pairs were correct from the start. (1) **It could not have run at all**: `MfaRequiredGuard` is a global `APP_GUARD` rejecting any user with `mfaEnabled = false` — **every role, not just the always-MFA three** — while `login` returns an MFA *challenge* rather than a session when it is true. No single value works; the account must log in without MFA and enrol over that session, which is exactly what every e2e spec's `makeUser` does and why they all do it. (2) **It would have locked every demo account out permanently**: the enrolment secret is generated in-process and never shown to anyone, so leaving `mfaEnabled = true` left a six-digit prompt nobody can answer, on the very accounts a demo depends on. Also: the RFQ line was picked from the INSURER's list rather than the lines the Opportunity's programme actually designed (~1 pipeline in 3 died there); `"Property All Risks (Fire)"` is not a coverage line at all (`COVERAGE_LINES` is the canonical set — a near-miss name is a line that does not exist); policies incepted in the FUTURE while losses were dated in the PAST, so no claim could fall inside cover; and re-runs 409'd re-linking actor accounts already tied to an `Employee`, which made the script's own "safe to re-run" claim false. **Verification**: smoke + full run above, 5/5 tenant-isolation checks against the seeded data (cross-office `GET` by id → **404, never 403**, with a "sees its own → 200" control so the 404s mean something), typecheck 6/6, lint 3/3, build 3/3, unit api 2926/2926 · web 16/16 · db 15/15, api e2e **483/483 across 74 files**. | **It targets the DEV database (`.env`), never `.env.test` — `npm run test:e2e` is unaffected, and the run confirmed it: 74 files, not 75.** The script lives in `scripts/` under `*.script.ts` with its own vitest config, so the e2e suite's `test/**/*.e2e-spec.ts` glob cannot collect it — two independent reasons, deliberately. **Demo accounts are handed back with MFA OFF** — sign in with the password alone, then pair an authenticator at **Settings → Security**. Every other screen 403s `MFA_ENROLLMENT_REQUIRED` until you do — that is §4.3.2's forced enrolment working, not a fault, and `/auth/me` plus the two enrolment endpoints are the only routes reachable before then, which is what makes it possible at all. **That sentence was written as settled fact and was not true of the demo this row describes.** The run behind the committed `seed-demo-report.json` finished an hour before `releaseActorsForHumanLogin` was written, so all sixteen accounts were handed back `mfaEnabled = true` holding a TOTP secret generated in-process and shown to nobody — a six-digit prompt with no answer, on every account the demo depends on. Repaired in the database and hardened on 2026-09-14 so it cannot recur silently: the release now runs after the whole seed instead of at the end of each office's happy path (a throw anywhere in between used to strand eight already-enrolled accounts, surfacing only as one line about an office), finds accounts by **email** instead of from the in-memory actor map (which omits any actor whose two-call enrolment half-succeeded, and every actor of an office that aborted), and ends with a post-condition that re-reads the database and **fails the run**, naming any account still locked — the absence of that check is why a report reading "1,292 rows, 0 failures" sat next to sixteen unusable logins. **Never leave a seeded account with `mfaEnabled = true` and no paired authenticator.** The script sets `SCHEDULED_JOBS=disabled` itself before boot — `.env` does not carry that flag the way `.env.test` does, so all 19 cron jobs would otherwise fire mid-run, including a real multi-megabyte sanctions download. `seed-demo-report.json` is **gitignored**: it is run output and records the demo accounts and their shared password. Scale is env-configurable (`DEMO_CUSTOMERS_PER_ORG` etc.) — smoke-test small before a full run. See `apps/api/scripts/README-SEED-DEMO.md`. |
Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). This paragraph predates
Domain B #12–22, Domains C–H and Parts D–F, all of which have since been built —
**root `README.md` § Scope status is the authority, not this summary.** Part F's
bilingual UI is now app-wide: all 89 screens under `app/(app)/` render in Arabic or
English from `apps/web/lib/i18n/translations/`, the 403 permission-denied message being
the one state still hard-coded English (see § What's New). Part G's final verification
checklist has not been run as a formal, evidence-attached gate. See § Known gaps for
each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web

# Demo data for two Organizations, through the real HTTP API — DEV DB only,
# never .env.test. Sign in with the password, then pair an authenticator at
# Settings -> Security. See apps/api/scripts/README-SEED-DEMO.md.
npm run seed:demo -w api
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
