@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-10 | **Part B — screening provider architecture, taken to provider-ready state (PR #15).** The rule the whole build enforces: **a screening that did not happen is never reported as a clear one.** Three adapters behind one contract (`built_in` OFAC/UN cache, `on_premise` yente, `commercial`) plus a `NotConfiguredProvider` null object; the registry deliberately does NOT fall back to built-in when an external provider is misconfigured. Five attempt outcomes, only `NO_MATCH` mapping to CLEAR, enforced three times over (contract, service, and a DB CHECK that refuses the wrong mapping from any write path). **Workflow holds (§17)**: `decide()` previously asked only whether `ScreeningResult` rows EXISTED — a file whose screening came back NOT_CONFIGURED has three of them, all PENDING_INVESTIGATION, and nothing read them, so it approved exactly like a clean file. Now NO_HOLD / REVIEW_REQUIRED (written, attributed `ScreeningHoldRelease` row) / BLOCKED, with two floors configuration cannot relax. **Dataset generations (§6/§7)**: the sync upserted ~19,000 rows and then deleted what it had not stamped, neither step transactional, so a screening mid-sync could read a half-written list — which returns CLEAR. Entries now belong to a generation and a screening reads only the PUBLISHED one; publishing is one transaction flipping one row, with a partial UNIQUE index making two published generations per source unrepresentable. Rollback restores a retained generation against a written reason. **Case lifecycle (§16)**: OPEN → ASSIGNED → UNDER_REVIEW → (ESCALATED) → CLOSED alongside the existing decision, with append-only case notes; a decision from OPEN is unreachable, not merely discouraged. **Identity discriminators (§11/§12)**: `ScreeningSubject` had declared `dateOfBirth`/`nationality` since the contract was written and no table carried either, so every match this system ever raised agreed on NAME ALONE; a subject fingerprint now also detects a UBO added after screening. **Four bugs found by testing my own work**: the recurring re-screen batch never re-screened anybody (idempotency key had no time component — for an external provider, ongoing monitoring stopped dead after each customer's first screening); an unhandled P2002 the code's own comment claimed was handled; Serializable isolation on publish broke the normal concurrent two-source sync; and `GET /kyc-records/:id/screening-hold` had no ownership check, so any `kyc.capture` holder could read any file's hold state. **Verification**: api unit **2698/2698**; new e2e — dataset-lifecycle 12/12 (including continuous reads during a live publish proving no intermediate state is observable), case-lifecycle 12/12, security 12/12 across ten categories, hold 11/11; Playwright screening-health 18/18 covering four render states plus permission-denied in LTR and RTL. `docs/screening-provider-setup.md` is the runbook; `docs/screening-traceability-matrix.md` maps requirement → implementation → test → evidence. | **There is still NO PEP data** — OFAC and UN are sanctions lists, and real PEP screening needs a commercial contract; the health endpoint reports `pepOperational: false` and the screen says so. The **commercial adapter has never run against a live vendor** (no credentials) — verifying it is the first task of whoever brings a contract. **yente was not run** on this host (C: drive full); the compose profile is defined and `docker compose --profile screening config` validates. New env vars, all optional: ten `SCREENING_HOLD_*` levels (two non-relaxable), `SCREENING_STALE_AFTER_DAYS`, `SCREENING_IDEMPOTENCY_WINDOW_MINUTES` — **do not raise the last one past the re-screening cadence**, that is exactly the bug fixed here. Migrations 20260922100000 → 20260924140000. |
| 2026-09-10 | **Process 49 — fuzzy sanctions matching + a human match-review queue.** The request was "make screening use a real provider"; the premise was outdated. A real integration already existed and worked — `watchlist-fetchers.ts` pulls the **live OFAC SDN** and **UN Consolidated** lists (free, no API key) and `ScreeningService` has matched the synced cache in production all along. Verified live before touching anything: HTTP 200, 5.7 MB, **19,369 records** parsed by this repo's own parser. `IMPROVEMENTS.md` §5.2 was simply stale — the same "a claim outlives the session that made it true" disease that file's own 2026-09-07 note describes; it is now rewritten. **What was genuinely broken:** matching was exact token-set equality, which silently missed the two commonest real shapes for a Jordan-based broker — a different romanisation of the same Arabic name (Muhammad/Mohammed), and a four-part national-ID name against a two/three-part list entry. Both produced a **CLEAR** result, the worst failure mode a sanctions control has. **Built:** `watchlist-match.config.ts` — canonical-token containment matching (entry tokens ⊆ subject tokens) reusing the curated transliteration table Part F item #6 already ships, so an Arabic-script given name collapses to the same key as its Latin spelling; `WatchlistEntry.canonicalTokens TEXT[]` with a **GIN index** and a raw-SQL `<@` containment query (Prisma cannot express the operator, and filtering 19k rows in JS would table-scan on every screening); a `ScreeningMatch` review queue with `GET /screening/matches` + `POST /screening/matches/:id/review`, a mandatory written reason on both outcomes, and a status-conditional write so two reviewers cannot overwrite each other. **Conservative by explicit user decision**: a match escalates to EDD and raises a queue item but NEVER auto-blocks or auto-suspends — fuzzy matching produces false positives by construction, so the call is a person's; clearing a match does not unwind the EDD escalation it caused. Single-token entries are excluded from fuzzy matching entirely (subset-matching one token against a four-part name is a substring search over 19,000 rows, not screening). **Verification**: +25 api unit tests → **2506/2506**; proven against the LIVE OFAC list loaded into db-test — a name plus an extra middle name went **MISS → HIT (fuzzy)** while an exact name stayed a HIT, and `Mohammed`/`Muhammad`/`محمد` all canonicalise identically; `typecheck` 6/6, `lint` 3/3, `build` 3/3, `test:security` OK; db-test cleaned of the 19,379 loaded rows afterwards so the e2e suite is unaffected. Migration `20260919120000_screening_fuzzy_match_review`; no new permission (`sanctions-pep.screen` already covered it). | **Still open and NOT to be self-selected: there is no PEP data.** OFAC and UN are sanctions lists; the backlog asks for "sanctions/PEP/AML" and no free PEP source exists — that needs a commercial provider (Dow Jones / Refinitiv / ComplyAdvantage) with a contract and API key. The adapter shape was scoped and deliberately NOT built, because it could not be tested against the real service without credentials. Also: **the sync has never been run on a real deployment**, so `WatchlistEntry` starts empty and every real-list check returns CLEAR until someone runs the 12-hourly `WatchlistSyncScheduler` or `POST /watchlist-sync/run`. The transliteration table covers ~50 common Arab **given** names only — family names across scripts still will not match. |
| 2026-09-09 | **Gap-closing pass over `IMPROVEMENTS.md` — plus six defects it found that were not tracked anywhere.** The previous session's `STATUS_90_PERCENT_COMPLETE.md` claimed all gates green; re-running them found `lint` red (2 `react-hooks/set-state-in-effect` errors in `apps/web`), `test:security` red (8 high advisories), and the single unit failure mis-attributed (`security-headers.middleware.spec.ts`, a stale test vs. a deliberate behaviour change — not `app.controller.spec.ts`). **Two of that session's four "fixes" shipped defects.** Fix #1 injected `CommissionRepository` into `InvoiceService` without providing it in `FinanceModule` — **the API could not boot**; unit tests construct the service by hand with mocks so nothing caught it, and no e2e had been run since. Fix #3 dropped `Receipt.invoiceId @unique` with no application logic, reinstating a P0 recorded in `IMPROVEMENTS.md` §2 (two receipts **and** two `in` `ClientFundsLedgerEntry` rows per invoice) and turning `CollectionService.finishReceipt`'s `P2002` branch into dead code — **and its migration never actually ran**: `ALTER TABLE ... DROP CONSTRAINT IF EXISTS` silently no-ops against a Prisma `@unique`, which Postgres holds as a unique INDEX, so the index survived every migrated database while `schema.prisma` said otherwise. That divergence is worse than either state: a FRESH database built from the schema had no protection, a migrated one did, and `db:migrate:status` calls both "up to date" because it compares migration history, not schema. Caught by an e2e writing a second instalment, not by reading the SQL; migration `20260909160000` now issues the real `DROP INDEX`. Also found: `refund.disburse` was the ONE `@RequirePermissions` code missing from the seed grid (403 for every role), and had it been reachable it checked no approval at all — a maker/checker bypass on real money out; the new rate limiter trusted `X-Forwarded-For` unconditionally (spoof the header, get a fresh bucket — a complete bypass) **and** made all 63 api e2e spec files unrunnable (5 auth requests / 15 min, one static store, every request from 127.0.0.1 → `429` on the sixth signup); `common/sanitization.util.ts` was dead code duplicating `document-html.util.ts`'s `escapeHtml`; and `SECURITY_FIXES.md` asserted fixes absent from the tree (lodash `_.template` removal, YAML parse timeouts, request-size limits, "rate limiting on all routes"). **Built (user-approved scope):** admin user provisioning (`POST /admin/users` + grant/revoke/activate, `user.manage` — previously seeded with no endpoint, so a production-seeded DB had no user able to sign in and no way to grant a role) with an opt-in seeded bootstrap admin; the **renewal module** (Part 3.9) — a nightly lead-time sweep opening a `RenewalCase` per expiring ACTIVE policy, which gives THREE already-shipped features their missing producer (the per-`RenewalCase` `LossRatio` upsert, Process 46's retention sweep, and `renewal_workflow_start`, one of only two `SLA_REGISTRY` entries with no caller — a Part G gate failure); the **Executive Dashboard** (#64, `dashboard.executive.view` — the other seeded-but-unwired permission), composed from the five sibling dashboards rather than re-querying, deliberately, so an executive figure cannot disagree with the dashboard it summarises; and **partial payments end to end** (instalment receipts, a row lock on the parent `Invoice` replacing the dropped `@unique` as the race gate, `Receipt.reference` as the idempotency key, and the #33 ageing / #34 payables readers corrected — the old ones dropped a part-paid invoice from ageing entirely, understating receivables). **Dependencies — a mis-diagnosis, now reverted:** `npm ls` reported the tree `invalid` (swagger 5.2.1 against Nest 11), which was read as a repo problem and "fixed" by regenerating `package-lock.json`. It was a **stale local `node_modules`** — `apps/api/package.json` and the lockfile already agreed on 11.4.7/6.1.3 and CI had been installing them cleanly all along. The regeneration then broke CI twice over: npm writes only the current platform's optional deps on a fresh resolve (npm/cli#4828), dropping the Linux `@rollup/*` binaries; and it **de-hoisted** `vitest` and `next` out of root `node_modules`, which `@testing-library/jest-dom` and `eslint-config-next` both depend on being there — breaking web unit tests and web lint on every platform. The lockfile is restored byte-for-byte. **This lockfile's hoisting layout is load-bearing and undeclared — do not regenerate it casually**; a deliberate refresh needs its own change, generated on Linux and verified with `npm ci && npm run test && npm run lint`. `multer` remains acknowledged in `scripts/audit-check.mjs` (no upgrade path — Nest 12 pins the identical version — and unreachable: zero multipart routes). **Verification**: api unit **2481/2481** (from 2423/2424 with one failure; +56 net: refund-disburse 11, user-admin 14, renewal 22, executive 4, rate-limit 1, plus the collection cases rewritten for instalment semantics); new `user-admin.e2e-spec.ts` **6/6** proving the bootstrap path end to end (provision → login → 403 pending MFA → enrol → 200 on a role-granted route); **full 64-file api e2e suite 322/323 in 15 min** — the single failure is the §1.2 TOTP timing flake (`rbac.e2e-spec.ts` passes 6/6 in isolation), NOT an assertion. That suite could not run at all before this session's rate-limiter fix, and on the un-reset db-test it was 311/323 in 44 min with all 12 failures being timeouts; resetting db-test (36,470 → 134 users, with the user's explicit consent) took `rbac.e2e-spec.ts` from 557 s to 10.4 s, which is the measured proof of §1.1's cumulative-DB diagnosis; `typecheck`/`lint`/`build` green across all workspaces; `test:security` green. Migration `20260909160000_partial_payment_receipts`; +3 seeded permissions (157). | **Read `IMPROVEMENTS.md`'s 2026-09-09 note first** — it lists what closed, what was found, and what was deliberately left. `STATUS_90_PERCENT_COMPLETE.md` now carries a correction box; treat its remaining claims as unverified. Still open and NOT to be self-selected: real screening (§5.2), the remaining PDPL systems (§5.1), the drafted/unsourced values (§4), reporting SQL aggregation (§6.1), standalone refund raise + the over-payment→`Refund` bridge (both need `Refund.endorsementId` nullable). New env vars, all optional: `BOOTSTRAP_ADMIN_EMAIL`/`BOOTSTRAP_ADMIN_PASSWORD` (seed a first administrator — **required for any non-dev deployment**), `TRUST_PROXY_HEADERS` (honour `X-Forwarded-For` behind a trusted proxy), `RATE_LIMIT_ENABLED` (force the limiter on outside production). |
| 2026-09-08 | **Part F item #8 — four-state (loading/empty/error/populated) screenshot evidence — PART F IS NOW COMPLETE.** Framed in the backlog as "a verification DISCIPLINE overlay on 1-7, not a separate build item." Two scoping decisions confirmed via `AskUserQuestion` after a research pass: (1) scope is the ~8 screens items #1-7 actually built/touched, not the whole app's 84 `page.tsx` files — only 13 commits were ever tagged "Part F item #N," touching mostly document-generation infra and global CSS/layout, not a broad per-page sweep; (2) capture mechanism is plain `page.screenshot()` PNG evidence, not Playwright's `toHaveScreenshot()` visual regression — matches `verification-contract.md`'s own framing ("a state with no screenshot is a state that is not implemented" — proof of existence, not a pixel-diff test) and avoids brittle cross-platform font-rendering failures on a bilingual RTL/LTR app. Research confirmed no screenshot infrastructure existed at all (no `toHaveScreenshot`, no screenshots dir, no `screenshot:` config) but the 4 states themselves mostly already exist in real code (checked `customers`/`leads` pages directly — genuine loading/empty/error/populated branches, an established house convention). New `apps/web/e2e/four-state-screenshots.spec.ts` — 10 tests across 8 screens (`/customers`, `/customers/[id]`, `/watchlist-sync`, `/complaints`, `/rfqs/[id]`, `/opportunities/[id]`, `/prospects`, `/vendors`), capturing whichever states are genuinely applicable per screen (skipping e.g. "empty" on a detail page, or "loading" where no visible indicator exists), rendered in Arabic where relevant to demonstrate RTL/bidi in the same screenshot. `/opportunities/[id]`'s populated capture shows all 4 of item #7's document-download buttons on that page at once (recommendation, policy schedule, certificate, invoice); spot-checked visually, not just asserted present. **Found and fixed a real bug in the new test's own fixtures (not application code)**: `GET /quotations?rfqId=` returns `QuotationChain[]` (grouped per insurer, `{current, versions, history}`), not a flat `QuotationVersion[]` — the wrong shape crashed `QuotationsSection.tsx` (`chain.current` undefined), surfacing as Chrome's own "This page couldn't load" rather than a React error boundary; found via `page.on('pageerror', ...)` in a throwaway diagnostic spec, not guessed. Screenshots save to `test-results/four-state-screenshots/<screen>/<state>.png`, already covered by the pre-existing `test-results/` gitignore entry — not committed, the same treatment Playwright's own trace files get. **Verification**: +10 new Playwright tests → full web suite **309/309 green** (a genuinely fresh full run, not assumed unaffected). `npm run typecheck`/`lint`/`build` (web) clean. No backend files touched, confirmed via `git diff --stat` (one new file). | No migration, no seed change. **Part F (backlog Part 11) is now fully complete** — all 8 named items either shipped or explicitly, deliberately scoped down by the user, every deferred edge documented in `ibms-brain/meta/context/bilingual-ui.md`. Do not self-select resuming item #5 (Hijri/multi-currency), item #6 (same-script typo tolerance/`Insurer` search), item #7 (real `Document` persistence), or item #8 (the other ~75 app pages, or visual-regression testing) without an explicit fresh user go-ahead — none of it is "finishing Part F." |
| 2026-09-08 | **Part F item #7 — invoice + certificate of insurance, the 5th and 6th (final) of 6 document types — ITEM #7 IS NOW COMPLETE.** Invoice (`GET /invoices/:id/document`, `client-accounting.read`): the first item #7 document with a FLAT, book-wide visibility permission rather than a scoped one — confirmed by re-reading `InvoiceService`'s own header comment ("there is no per-owner visibility filter") before building, so `InvoiceDocumentService` reads `InvoiceRepository`/`CustomerRepository`/`PolicyRepository` directly, no `getByIdWithCustomer()`-style helper needed. Content, confirmed via `AskUserQuestion`: deliberately EXCLUDES `commissionDeducted`/`netRemittance`/the insurer `Remittance` leg (internal broker-insurer economics) even though `FinanceSection.tsx`'s own "Billing" block shows them on screen — the recommendation-report document's "internal governance metadata stays internal" precedent, applied again. Certificate of Insurance (`GET /policies/:id/certificate` — a SEPARATE endpoint from the pre-existing `GET /policies/:id/document`, which stays the schedule-summary document): reuses the exact same `PolicyService.getByIdWithCustomer()` visibility read and `schedules.length === 0 → 422` gate the schedule-summary slice already built; content, also `AskUserQuestion`-confirmed, is a genuinely SHORTER, different Certificate-of-Insurance convention (insured/policy number/insurer/line/period/a one-line sum-insured summary) — deliberately not the schedule summary's content under a new heading. A shared `coverageFigureEntries()` helper was promoted (byte-identical relocation) from a private function in `policy-schedule-summary-document.service.ts` into `policy.config.ts`, since the certificate needed the identical JSON-entries-for-display read as a second consumer. **A `@code-reviewer` pass found 1 real BLOCKER, fixed before this was considered done**: the certificate made an UNCONDITIONAL present-tense "currently in force" attestation with no check on `policy.status` anywhere — a CANCELLED or EXPIRED policy would still get a certificate falsely claiming active coverage, and this document type exists specifically to be handed to a third party (landlord/regulator/lender) to rely on as proof of that; fixed by refusing with 422 (the same shape the existing data-availability gate already uses) for `CANCELLED`/`EXPIRED`, verified with a new e2e assertion. A MINOR was also fixed: the invoice document was rendering the raw internal `Invoice.status` collection-cycle enum (`RECONCILED`/`REMITTED`/etc. — the broker's own settlement progress with the insurer) verbatim, in tension with that same document's own stated commission-exclusion decision; replaced with a client-facing "Outstanding"/"Paid" label derived from whether the client's own collection receipt exists. **Verification**: +23 new api unit tests (`invoice-document.template.spec.ts` +12 incl. 1 added during the review fix, `certificate-of-insurance.template.spec.ts` +11) → api unit **2416/2417** (from 2394; the 1 failure is `app.controller.spec.ts`, a pre-existing zero-diff failure that also fails in complete isolation, unrelated to this change); +2 new api e2e tests, each with multiple assertions (permission/404/AR-EN-DUAL/invalid-language cases, `EXTERNAL_AUDITOR` proving the invoice endpoint's book-wide-not-owner-scoped permission, the new CANCELLED/EXPIRED 422 proof on the certificate, and a same-policy size proof that the certificate is genuinely smaller/different content than the schedule summary) — targeted run of the 2 directly-touched e2e files **16/16 green**, re-confirmed after the review-fix round; +richer assertions inside 2 pre-existing Playwright tests (no new test cases) — full `rfq.spec.ts` **29/29 green** (required a fresh `npm run build`, since Playwright's `webServer` serves the last build's output, not live source — a recurring gotcha this project's own history already hit twice before); full api+web `typecheck`/`lint`/`build` clean. **A genuine host-level disk-space crisis mid-session, unrelated to the code, consumed significant time**: the C: drive hit 0 bytes free (Docker Desktop's `docker_data.vhdx` had grown to ~42GB and never auto-shrinks — `docker builder prune` freed 22.82GB inside the VM but only ~640MB came back on the host), followed by a separate Docker Desktop stuck-backend recurrence (the same failure mode this project has hit before) — both required the user's own hands-on fix (an elevated VHDX compaction is still outstanding; a full tray quit + relaunch resolved the stuck backend). Given that time cost and this host's own repeated prior failure to complete either full suite under sustained memory pressure, **the full 63-file api e2e suite and the full 60+-file web suite were NOT attempted fresh this round** — the targeted evidence above stands in, the same accepted resolution every earlier item #7 round used. | No migration (two new `invoice`/`certificate_of_insurance` `DocumentTemplate` seed rows). **Part F item #7 (system-generated bilingual documents) is now fully built — all 6 named document types exist.** Read `ibms-brain/meta/context/bilingual-ui.md`'s "What item #7's invoice slice covers"/"What item #7's certificate-of-insurance slice covers" before assuming further scope: real persistence / a `Document` audit trail for a generated file remains explicit, documented future work (this app has no real object storage anywhere) — do not self-select building it. Both full suites (api e2e, web e2e) need a genuinely fresh run once host memory/disk allow. See project memory `project_docker_vhdx_disk_full.md` before troubleshooting a future disk-space issue on this host. |
Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
