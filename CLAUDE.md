@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-02 | Part C #21 (Policy Delivery) landed — extends the `policy` module (**no migration** — the `DeliveryRecord` model (`policyId @unique`, `deliveredAt`, `method`, `recipient`, `receiptAcknowledgedAt`), the `policy.deliver` perm (`[SALES, PLACEMENT]`), and the `VERIFIED → DELIVERED → ACTIVE` map all already existed). New `PolicyDeliveryService` + `PolicyDeliveryRepository`. `POST /policies/:id/delivery` (`{ method ∈ email\|portal\|courier\|in_person, recipient, deliveredAt? }`, `policy.deliver`) — drives `Policy VERIFIED → DELIVERED` through `WorkflowTransitionService.transition` (its status-conditional `updateMany` is the race gate — a concurrent delivery → 409; an "already in status DELIVERED" engine rejection is **normalised to the same 409**, review MINOR), then creates the one `DeliveryRecord` (`policyId @unique` → `P2002` → 409); a crash-recovery re-entry branch (status already `DELIVERED`, no `DeliveryRecord`) creates the missing record without re-transitioning. `POST /policies/:id/delivery/acknowledge-receipt` (`{ acknowledgedAt? }`, `policy.deliver`) — stamps `DeliveryRecord.receiptAcknowledgedAt` via a status-conditional `updateMany` (double-ack → 409) and **best-effort** advances `Policy DELIVERED → ACTIVE` (logged, never thrown — the stamp is the authoritative "client confirmed" record; `ACTIVE` is NOT a safety gate the way `DISCREPANCY` is, so best-effort is correct here per `policy-lifecycle.md`); a resume branch does just the `ACTIVE` advance when the stamp already committed. `deliveredAt` / `acknowledgedAt` via `parseHistoricalInstant` (past-only, offset-required on datetimes); 422 if `acknowledgedAt < deliveredAt`. `DeliveryRecord` is **NOT** a `WorkflowTransitionService` entity (no `status`). Audit: `CREATE` / `UPDATE DeliveryRecord` — `method` / `recipient` / `deliveredAt` (delivery is an accountability record; `recipient` — a name/email — is fine in the trail, same tier as the user emails already on `LOGIN` rows). `delivery` (+ `deliveryComplete` = `receiptAcknowledgedAt` set) folded into `PolicyView`. **Refactor (touches #20's code)**: `PolicyService.loadVisible` made **public** so `PolicyCheckingService` + `PolicyDeliveryService` share one visibility path; `PolicyCheckingService` drops its own `loadVisiblePolicy` / `canReachAnyPolicy` / `CustomerRepository` dep. `apps/web/.../opportunities/[id]/`: the "Policy" section gains a Sales/Placement delivery form + an "Acknowledge receipt" button + a read-only display. **`@code-reviewer` (mandatory — workflow transitions + a state machine + audit) → APPROVE WITH MINORS, no blockers, no MAJOR, no lex violation** (all six mandatory checks pass — no money; every `Policy` status move through the engine; no maker/checker needed — delivery is single-actor factual recording per `maker-checker-segregation.md` § "what does NOT trigger this rule"; no Highly Confidential logged; no PDPL-registry SLA). MINORs fixed: (1) a concurrent-delivery loser now always gets 409 (was 409-or-422 depending on the engine's rejection path); (2) the `deliveryComplete` doc comment corrected (it tracks the stamp, not `status === ACTIVE`). NITs: e2e now asserts exactly **5** `TRANSITION` audit rows across the #19→#21 chain (a direct `.status =` write anywhere would drop it). See README § Known gaps, Part C #21. | None — no migration, no seed change (`policy.deliver` already seeded) |
| 2026-09-02 | Part C #22 (Endorsement Management) landed — **new module** `apps/api/src/modules/endorsement/` (+ `repositories/endorsement.repository.ts`). `Endorsement` / `Cancellation` / `Refund` / `CommissionReversal` / `PolicySchedule` models + the `Refund_maker_checker_distinct` CHECK (`20260826091424`, `approvedByUserId <> raisedByUserId`) all already existed. **`Endorsement` IS a `WorkflowTransitionService` entity** (`status` moves ONLY through the engine, `REQUESTED → SUBMITTED_TO_INSURER → INSURER_CONFIRMED → FINANCIAL_ADJUSTMENT_CALCULATED → (REFUND_APPROVAL_PENDING →) APPLIED → CLIENT_NOTIFIED`); the child `Cancellation`/`Refund`/`CommissionReversal` have **no `status`** (their lifecycle is the parent endorsement's) — same "not-a-workflow-entity" shape as `PolicyChecking`/`DeliveryRecord`. `POST /policies/:id/endorsements` (`{ type ∈ POSITIVE\|NEGATIVE, changeType, premiumAmount (unsigned), effectiveFrom, targetCoverage? }`, `endorsement.create`/Placement) — Policy must be `ACTIVE` (422); `signedPremiumAdjustment` (pure) signs the fils-quantized amount by `type` (a NEGATIVE endorsement returns premium → negative `premiumAdjustment`). `POST /policies/:id/cancellation` (`{ reason, basis ∈ short_period\|pro_rata, effectiveFrom }`, `cancellation.create`/Placement) — a NEGATIVE endorsement with `changeType: cancellation`; `cancellationReturnPremium` (pure) computes the return: `pro_rata` = `issuedPremium × unexpiredDays / totalDays` (via `money.util.ts` `applyPercentage`), `short_period` = **drafted, unsourced** `SHORT_PERIOD_CLIENT_RETURN_PERCENT = '90'`% of the pro-rata figure (`/brain-gap` candidate — no market short-period scale table); the `Endorsement` + its `Cancellation` child are created in **one `$transaction`** (local exception, like `QuotationRepository.reviseChain`). `POST /endorsements/:id/advance` (`endorsement.create`) walks REQUESTED → SUBMITTED_TO_INSURER → INSURER_CONFIRMED. `POST /endorsements/:id/calculate-adjustment` (`{ premiumAmount? }` override for a non-cancellation, `endorsement.apply`) — from INSURER_CONFIRMED; for a NEGATIVE endorsement it creates the auto-tied `Refund` (maker side, `raisedByUserId`) **+** `CommissionReversal` (`|premiumAdjustment| × recommendedQuotation.commissionRatePercent`, `commissionReversalAmount` pure — **never a separate hand calc**, `policy-lifecycle.md`) in **one `$transaction`** (`P2002` → 409; `skip*` flags let a crash-recovery re-run fill only the missing row), then transitions to FINANCIAL_ADJUSTMENT_CALCULATED — and further to REFUND_APPROVAL_PENDING iff the refund is `≥` the **drafted, unsourced** `REFUND_APPROVAL_THRESHOLD_JOD = '5000.000'` (`/brain-gap` candidate — no CBJ / Finance approval-matrix source; matches #16's drafted-constant precedent). `POST /endorsements/:id/apply` (`endorsement.apply`) — FINANCIAL_ADJUSTMENT_CALCULATED → APPLIED; `PolicyRepository.versionScheduleForEndorsement` closes the open `PolicySchedule` at `effectiveFrom` and opens a **NEW** version (`sourceEndorsementId @unique`, coverage from `targetCoverage` or carried forward) in **one `$transaction`** — the prior version is never overwritten; a cancellation apply also **best-effort** transitions `Policy ACTIVE → CANCELLED` (logged, never thrown). 422 while REFUND_APPROVAL_PENDING ("apply via POST /refunds/:id/approve"). `POST /refunds/:id/approve` (**new? no** — `refund.approve`/Manager, already seeded) — **maker/checker** `assertDifferentActors(raisedByUserId, actor)` (403) + the CHECK; status-conditional `recordRefundApproval` `updateMany` (0 rows → 409); on success runs `applyCore` (REFUND_APPROVAL_PENDING → APPLIED + schedule version). `POST /endorsements/:id/notify-client` (`endorsement.apply`) — APPLIED → CLIENT_NOTIFIED (+ stamps `Cancellation.clientNotifiedAt`). `GET /policies/:id/endorsements` + `GET /endorsements/:id` (**new perm `endorsement.read`**/Sales,Placement,Finance,Manager,Exec). Migration `20260902160000_add_endorsement_management` (hand-authored + `migrate deploy`; `db` + `db-test`): `Endorsement.targetCoverage JSONB`, `effectiveFrom` / `submittedToInsurerAt` / `financialAdjustmentCalculatedAt` timestamps, `Endorsement_policyId_idx`. No new `Decimal` columns (all 4 money fields already classified). Audit snapshots **metadata not body** — money as fixed strings, `hasReason` boolean, never the `reason` text; a CREATE row per `Endorsement` / `Cancellation` / `Refund` / `CommissionReversal` / `PolicySchedule`. Repo bug fixed pre-review: `commissionRateFor` was passed `e.policy.id` (the policy id) where an `opportunityId` was expected — `ENDORSEMENT_INCLUDE` now also selects `policy.opportunityId`. `apps/web/.../opportunities/[id]/`: a new "Endorsements" section (`components/policy/EndorsementSection.tsx`) — Placement request/cancellation forms once the Policy is ACTIVE, per-endorsement one-action buttons (Advance / Calculate / Apply / Notify), Manager "Approve refund". **`@code-reviewer` (mandatory — workflow transitions + maker/checker + financial calc + a migration) → CHANGES REQUESTED → resolved.** BLOCKER: the above-threshold refund maker/checker gate was only the `REFUND_APPROVAL_PENDING` status (set by a *separate* best-effort transition) — a crash/concurrent `apply` between the two writes could reach `APPLIED` unapproved; fixed — `applyCore` refuses `refund && needsApproval && approvedByUserId == null` regardless of status. MAJOR: double cancellation → double `Refund`/`CommissionReversal` (Policy stays ACTIVE until APPLY); fixed — **new migration `20260902170000` partial UNIQUE `Endorsement_one_live_cancellation_per_policy` (`WHERE changeType='cancellation' AND status<>'CLIENT_NOTIFIED'`)** + pre-check + `P2002`→409. MINORs: `effectiveFrom` bounded to `[openSchedule.effectiveFrom, expiry]`; the cancellation `Policy→CANCELLED` walk is a hard 409 on an unappliable outcome (not a swallowed warn); `SHORT_PERIOD_RETAINED_PERCENT`→`SHORT_PERIOD_CLIENT_RETURN_PERCENT`; `refund.approve` widened `[MANAGER, FINANCE]`; a materially-different late `premiumAmount` override → 422; the folded `effectiveFrom` ALTER carries `IF NOT EXISTS`. **`/brain-gap` filed + pushed** (`ibms-brain` `7b60bbd`): `policy-lifecycle.md` Cancellation row gains 5 sub-points (drafted constants; APPLY must re-check approval structurally; one-live-cancellation is a DB constraint; the policy-cancel walk must throw; `effectiveFrom` bounds). See README § Known gaps, Part C #22. | Run `npm run db:migrate:deploy` (both `20260902160000` + `20260902170000`; checksum drift blocks `migrate:dev`), then `npm run db:seed` (or `db:test:seed`) for the new `endorsement.read` permission + the widened `refund.approve` grant |
| 2026-09-02 | Part C #23 (Claim Notification) landed — opens Domain C. **new module** `apps/api/src/modules/claim/` (+ `repositories/claim.repository.ts`). **No migration** — the `Claim` / `ClaimStatusHistory` / `ThirdPartyClaimant` / `Settlement` / `Adjuster` / `ClaimFollowUpAlert` models, the `ClaimStatus` enum, the `WORKFLOW_TRANSITIONS.Claim` map (`NOTIFIED → REGISTERED → … → CLOSED`), all four claim `Decimal` money fields (already in `MONEY_DECIMAL_FIELDS`), and `ENCRYPTED_FIELDS.ThirdPartyClaimant = ['contactDetailsEnc']` all pre-existed. **`Claim` IS a `WorkflowTransitionService` entity**, but #23 only does the **initial creation** at the schema `@default(NOTIFIED)` — NOT via the engine (same pattern as `Policy` at `PLACEMENT_CONFIRMED`, `Opportunity` at `NEEDS_CONFIRMED`, `Endorsement` at `REQUESTED`); `NOTIFIED → REGISTERED` is Process 24. **No maker/checker at notification** (single-actor Sales/Claims work per `maker-checker-segregation.md` § "what does NOT trigger this rule"; the second approver is at settlement, Process 28). `POST /claims` (`{ policyId, lossDate, causeOfLoss, lossLocation?, estimatedLoss, isThirdPartyInvolved?, thirdParty?: { fullName?, contactDetails?, subrogationRecoveryFlag? } }`, `claim.notify`/Sales,Claims — already seeded) — `ClaimRepository.createNotification` writes the `Claim` + its opening `ClaimStatusHistory` row (`null → NOTIFIED`, `changedByUserId` = notifier — there is no `Claim.notifiedByUserId` scalar) + (for a third-party loss) the one `ThirdPartyClaimant` (`contactDetails` field-level encrypted via `encryptEntityFields`, id pre-generated for the `ENCRYPTION_KEY_USED` audit) in **one `$transaction`** (local exception). `lossDate` via `parseHistoricalInstant` (past-only); `estimatedLoss` `quantizeMoney`'d + must be `> 0`. **Coverage in force AT THE LOSS DATE** — `resolveCoverageAtLossDate` (pure & **total**) finds the `PolicySchedule` version whose window `[effectiveFrom, effectiveTo)` contains the loss date (the set of versions IS the materialised #22 endorsement history — resolves to the version that actually applied, NOT the current open schedule); `Policy.expiryDate` is an **independent upper bound** (nothing closes the open row at expiry). Unresolvable → hard **422** on notify (`not_issued` / `before_inception` / `after_cover_ended` / `coverage_gap`), but a non-throwing `coverage: null` + `coverageResolvedAtLossDate: false` on a later read (a #22 forward cancellation can strand a validly-notified mid-term loss). `isLargeClaim` derived from **drafted, unsourced** `CLAIM_LARGE_THRESHOLD_JOD = '25000.000'` as an advisory NOTIFICATION-TIME SNAPSHOT — #28's second-approver gate must **re-derive from live data** (the #16 generalisation). `GET /claims?policyId=\|customerId=` + `/:id` (**new perm `claim.read`**/Sales,Claims,Manager,Exec) — **every read is audit-logged** (`action: READ`, `isSensitiveDataAccess` when a claim is returned — ids/counts only, never `causeOfLoss` / `lossLocation` / the claimant name; mirrors `CrmService.get360View`); list capped at `CLAIM_LIST_LIMIT = 200`. Audit snapshots **metadata not body** (`hasLossLocation` bool + resolved schedule id/window, never the text; third-party = `hasFullName` / `hasContactDetails` bools + `subrogationRecoveryFlag`). `CLAIMS_OFFICER` added to `policy.read` (seed, additive — a claims officer needs the underlying policy context) + new shared `CLAIM_CROSS_OWNER_ROLES` (Claims/Manager/Exec cross-book; Sales own-customer only). `apps/web/.../opportunities/[id]/`: a "Claims" block in the "Policy" section (Sales/Claims notify form once the policy is issued, read-only claim list with the resolved coverage window). **`@code-reviewer` (mandatory — workflow entity + validation gate + Highly Confidential data + a financial figure) → CHANGES REQUESTED → resolved.** MAJOR: `list` / `get` served Highly Confidential claim content with no access trace — fixed by the sensitive-data `READ` audit above (+ 2 unit tests + an e2e assertion). MINORs: (1) `/brain-gap` filed + pushed (not just asserted in a comment); (2) `findManyBy*` bounded at `CLAIM_LIST_LIMIT = 200`; (3) new `coverage_gap` reason so a hole between schedule versions isn't mislabelled "cover ended". NITs: `@IsString()` before `@Matches` on `estimatedLoss`; dropped unused `status` / `inceptionDate` from `CLAIM_INCLUDE`. **`/brain-gap` filed + pushed** (`ibms-brain` `67582ee`): `claims-lifecycle.md` — the coverage-at-loss-date row (schedule-window resolution; `expiryDate` a separate bound; reject-on-notify-not-on-read) + the large-claim row (drafted threshold; re-derive the #28 gate). See README § Known gaps, Part C #23. | `npm run db:seed` (or `db:test:seed`) — new `claim.read` permission (146 total) + `policy.read` grant widened to `CLAIMS_OFFICER`. No migration. |
| 2026-09-02 | Part C #24 (Claim Registration with Insurer) landed — extends the `claim` module. **Migration `20260902180000` adds ONLY `@@unique([claimId, toStatus])` on `ClaimStatusHistory`** (a `@code-reviewer` MINOR — `recordRegistration` guards the `REGISTERED` history-row insert with a `count()`-then-`if`-then-`create`, the `race-safe-invariants.md` check-then-act shape; today transitively backstopped by the sibling `Adjuster.claimId @unique` in the same `$transaction`, now structural. Valid over the whole lifecycle because `WORKFLOW_TRANSITIONS.Claim` is an acyclic DAG — a status is entered once. The `count()` pre-check stays so a crash-recovery resume finds the row present and skips rather than hitting the constraint). No new `Decimal` column, no seed change (`Adjuster` model, `Claim.insurerClaimReference` / `Claim.claimNumber @unique`, `ClaimStatus.REGISTERED`, the transition map, `claim.register` perm `[CLAIMS_OFFICER]` all pre-existed). `POST /claims/:id/registration` (`{ insurerClaimReference (required), claimNumber?, adjuster: { name, firm? } }`, `claim.register`/**Claims Officer**) — drives `Claim NOTIFIED → REGISTERED` through `WorkflowTransitionService.transition` with `{ insurerClaimReference, claimNumber? }` as the transition `data` (status flip + scalar write **one atomic engine-audited write**; a `claimNumber` collision → `P2002` → 409); then `ClaimRepository.recordRegistration` writes the `REGISTERED` `ClaimStatusHistory` row (idempotent) + the one loss `Adjuster` (`claimId @unique`) in **one `$transaction`** (local exception). **The first real engine transition on a `Claim`** (#23 only created it at `@default`); the e2e asserts exactly **one** `TRANSITION` audit row across the register + every re-call. **No maker/checker** (single-actor Claims work; second approver is at settlement #28). Idempotent / race-safe: a byte-identical re-call (insurer ref + adjuster name/firm + claim number all equal) is a `200` no-op; **any** field change is a `409` ("recorded once — a correction is not yet supported"); a concurrent register that lost the `NOTIFIED → REGISTERED` race (engine 0-rows `ConflictException` or "already in status REGISTERED") is reloaded and handled as already-registered; a crash-recovery re-entry (status `REGISTERED`, no `Adjuster`) does only the artefact write (no re-transition, no `UPDATE Claim` scalar-audit row). Audit: `CREATE Adjuster` (adjuster `name` + `firm` — a professional loss-assessment provider, not the claimant; same tier as the #21 delivery `recipient`) + `UPDATE Claim` (registration scalars, only on the transitioning call). `insurerClaimReference` + `Adjuster` folded into `ClaimView`. `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-`NOTIFIED`-claim "Register with the insurer" form (`canRegister={isClaims}`). **`@code-reviewer` (mandatory — workflow transition + Highly Confidential entity) → APPROVE WITH MINORS, no blockers, no MAJOR, no lex violation.** Fixed: (1) `@@unique([claimId, toStatus])` migration (invariant now structural); (2) idempotency key compares **every** registration field (a changed `firm` / `claimNumber` → 409, not a silent no-op); (3) comment on the concurrent-different-payload winner/loser outcome; (4) module docstring → "Process 23-24". Noted (no code change): the transition-then-artefacts seam (accepted #19/#21 pattern); Process 27's follow-up clock should key off the `REGISTERED` `ClaimStatusHistory.changedAt`. NIT: `recordRegistration` returns `Adjuster` directly. See README § Known gaps, Part C #24. | Run `npm run db:migrate:deploy` (migration `20260902180000`; checksum drift blocks `migrate:dev`, not `deploy`). No seed change. |
| 2026-09-02 | Part C #25 (Claim Documentation) landed — extends the `claim` module. **No migration of its own, no seed change** — the `ClaimDocument` / `Document` models, the `DocumentCategory` / `DataClassification` enums and the `claim.document` perm (`[CLAIMS_OFFICER]`) all pre-existed; migration `20260902180000` (the #24 `@@unique([claimId, toStatus])`) also backstops the #25 best-effort history-row write. `POST /claims/:id/documents` (`{ documents: [{ docType ∈ claim_form\|police_report\|medical_report\|photo\|invoice\|repair_estimate\|expert_report\|correspondence, classification, fileName, storageRef }] }`, `claim.document`/**Claims Officer**) — valid from `REGISTERED` onward (`NOTIFIED` → 422 "register it first"); `ClaimRepository.attachDocuments` writes one `Document` per file (`category: CLAIM`, `storageRef` pointer — never bytes) + one `ClaimDocument` join carrying `docType` in **one `$transaction`** (local exception). A `medical_report` MUST be `HIGHLY_CONFIDENTIAL` (`claims-lifecycle.md` — health data is classification-driven from first contact; 422 else). **The first attach best-effort advances `Claim REGISTERED → DOCUMENTATION_IN_PROGRESS`** via the engine + `ClaimRepository.recordStatusHistory` (idempotent `count()` pre-check + the `@@unique` backstop) — best-effort because `DOCUMENTATION_IN_PROGRESS` is a forward-progress marker, NOT a #20-style safety gate, so a failed advance is logged + retried on the next attach, never thrown; a later attach does not re-transition. The e2e asserts every `Claim` status move is an engine `TRANSITION` row. **The mandatory-document checklist per claim type** — `classifyInsuranceLine` (pure) maps the free-text `Policy.insuranceLine` to a broad line family (`property`/`motor`/`medical`/`liability`/`marine`/`other`) by keyword; `mandatoryDocTypesFor` maps each family (+ `police_report` for any third-party loss, + `claim_form` always) to a required `docType` set; `buildDocumentChecklist` derives `documentationComplete` + `missingMandatoryDocuments`. **Classifier + matrix are `ibms-app` drafted, unsourced** (Part 3.7 lists doc *types* only, no per-line matrix; no line taxonomy) — same status as `CLAIM_LARGE_THRESHOLD_JOD`/#16's 10%/2pp/#22's constants. `ClaimView` gains `documents` (no `storageRef`), `documentChecklist`, `documentationComplete`, `missingMandatoryDocuments`. Audit `CREATE ClaimDocument` = ids/`docType`/`category`/`classification` only, **never `fileName`/`storageRef`** (a claim doc filename can name an injured person — #18-19's `policyDocumentAuditSnapshot` rule). A claim `Document` carries no `policyId` (the `ClaimDocument` join is the canonical link). No maker/checker (single-actor Claims work). `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim checklist + single-file attach form (`canDocument={isClaims}`). **`@code-reviewer` (mandatory — workflow transition + Highly Confidential data) → APPROVE WITH MINORS, no blockers, no MAJOR, no lex violation** (all six mandatory lex checks pass). MINORs fixed: (1) the best-effort `REGISTERED → DOCUMENTATION_IN_PROGRESS` advance keys its resume off the **domain history row being absent** (`!statusHistory.some(h => h.toStatus === 'DOCUMENTATION_IN_PROGRESS')`), not `status === 'REGISTERED'` — a transition that committed but whose separate history write then threw is backfilled on the next attach (only the transition itself is skipped once `status` is already `DOCUMENTATION_IN_PROGRESS`); (2) `classifyInsuranceLine`'s `property` branch `\b(propert|…)\b` could never match the bare word "property" (`\b` between `t` and `y` fails) → `\bpropert\w*`; (3) `attachDocuments` now emits the `READ` sensitive-data-access audit row (ids/counts only) that `get`/`list` emit — its response echoes every file's `fileName` + `causeOfLoss` + third-party name. NITs: (1) the best-effort warn now says "will retry on the next attach"; (2) `CLAIM_INCLUDE.documents` gets a secondary `{ id: 'asc' }` sort (one `$transaction` → all `Document`s share `createdAt`). +5 spec tests for the three MINORs; api unit 980 (72 files); `claim.e2e-spec.ts` 6/6 isolated, full e2e 118/119 (the 1 fail = the known `enrollMfa` MFA-timing flake, this run on `cross-sell.e2e-spec.ts`). **`/brain-gap` filed + pushed** (`ibms-brain` `0dfa33f`): `claims-lifecycle.md` gains a Claim Documentation row + a medical-data sub-point. | Run `npm run db:migrate:deploy` if not already applied for #24 (migration `20260902180000`). No seed change. |

Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
