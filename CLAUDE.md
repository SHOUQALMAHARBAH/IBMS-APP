@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-15 | **Hint text and accessible names — the last two places English was still hiding, and one of them no test could see.** Three clusters, all of them copy the earlier bilingual passes never claimed. **(1) Placeholders.** A previous row put the figure at thirteen; that was an undercount **from the scan, not the tree** — the regex was anchored to a letter (`placeholder="[A-Za-z]`), so it skipped every numeric example. There are **27**. Thirteen carried English prose and now read from the dictionary; **fourteen are format examples and are deliberately left alone** (`"17500.000"`, `"+962-7-..."`, a UUID sample) because `lib/i18n/format.ts` pins Arabic to `'ar'`, never `'ar-JO'`, **specifically so numerals stay Western** — verified against Node's ICU, where `(17500).toLocaleString('ar', …)` returns `17,500.000`, byte-identical to `en-GB`. An AR key there would be the same string as its EN twin. **(2) Thirty template-literal `aria-label`s were English** — `` `Confirm access for ${name}` ``, `` `Document ${i+1} file name` `` — across 18 files, now interpolating through `translate()`'s existing `params` argument. **axe cannot catch these**: `color-contrast` and the label rules check that an accessible name *exists*, not what language it is in, so all 66 `@a11y` specs passed over them. Neither could the functional suite, which asserts visible text. **(3) Four ageing-bucket labels** on `/financial-report` (`"1–30 days"`…) rendered English between two siblings that were already `t()`-driven. **Reuse beat invention where the concept was already owned**: three placeholder sites took existing keys rather than new ones — including `opMitigationActionLabel`, which `operational-pi-risk` **already used as a placeholder 200 lines above** the site that hard-coded the same English. Two aria-labels matched existing copy exactly (`complaintsTextAria`, `customersViewProfileAria`) and were **deliberately NOT reused**: generic wording shared by two domains is not shared ownership, and the DSR and prospects screens got their own keys. **Every EN value is byte-identical to the English it replaced** — `screening-matches.spec.ts` locates a control by `getByLabel("Review reason for Ahmad Khalid Yousef Al Hashimi")`, and an accessible name is a locator as much as it is copy. **Verification**: typecheck 6/6, lint 3/3, build 3/3; dictionary tests 13/13; **`npm run test:a11y` 66/66, exit 0** — held; web e2e **288/288, exit 0**. **2,749 keys per language** (+44), exact AR/EN parity, 0 collisions, and **159 unreferenced before and after** — no orphans introduced. | **An `aria-label` and the visible label of the same control must be in the same language.** A screen reader and the screen disagreeing is the defect this closed, and **no gate in this repo can detect it** — axe checks a name exists, Playwright asserts visible text. It is a review question, not a test one. **Numeric placeholders stay untranslated on purpose** — see the `'ar'`-not-`'ar-JO'` note in `lib/i18n/format.ts`; do not "finish the job" by adding AR keys that duplicate their EN twins. **Keep an accessible name byte-identical when you move it into the dictionary** unless you mean to update the spec that locates by it. **One category remains and is NOT started — do not self-select it**: English text interleaved with interpolations, which a bare-JSX-text scan structurally cannot see because the text node contains `{`. A filtered pass finds **29 sites across 14 files** — unit words (`JOD` on four dashboards, `minutes` on `/settings/security`, `days`), pluralisation fragments (`{n} invoice(s) across {m} customer(s).`, `entr{y\|ies}`, `line{s}`) and a handful of full sentences. Pluralisation is the real work there: Arabic has more plural forms than the `s`/`no-s` ternaries these sites use, so it needs a plural rule, not a key. |
| 2026-09-15 | **The a11y suite runs for the first time in a while, and 24 of its 66 specs were failing — none of them for the reason we thought.** The working theory was `--surface-page`. That token pairs with `--ink-primary` at **12.33:1**; it appears in six of the nine failing colour pairs, always as the *background*. Read out of the 24 `error-context.md` artefacts axe wrote — **51 violating nodes, 9 pairs, 3 causes**. **(A) `--ink-muted` `#828c9a` was simply too light**, failing on every surface it lands on — 3.41 on a white card, and **a card is as light as a background gets, so there was nothing to darken but the ink**. Now `#656d7b` (5.22 / 4.82 / 4.61 on card / page / sunken); dark `#7c8898` -> `#8b95a4`, which also clears a dark-theme failure at 4.19 that no test covers. **(B) 132 sites across 83 files muted text with `opacity: .6/.65`** — which composites toward the ground, so the rendered colour is one no token declares and axe measures the blend (`#787f89`, 3.73). All 132 now read `color: var(--ink-secondary)` (6.09). **(C) a hard-coded `#d33`** in two files, 4.22 -> `var(--danger-ink)`; in dark that literal was **3.71 / 3.30**, so the token swap fixed a second failure the suite never tested. Two edges folded in: the seven `rgba(128,128,128,0.05)` panels — which were not tokens at all, and are the `#eef0f4`/`#f0f0f0` backgrounds in the axe output — now read `--surface-sunken`; and **the 24th spec was never a contrast failure**: `retention-cases.spec.ts`'s `@a11y` copy still asserted the raw enum `lapse_risk` after the bilingual pass relabelled it, while its non-`@a11y` sibling had been corrected — `npm run e2e` is `--grep-invert @a11y`, so nothing ran it. **Three options were priced before anything was touched** (darken the ink / darken `--ink-primary` to near-black and keep the fades / raise the fade floor to 0.8); all three reach 4.5:1, and the one chosen is the only one that leaves every heading and body line at the sampled comp value — captions move −13.4 ΔL*, body text 0.0. **Verification**: **`npm run test:a11y` 66/66, exit 0, 2.1 min** (from 24 failed / 42 passed), re-run after a final rebuild so the number is against the exact reviewed tree; web e2e **288/288, exit 0, 3.3 min**; typecheck 6/6, lint 3/3, build 3/3; unit api 2927/2927 · web 19/19 · db 15/15. Before/after captured from real builds either side of the change (BEFORE from a stashed tree at `df6375a`): sampled glyph colours confirm `#787e88 -> #525e6f` on captions and `#828c9a -> #656d7b` on muted card text, a 0.23% pixel delta confined to the caption regions. | **`npm run test:a11y` is a SEPARATE gate — `npm run e2e` is `--grep-invert @a11y`.** Run both and report both counts; "web e2e green" is not a statement about accessibility, and the `retention-cases` regression above is what a suite left unrun for a few commits actually costs. **Never mute text with `opacity`** — it is invisible to the token system and to `tsc`, and it is what produced 20 of the 51 nodes; reach for `--ink-secondary` or `--ink-muted`. `opacity` on a **disabled control** stays fine (axe exempts those, and `components/ui/Button.tsx` still uses `0.55`). **`--ink-muted` is the one token that is NOT a literal comp sample** — the header comment in `globals.css` says so and why; re-sample the rest, but do not restore `#828c9a`. **api e2e was deliberately skipped and this is the reason**: every changed path is under `apps/web/` — 69 pages, 24 components/styles, `globals.css`, one e2e spec — with no schema, DTO or api file touched. **Dark theme is computed, never measured**: Playwright runs light, so every dark figure in this row is arithmetic. Two dark pairs still compute under 4.5 and **neither renders** — `--ink-muted` on `--surface-hover` (4.34; that token has **zero consumers**) and `--nav-ink-muted` on `--nav-item-active` (2.91; muted nav ink only ever sits on the nav ground, and the active link sets `opacity: 1`). Adding a dark-theme axe pass is real, unstarted work — do not self-select it. **178 `opacity: .7/.75/.8` sites survive** and pass only because they sit on `--ink-primary` (4.97 / 5.70 / 6.66); the same markup on `--ink-secondary` would be 3.14. |
| 2026-09-14 | **The 403 branch stops being English, the sign-in screen stops being Arabic-by-default-but-English-on-screen, and the merged dictionary finally gets a test.** Part F's bilingual UI closes at **93 of 93** screens — the 89 under `app/(app)/` plus the four `(auth)` screens the row below claimed were done and were not — against **2,705 keys per language across 16 dictionaries in exact AR/EN parity** (+295 keys, +`auth.ts`), and **536 new `t()` call sites**. The 403 permission-denied branch is wired at **86 `t('*NoPermission*')` sites across 74 files, with zero `*NoPermission` keys left unreferenced**: 55 hard-coded English literals replaced, **plus seven pages that had no 403 branch at all**. `cross-border-transfers`, `data-sharing-approvals`, `dpia-screenings`, `dpo-workspace`, `privacy-notices`, `retention-disposal` and `ropa-entries` passed `err.message` straight through, so a denial surfaced the API's English sentence in both languages — their keys were written for them last pass and never called. **The `(auth)` screens were worse than untranslated.** They render before `/auth/me`, so `LanguageProvider` falls back to the schema default — **Arabic** — and being hard-coded English meant an Arabic-preferring user met an English sign-in form with no way to tell: `login.spec.ts` asserted `"Sign in"` and passed because that string was the only thing the page could produce. A new test clears `ibms.languagePreference`, asserts the Arabic heading **and** `dir="rtl"`, and asserts `"Sign in"` -> `toHaveCount(0)`; the other three specs now pin EN in a `beforeEach`, the way the authenticated specs do through their `/auth/me` mock. **`IdleLockOverlay` could not be translated where it stood**: `AuthProvider` rendered it, which put it OUTSIDE `LanguageProvider`, so `useLanguage()` threw on every authenticated page — and the providers cannot simply be swapped, because `LanguageProvider` reads `user.languagePreference` from `useAuth()`. A new `IdleLockOverlayHost` mounts it as a child of both. **The collision the row below predicted is fixed, and is now a test rather than a convention.** Four `commChannel*` keys were declared in both `rfq.ts` and `customer-service.ts`; the merge spreads customer-service later, so `rfqs/[id]` rendered "Phone call"/"Customer portal" instead of its own "Call"/"Portal". The RFQ set is renamed `rfqComm*`, and `translations.test.ts` gained three checks the prefix convention only ever promised — **every dictionary file on disk is covered, every key is declared in exactly one file, every file is at exact AR/EN parity** — the first of which exists because adding `auth.ts` left the other two silently under-covering. **Seven e2e specs were asserting the defect**, two of them *titled* "sees a friendly message" while asserting the unfriendly raw API sentence; all seven now assert the translated fragment **and** `toHaveCount(0)` on the raw message, without which the old behaviour satisfies the new assertion. **Verification**: typecheck 6/6, lint 3/3, unit api 2927/2927 · web **19/19** (from 16 — the three new merged-dictionary tests) · db 15/15; **web e2e 288/288 in ONE uninterrupted run, exit 0, 2.4 min**; api e2e **486/486 across all 74 files, 0 cached, exit 0** — run before the seven-page wiring and still valid, because every path in this commit is under `apps/web/` apart from this file and `README.md`. | **Copy for a pre-authentication screen goes in `auth.ts`, and the default there is ARABIC** — there is no `user.languagePreference` to read yet, so a Playwright spec for those four screens must pin the language it means to assert. **The `@a11y` suite is a SEPARATE gate: `npm run e2e` is `--grep-invert @a11y`.** Run `npm run test:a11y` and report its count on its own — "web e2e green" is not a statement about accessibility. **24 of 66 `@a11y` specs currently fail on colour contrast**, attributed here to `--surface-page` from `ab263fb` — **that attribution was wrong**, and the row above records what the failures actually were; the "unrelated to this diff" part held. Fixed 2026-09-15. **Known-open when this row was written, closed 2026-09-15**: the "thirteen `placeholder`" figure here was an undercount from the scan itself — the regex was anchored to a letter, so it skipped every numeric example. There are **27** placeholder literals; 13 carried prose and are now translated, 14 are format examples left untranslated by design. Still open: and `policy.ts` still carries **98** unreferenced keys, down from 161, of **159** across all sixteen dictionaries — pre-existing debt from the `pold*` move, to fold in deliberately rather than chase. `atNoPermission` and `commonNoPermissionGeneric` were deleted as genuinely dead — the first superseded by `atNoPermissionFor`, which interpolates the permission code instead of hard-coding `audit-log.read`. **A verification trap worth one wasted run**: `npm run e2e \| tail -40` in a background shell reported **exit code 0 while turbo had failed** — the pipe swallows the status. Redirect to a file and echo `$?`, or `set -o pipefail`. |
| 2026-09-14 | **Sixteen demo accounts nobody could sign into, and the reason the seed said `0 failures` anyway.** Logging in as the Manager demo account landed on an MFA *challenge* — a six-digit prompt — rather than the enrolment screen, on an account that had never been enrolled by a human. **It is not a login bug**: `AuthService.login` branches on `user.mfaEnabled`, never on role policy, and `ALWAYS_MFA_ROLES` governs only the trusted-device skip, so the routing is right and `auth.e2e-spec`'s "blocks MFA-gated routes until enrolled, then requires a code at login" proves it. The accounts were simply left enrolled: `enrolMfa` gives every actor a TOTP secret generated in-process and shown to nobody, and `releaseActorsForHumanLogin` — which exists to switch that back off — **never ran against this data at all**. The seed finished 22:43 on 2026-09-13; the release was committed at 23:50, 67 minutes later. The report is the proof it would otherwise have run: `errors: []`, `actor: 16`, both offices present in `summaries`, and an office only lands there if `seedOrganization` **returned** — past the point the call now sits. All 14 stale credentials carry `lastUsedAt == createdAt` at 19:39–19:41Z: written by the seeder, never touched again. **So the honest count is zero of sixteen released, not one** — `demo.sales@office-b` was cleared by hand at 19:51Z while the fix was being written, which is the only thing that made it look like a loop that stuck once. Remedied by a scoped transaction over exactly the 14 locked rows (**SELECT 14 / DELETE 14 / UPDATE 14**, no re-seed), deliberately excluding `demo.sales@office-a`, whose credential was created 2026-09-14 10:37 and last used 14:44 — a real human pairing, separable from the seeder's because the seeder's `lastUsedAt` never moved. **Three holes in the script are what let a silent lockout ship, and all three are closed.** The release ran as the last statement of `seedOrganization`'s happy path — an actor is locked from the moment `enrolMfa` returns, so any throw in the ~70 lines between strands eight real accounts, and the caller catches office-level failures and carries on, logging one line about an office and never "nobody can sign in"; it now runs after the whole seed. It keyed on the in-memory `Actors` map, which holds only actors whose entire create-login-enrol attempt succeeded — and `enrolMfa` is **two** HTTP calls, so an actor that enrolled and then failed to verify owns a credential the map never recorded, while the "could not provision" guard throws and the other seven go unreleased too; it now finds accounts by **email**, via a single `actorEmail()` shared with `ensureActor` so a rename cannot leave the release hunting for names nothing answers to. And nothing checked the end state; a post-condition now re-reads the database and **fails the run, naming accounts**, if any demo actor is still `mfaEnabled`, owns a credential, or owes a password change. **Verification**: typecheck 6/6 with `tsc --listFiles` confirming the script is genuinely in the api program; api e2e **35/35** across `auth.e2e-spec` + `auth-mfa-session`; all 16 accounts probed live — 15 return a session and `MFA_ENROLLMENT_REQUIRED` on every real screen, `demo.sales@office-a` correctly challenges; and a full first-login round trip on an always-MFA role (session → 403 `MFA_ENROLLMENT_REQUIRED` → enrol+verify → **200 on a real screen** → next login challenges), restored afterwards. | **A demo account is handed back with MFA OFF and the run now fails if it is not** — sign in with the password alone, then pair an authenticator at **Settings → Security**; every other screen 403s `MFA_ENROLLMENT_REQUIRED` until you do, which is §4.3.2 working. **Never key a cleanup on the in-memory record of what succeeded** — it is exactly the set that excludes the half-finished cases the cleanup exists for. **A run that reports `0 failures` is not evidence the end state is usable**; that is what the new post-condition is for, and its absence is the whole of this defect. **The two new functions were typechecked but never EXECUTED** — the script has no dry-run path and the only way to run them is a full seed, which would reset `demo.sales@office-a` and wipe a real paired authenticator; run a minimum-scale seed against **db-test** if you want them exercised before trusting them. **`scripts/` is outside the api lint glob** (`{src,apps,libs,test}/**/*.ts`), so `npm run lint` never sees this file: it carries **210 pre-existing prettier errors at HEAD** and `--fix` reformats the whole thing (759 insertions), which is why this change leaves the formatting alone — lint it directly with `npx eslint scripts/seed-demo.script.ts` and compare the rule mix, not the count. |
| 2026-09-14 | **The whole app speaks Arabic — 89 screens, one dictionary per domain — and the four-state rule stops being asserted in English.** Every page under `app/(app)/` is now bilingual: **89 of 89** (the four `(auth)` pages were claimed done here and were not — closed in the 403/auth row at the top of this table), against **2,410 keys per language** in exact AR/EN parity. Copy lives in **nine new namespaced dictionaries** under `lib/i18n/translations/` covering **seven page clusters** — customer service (6 pages), PDPL (9), finance (6), compliance (11), dashboards (14), operations (16), detail pages (16) — with the remaining 11 pages served by the `rfq`/`customers`/`leads`/`policy` dictionaries that already existed. Arabic is **written, not translated from the English**; where no sourced term exists it is marked in place rather than guessed (`complaints.ts` carries a `TODO(arabic-terminology)` on the Insurance Dispute Resolution Committee's name, the only place it appears). **Enum labels follow the policy-status precedent**: a new `lib/lead/lead-status.ts` replaces `leads/[id]`'s own `Record<string, string>`, which listed four of the five statuses — `DISQUALIFIED` rendered as the raw token, in Arabic too, the identical defect the policy detail page had; 16 `Record<Enum, TranslationKey>` maps now cover the enums a user reads, so a new member is a compile error. **The most useful thing the pass found is what the existing tests were not testing.** `rtl-layout.spec.ts` asserted the **English** column headers `Source`/`Completed` on a page it had itself switched to Arabic — it passed only because the screen was English-only, which is the very thing that test exists to deny; `four-state-screenshots.spec.ts` proved its AR loading and empty captures with English `"Loading…"`/`"No prospects yet."`; and three specs asserted raw enum tokens (`ESCALATED`, `post_issuance`, `lapse_risk`) as if they were labels. All five are corrected to the Arabic or the label. **A Playwright trap came out of that**: `getByRole`'s `name` match is **case-insensitive**, so a bare `"ESCALATED"` still matches the new `"Escalated"` label and proves nothing — `complaints.spec.ts` now asserts the label *and* `{ name: "ESCALATED", exact: true }` → `toHaveCount(0)`. **One state is deliberately NOT closed and is recorded, not hidden**: the 403 permission-denied branch is still hard-coded English at **54 sites across 48 pages**, even though this pass added the AR+EN keys for exactly those messages — all **54 `*NoPermission` keys are defined and unreferenced** (`tmNoPermission`, `dpolNoPermission`, `consNoPermission`, … plus `commonNoPermissionGeneric`). **Verification**: typecheck clean, lint clean, web unit 16/16; **web e2e 287/287 in ONE uninterrupted run, exit 0, 3.2 min**; api e2e **486/486 across all 74 files, 0 cached, exit 0** — unchanged, as a web-only change should leave them. | **Copy goes in the domain dictionary, never in the page.** A page-local `Record<string, string>` of labels is the defect `lib/policy/policy-status.ts` and now `lib/lead/lead-status.ts` exist to prevent — `tsc` cannot see it, because both sides are `string`. **The next commit wires the 403 branch**: the keys are already written and named per screen, so it is a substitution at 54 sites plus the ~25 stragglers this pass missed — `audit-trail` is the worst (five visible `<label>`s in English while the `aria-label` beside each is Arabic, so a screen reader and the screen disagree), then `Record a new …` headings on `employees`/`vendors`/`bcp-dr-plans`/`information-assets`/`documents` and `Timers`/`Workflow`/`Entity` on `sla-dashboard`. **`customer-service.ts` re-declares four `commChannel*` keys that `rfq.ts` already owns** and spreads later, so `rfqs/[id]` silently renders "Phone call"/"Customer portal" instead of its own "Call"/"Portal" — the merge in `translations.ts` claims prefixes make collisions impossible and these are the first four; rename the customer-service set when the 403 work lands. **Do not treat `policy.ts`'s 161 unreferenced keys as this pass's doing** — they were already orphaned at HEAD, because the policy screens read `pold*` from `detail-pages.ts`; that is pre-existing debt to fold in deliberately, not a regression to chase. |
Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). This paragraph predates
Domain B #12–22, Domains C–H and Parts D–F, all of which have since been built —
**root `README.md` § Scope status is the authority, not this summary.** Part F's
bilingual UI is complete: all 93 screens — the 89 under `app/(app)/` and the four
`(auth)` screens that render before sign-in — read from
`apps/web/lib/i18n/translations/`, including the 403 permission-denied branch
(see § What's New). Part G's final verification
checklist has not been run as a formal, evidence-attached gate. See § Known gaps for
each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web

# Demo data for two Organizations, through the real HTTP API — DEV DB only,
# never .env.test. Sign in with the password, then pair an authenticator at
# Settings -> Security. See apps/api/scripts/README-SEED-DEMO.md.
npm run seed:demo -w api
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
