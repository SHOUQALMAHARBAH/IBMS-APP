@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-03 | Part C #33 (Client Accounting) landed — extends the `finance` module. **No migration, no seed change** — `client-accounting.read` (`[FINANCE_COLLECTIONS_OFFICER, BRANCH_DEPARTMENT_MANAGER, EXECUTIVE_MANAGEMENT, EXTERNAL_AUDITOR]`; its seeded description is literally "View the client accounts-receivable/ageing report") pre-existed. New `ClientAccountingService` + `@Controller('client-accounting')` in `FinanceModule` (no `AuthModule` — the global `PermissionsGuard` covers it, same as #30/#32), `InvoiceRepository.loadOutstandingReceivables`, and the pure `buildReceivablesAgeing` / `daysOverdue` / `ageingBucketFor` / `AR_AGEING_BUCKET_KEYS` / `AR_AGEING_INVOICE_LIMIT` in `finance.config.ts`. **`GET /client-accounting/ageing?customerId=&asOf=`** (`client-accounting.read`) returns the accounts-receivable / ageing report — **one row per customer with an outstanding balance**, each split into `current` / `d1_30` / `d31_60` / `d61_90` / `d90_plus` buckets, plus a `totals` row pooling every outstanding invoice in scope. **Computed on the fly — no stored aggregate table** (the #30 Claims Analytics shape; the unscoped `GET /invoices` 400 message already pointed here). **"Outstanding" is structural — an `Invoice` with no collection `Receipt`** (#32 records exactly one per invoice for the full total, so a receipt means paid in full; partial payments are a deferred #32 refinement; an `EXCEPTION_RAISED` / #32-crash-seam invoice with no receipt still reads as outstanding). **`asOf` is the ageing reference date** — a bare `YYYY-MM-DD`, today or earlier (a future `asOf` → **422**, via `parseHistoricalInstant`), default today; it is **point-in-time correct for the outstanding set with no history table** — the query filters `Invoice.createdAt < asOf+1d` (did it exist yet) and requires `Receipt.receivedAt < asOf+1d` to be `none` (was it still unpaid then), and `Invoice.dueDate` is write-once at #31 so nothing else needs reconstructing. **Buckets are the textbook 30 / 60 / 90-day bands, drafted / unsourced** (`≤ 0` days overdue is `current`, then 1–30 / 31–60 / 61–90 / 90+; `ageingBucketFor` over `daysOverdue(dueDate, asOf)` on whole UTC calendar days) — same drafted status as `INVOICE_MAX_DUE_DAYS_AHEAD` (#31), `CLAIM_LARGE_THRESHOLD_JOD` (#23), the #27 follow-up thresholds, the #29 loss-ratio "period". **Book-wide** (`client-accounting.read` is a Finance / cross-book reporting perm — no per-owner filter; the optional `customerId` just narrows to one client); rows ordered **worst-first** (largest days-overdue, then largest outstanding balance, then customer name in a fixed `en` locale); capped at `AR_AGEING_INVOICE_LIMIT = 5000` (the #30 `ANALYTICS_POLICY_LIMIT` precedent — `logger.warn` on truncation); every figure pooled through `sumMoney` (`money.util.ts`). **No maker/checker** (a read). **Not audit-logged** — an invoice total is Confidential, not Highly Confidential: the #31 decision (`GET /invoices` is likewise not audited, same tier as the `Policy` premium read); contrast the #30 breakdown, which aggregates HIGHLY_CONFIDENTIAL `Claim` rows and does write a `READ` row. `apps/web/`: a new **"Client accounting"** top-level screen (`app/(app)/client-accounting/page.tsx` + `lib/client-accounting/ageing-api.ts` + an `AppNav` entry after "Claims analytics") — an `asOf` date `<input>`, a worst-first table of per-customer ageing buckets + a bold totals row, a "no outstanding receivables" empty state, and a friendly `client-accounting.read`-missing message. **`/brain-gap` filed** (`ibms-brain`): `finance-lifecycle.md` gains a "Client Accounting (Process 33)" section + the intro / endpoints / "Where the code lives" / "Out of scope" updated; the brain `CLAUDE.md` What's New got a #33 row (5-row cap — dropped the #28 row). **`@code-reviewer` (mandatory — financial calculation + Confidential financial data) → APPROVE WITH MINORS**, no blocker, no MAJOR, no lex violation, all six mandatory checks pass (`outstandingTotal` provably `=` Σ the five bucket strings; the `asOf` `createdAt` + receipt-`receivedAt` point-in-time query confirmed correct; the not-audited decision confirmed consistent with the Confidential tier / #31 precedent). 1 MINOR (documented, not code-changed — cross-currency pooling, harmless JOD-only, identical to #30's `buildLossRatioBreakdown`; a `SINGLE-CURRENCY` code comment added). 2 NITs noted, not taken (no `truncated` flag in the payload — the #30 `ANALYTICS_POLICY_LIMIT` precedent; the `parseHistoricalInstant` UTC-vs-local edge — pre-existing shared-util, #10/#12 use it identically, the web input caps the same UTC way). **Verification**: +12 api unit (`finance.config.spec.ts` +7 — `daysOverdue` / `ageingBucketFor` boundaries, `buildReceivablesAgeing` grouping / bucketing / pooled totals / worst-first + tie-break / empty / `asOf` normalisation; new `client-accounting.service.spec.ts` 5 — the repo scope + `asOfExclusiveUpper`, `asOf` defaults / future-422 / bad-date-422, the report built from loaded rows); `test/invoice.e2e-spec.ts` +1 (raise → customer in `current`; non-Finance 403; future `asOf` 422; `asOf` before the invoice existed → absent; backdate `dueDate` 45d → `d31_60`; collect → drops out; unknown `customerId` → empty). api unit **1172** (81 files); `invoice.e2e-spec.ts` 4/4 isolated; full api e2e green; turbo `build` + `typecheck` OK, api / web `eslint` OK; new Playwright `client-accounting.spec.ts` (3). `prisma migrate status` unchanged (**36**). **Follow-up fix (same branch, next commit)**: `packages/db` `prisma/seed-data/permissions.spec.ts` had one failing assertion since Part C #22 (`4aa7c3b` added `FINANCE` to `refund.approve` per the brain but did not update the stale role-level test) — **corrected the test, not the seed**: `roles-and-segregation-of-duties.md` says Finance "Cannot approve **own** refunds/write-offs" (instance-level — the "own"), and `maker-checker-segregation.md` maps the refund *checker* to a "Finance approver above the value threshold", so `FINANCE` legitimately holds `refund.approve`; raiser ≠ approver on a given `Refund` is enforced by `assertDifferentActors` + the `Refund_maker_checker_distinct` CHECK (`endorsement.service.ts` `approveRefund`), not by withholding the code — the same shape as the Claims Officer first-approver case in the same spec. `@ibms/db` tests 15/15, full `npm run test` green. | No migration, no seed change — `npm run db:migrate:deploy` already current at 36. |
| 2026-09-02 | Part C #29 (Claim Closure) landed — extends the `claim` module + a small new **`loss-ratio` module**. **No migration, no seed change** — the `claim.close` perm `[CLAIMS_OFFICER]`, `ClaimStatus.CLOSED`, the `WORKFLOW_TRANSITIONS.Claim` `SETTLED → CLOSED` / `DECLINED → CLOSED` edges, `Settlement.clientPaymentConfirmedAt`, and the `RenewalCase` / `LossRatio` models (migrated in `20260825124114`) all pre-existed. `POST /claims/:id/closure` (`{ clientPaymentConfirmedAt? }`, `claim.close`/**Claims Officer**) — **no maker/checker** (single-actor Claims work; the second approver is at settlement #28). **`SETTLED → CLOSED` is gated on the client's payment receipt**: `clientPaymentConfirmedAt` is body-supplied, stamped **write-once** on the `Settlement` (`ClaimRepository.confirmSettlementPayment` = status-conditional `updateMany`), past-only (`parseHistoricalInstant`) and **no earlier than the loss date** (a tighter "after the `Settlement` was recorded" bound deliberately NOT enforced — data-entry lag is normal, the #21 `deliveredAt` latitude). Closing without it → **422**; a *different* instant once recorded → **409**; a byte-identical re-close resumes a stuck close. **`DECLINED → CLOSED` is direct** — a `clientPaymentConfirmedAt` on a declined claim → **422**. Any other status → 422; an already-`CLOSED` claim = **200 no-op** that does **NOT** re-fire the recompute. **`closeCore`** drives `Claim (SETTLED \| DECLINED) → CLOSED` via `WorkflowTransitionService.transition` (+ a best-effort domain `ClaimStatusHistory` row — the #24–28 seam; a concurrent close normalises to an idempotent no-op) and **only the call that actually transitioned** best-effort triggers `LossRatioService.recomputeForPolicy` (logged, never thrown — closure has committed; the recompute is a downstream input, not a gate). The e2e asserts exactly **6** `TRANSITION` rows across the #23→#29 chain. **`LossRatioModule`** (`LossRatioService` + `LossRatioRepository`, `loss-ratio.config.ts`'s pure `computeLossRatio`) — `recomputeForPolicy(policyId)` upserts the `LossRatio` for the policy's open `RenewalCase` (`LossRatio.renewalCaseId @unique`); **the renewal module (Part 3.9) is NOT built, so no policy has a `RenewalCase` → it is a logged no-op today** (a standalone per-claim loss ratio is deliberately NOT created; the `CLOSED` `ClaimStatusHistory` row is the durable trigger record). `computeLossRatio` (pure): `periodClaims` = Σ `Settlement.netSettlement` over the policy's `SETTLED`/`CLOSED` claims (a `DECLINED` claim contributes 0), `periodPremium` = `Policy.issuedPremium ?? requestedPremium`, `ratio` = `periodClaims ÷ periodPremium` at 4 dp (`ROUND_HALF_UP`; zero premium → zero ratio). **The "period" is drafted/unsourced** — all-time for the policy; the renewal module will narrow it to the policy year (same status as `CLAIM_LARGE_THRESHOLD_JOD` etc.). `ClaimView` gains `closedAt` (the `CLOSED` `ClaimStatusHistory.changedAt`, or null) + `settlement.clientPaymentConfirmedAt`. Audit: the engine `TRANSITION` row + `UPDATE Settlement` (ids + the ISO timestamp, no narrative) when the payment is stamped + `UPDATE LossRatio` (ids + the three figures as fixed strings + trigger `claim-closed`) per real recompute; every closure also emits the `READ` `isSensitiveDataAccess` row `get`/`list` emit. `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim `ClaimClosure` sub-block (a "Client received the settlement payment on" date input + "Confirm payment & close claim" for `SETTLED`, a "Close claim" button for `DECLINED`, a read-only "Closed …" line once `CLOSED`), `canClose={isClaims}`. **`/brain-gap` filed + pushed** (`ibms-brain` `194888c`): `claims-lifecycle.md` — a new Claim Closure bullet + the Loss Ratio bullet extended. **`@code-reviewer` (mandatory — money arithmetic + a workflow transition + Highly Confidential data) → APPROVE WITH MINORS, no blocker, no MAJOR, all six mandatory lex checks pass** ("only the actual transitioner fires the recompute" confirmed). 3 MINORs fixed: (1) `computeLossRatio` clamps the ratio at `999.9999` (the `Decimal(7,4)` column max) + returns `ratioCapped` (into the result + audit + a `logger.warn`) so a huge all-time claims total can't make the upsert throw once renewal is wired; (2) `LossRatioService` audits **best-effort** (`recordAuditBestEffort` — `logger.error`, no rethrow) so an audit failure after the `LossRatio` row committed doesn't surface as a false "recompute did not run"; (3) the claim `#29` e2e now creates a `RenewalCase` so the closure drives a **real** recompute (`loadPolicyForRecompute` + `computeLossRatio` + `upsertLossRatio` + audit exercised against a real DB). NIT fixed: dropped "open `RenewalCase`" (1:1 with `Policy`; `TODO(renewal)` marks the renewal module owns any status predicate). NITs not taken: re-close-CLOSED-with-different-instant is a 200 no-op not 409 (brain sanctions it); the `.trim()` guard over `@IsISO8601`. +4 unit tests; api unit 1095. See README § Known gaps, Part C #29. | No migration, no seed change — `npm run db:migrate:deploy` already current at 34. |
| 2026-09-02 | Part C #30 (Claims Analytics) landed — extends the **`loss-ratio` module**, the last built Claims process. **No migration, no seed change** — `claims-analytics.view` perm (`[CLAIMS_OFFICER, BRANCH_DEPARTMENT_MANAGER, EXECUTIVE_MANAGEMENT, EXTERNAL_AUDITOR]`) already seeded; `computeLossRatio` (#29) reused. **`GET /claims-analytics/loss-ratio?groupBy=customer\|policy\|line`** (`claims-analytics.view`) — the aggregate `Claims ÷ Premium` breakdown that feeds the reporting dashboard and (once built) renewal. **Book-wide** — the perm is a cross-book reporting role, so NO per-owner visibility filter; optional `customerId`/`policyId`/`insuranceLine` params just narrow the policy set. **Computed on the fly — NO stored aggregate table** (the per-`RenewalCase` `LossRatio` write stays #29's). `LossRatioRepository.loadPoliciesForAnalytics` loads every **written** policy (status past `PLACEMENT_CONFIRMED` — `ANALYTICS_WRITTEN_POLICY_STATUSES`) + `customer.legalName` + its `SETTLED`/`CLOSED` claim `netSettlement`s; pure **`buildLossRatioBreakdown`** groups them + runs `computeLossRatio` **per group over the group's POOLED net settlements + POOLED written premium** (a *paid, all-time* loss ratio — NOT a sum/average of per-policy ratios) + a `totals` row pooling every in-scope policy regardless of `groupBy`. Row = `key`/`label`/`periodClaims`/`periodPremium`/`ratio`/`ratioCapped`/`claimCount`/`policyCount`; **rows ordered worst-first** (highest ratio). A `DECLINED` claim → 0; an open claim not counted (*paid* ratio; *incurred* w/ reserves deferred); a `CANCELLED`/`EXPIRED` policy still contributes its **full written premium** (earned-premium proration = renewal-module refinement). Audit: `ClaimsAnalyticsService` writes a `READ` row (`entityType: 'ClaimsAnalytics'`, `entityId` = the scoping id or `'book-wide'`, `afterValue` = `groupBy` + filters + counts — **never a figure or a customer name** — `isSensitiveDataAccess` when a claim contributed; best-effort, `logger.error` on failure). New `@Controller('claims-analytics')` in `LossRatioModule` (no `AuthModule` import — global `PermissionsGuard`/`@CurrentUser` cover it, same as `CrmModule`). `apps/web/`: a new **"Claims analytics"** top-level screen (`app/(app)/claims-analytics/page.tsx` + `lib/claims-analytics/analytics-api.ts` + an `AppNav` entry) — a group-by `<select>` + a worst-first table (loss ratio as %, claims paid, written premium, counts) + a bold totals row + a friendly `claims-analytics.view`-missing message. **`/brain-gap` filed + pushed** (`ibms-brain` `d1a0a1a`): `claims-lifecycle.md` — the Loss Ratio bullet gains a Process 30 sub-point. **`@code-reviewer` (mandatory — money arithmetic + Highly Confidential data aggregation) → APPROVE WITH MINORS, no blocker, no MAJOR, all six mandatory lex checks pass** (pooled ratio = `computeLossRatio` over Σclaims÷Σpremium, hand-computed-literal-asserted, never an avg of per-policy ratios; `totals` over all in-scope policies; the `READ` audit payload negative-asserted against figure/name leakage). 2 MINORs fixed (same window, `#27 FOLLOWUP_SWEEP_LIMIT` precedent): (1) `loadPoliciesForAnalytics` capped at `ANALYTICS_POLICY_LIMIT = 5000` (`take` + `orderBy createdAt`) + a service `logger.warn` on truncation; (2) the pooled sums no longer spread a book-scale array into `addMoney(0, ...list)` — a new **`sumMoney(values[])`** helper in `money.util.ts` (list arg, empty→`0`, no throw) used by `computeLossRatio` + `breakdownRowFor`, removing a latent `RangeError`. NITs fixed: `localeCompare(b, 'en')` for a deterministic equal-ratio tie-break; the audit `entityId` prefers the narrowest scope (`policyId ?? customerId ?? 'book-wide'`). +3 `sumMoney` tests; api unit 1109. See README § Known gaps, Part C #30. | No migration, no seed change — still 34 migrations. |
| 2026-09-03 | Part C #31 (Premium Billing) landed — **opens Domain D (Finance)**. New module `apps/api/src/modules/finance/` (+ `repositories/invoice.repository.ts`). **Migration `20260902210000`** adds `Invoice.invoiceType TEXT NOT NULL DEFAULT 'new_business_premium'` + a partial `UNIQUE ("policyId") WHERE "invoiceType" = 'new_business_premium'` (raw SQL — Prisma can't express a predicate `UNIQUE`) + `@@index([policyId])`. **No seed change** (`invoice.create` `[FINANCE_COLLECTIONS_OFFICER]` / `client-accounting.read` `[FINANCE, MANAGER, EXEC, AUDITOR]`, the `Invoice` model + its five `Decimal` money columns, the `InvoiceStatus` enum and `WORKFLOW_TRANSITIONS.Invoice` all pre-existed). `POST /invoices` (`{ policyId, taxAmount, feesAmount?, dueDate }`, `invoice.create`/**Finance**) raises the one **new-business premium `Invoice`** per policy: **`premiumAmount` carried from `Policy.issuedPremium`** (422 if unissued — a policy that reached `ISSUED` then went `CANCELLED`/`EXPIRED` is still billable, the mid-term return being a separate #22 `Refund`); **`commissionDeducted` auto-derived** = `applyPercentage(premium, commissionRatePercent)` where the rate is the placed `Recommendation.recommendedQuotation.commissionRatePercent` (the #22 `commissionRateFor` lookup; 422 if none captured — Process 35's `CommissionAgreement` will replace it); `taxAmount` + `feesAmount` the **only** money inputs (each `0 ≤ x ≤ premium`; fees default `0`); **`totalAmount` ALWAYS `premium + tax + fees − commissionDeducted` computed server-side** (`addMoney` → `subtractMoney`; the DTO rejects a `totalAmount` field — the #28 `netSettlement` lesson). `dueDate` a required `YYYY-MM-DD`, today .. +365d (`INVOICE_MAX_DUE_DAYS_AHEAD`, drafted). **One new-business premium invoice per policy** — the partial `UNIQUE` is the race backstop; write-once #24/#28-style: a byte-identical re-`POST` (five figures + `dueDate` via `compareMoney` / `getTime()`) returns the existing invoice, any different figure → **409**, concurrent → `P2002` → 409. **`Invoice` IS a `WorkflowTransitionService` entity** but #31 only creates it at the schema `@default(INVOICED)` — no engine transition (`INVOICED → COLLECTED` is Process 32; precedent #23 creating a `Claim` at `@default(NOTIFIED)`). **No maker/checker** (`roles-and-segregation-of-duties.md` lists "raise invoices" as a Finance single-actor duty; the Finance maker/checker pair is refunds / write-offs). Book-wide reads (`GET /invoices?policyId=\|customerId=` → 400 if neither, `GET /invoices/:id`). Audit: one best-effort `CREATE Invoice` row (ids + five figures as fixed 3dp strings + the commission rate + due date, **no free text**); reads **not** audited (Confidential, not Highly Confidential — same tier as the `Policy` premium read, which #18–21 also don't audit). `apps/web/.../opportunities/[id]/`: a "Billing" block in the "Policy" section — a read-only figure card once billed, a tax + fees + due-date form for Finance otherwise (`canInvoice={isFinance}`). **`/brain-gap` filed + pushed** (`ibms-brain` `f8843ed`): **new `meta/context/finance-lifecycle.md`** (Domain D seed) documents Process 31; `policy-lifecycle.md` § Out of scope points there. **`@code-reviewer` (mandatory — financial calculation + a migration + Confidential financial data) → APPROVE WITH MINORS, no blocker, no MAJOR, all six mandatory lex checks pass** (`race-safe-invariants` handling called "a textbook application"; the figures-can't-disagree property confirmed). 3 MINORs fixed: (1) added a `0 ≤ rate ≤ MAX_COMMISSION_RATE_PERCENT` billing-time backstop + a `totalAmount ≥ 0` assert on the new-invoice path (nothing bounded the commission rate at billing time — a rate > 100 would silently produce a negative bill), and corrected `computeInvoiceFigures`'s comment; (2) the write-once resume/409 check now runs **before** the input-bound checks (`parseDueDate` split into `parseDueDateInstant` + `assertDueDateInWindow`) so a genuine idempotent retry after the original due date has elapsed still resumes rather than 422-ing — the #28 `recordSettlement` ordering; (3) the stale-commission-rate risk (no `Policy → Quotation` link — a post-recommendation #15 negotiation round could leave the invoice netting the recommended-quote's rate) is now a code comment + a README Known-gaps note, tracked until #35. NITs: `commissionRatePercent.toFixed(2)` replaces `quantizeMoney(...).toFixed(2)` in the audit snapshot; a comment on the `issuedPremium != null` gate noting `CANCELLED`/`EXPIRED` is intentionally billable; `GET /invoices/:id` keeps a bare `@Param('id')` (matches the claim/policy/endorsement controllers). +26 api unit (`finance.config.spec.ts` 10, `invoice.service.spec.ts` 16); api unit **1135**. See README § Known gaps, Part C #31. | Run `npm run db:migrate:deploy` (migration `20260902210000`; checksum drift blocks `migrate:dev`, not `deploy`). No seed change. |
| 2026-09-03 | Part C #32 (Collection) landed — extends the `finance` module. **Migration `20260902220000`** adds ONLY `Receipt.invoiceId @unique` (`Receipt_invoiceId_key`) — the "one collection receipt per invoice" race backstop (a `@code-reviewer` **BLOCKER** fix, below). **No seed change** (`receipt.record` / `remittance.record` `[FINANCE_COLLECTIONS_OFFICER]`, the `Receipt` / `Remittance` / `ClientFundsLedgerEntry` models and the `WORKFLOW_TRANSITIONS.Invoice` cycle `INVOICED → COLLECTED → RECONCILED → REMITTED` all pre-existed). **`Invoice` IS a `WorkflowTransitionService` entity and #32 is where the engine transitions happen** — three `InvoiceController` endpoints, one hop each: `POST /invoices/:id/receipt` (`receipt.record`/**Finance**) → `INVOICED → COLLECTED`; `POST /invoices/:id/reconcile` (`receipt.record`) → `COLLECTED → RECONCILED`; `POST /invoices/:id/remittance` (`remittance.record`) → `RECONCILED → REMITTED`. Every move via `WorkflowTransitionService.transition` (its status-conditional `updateMany` + `Receipt.invoiceId @unique` + `Remittance.receiptId @unique` are the race gates); the `Receipt` / `Remittance` / `ClientFundsLedgerEntry` artefacts written **after** the transition commits (the #24 `register` pattern — lost race → resume/409; crash between transition and artefact → re-entry writes only the artefact); the e2e asserts exactly **3** `TRANSITION` audit rows + exactly **1** `Receipt`. **One exact-amount receipt per invoice** — `Receipt.amount` **must equal `Invoice.totalAmount`** (`compareMoney === 0`); a partial / over payment is a **422** (`money-decimal-jod.md`: a mismatch is raised as an exception, never silently written off — the variance path is Process 39). `P2002` on the `Receipt` `UNIQUE` → reload → byte-identical resume / 409. **Reconcile re-derives `sumMoney(receipts) === totalAmount` from the live rows** (the #16 "re-check at the decision point" rule) — mismatch 422, already-`RECONCILED`/`REMITTED` = idempotent 200. **The remittance is `premiumAmount − commissionDeducted`, computed server-side** (`subtractMoney`, `≥ 0` since #31 bounds commission ≤ premium; tax + fees stay with the broker); `insurerId` from `Policy.insurerId`; a non-policy invoice → 422; deterministic → a re-`POST` is an idempotent no-op; `Remittance.receiptId @unique` + `P2002` backstop. **Part 7.3 client-money segregation** — every collection books an `in` `ClientFundsLedgerEntry`, every remittance an `out` one, **each in the same `$transaction` as its `Receipt` / `Remittance`** (a deliberate local `$transaction` exception, same rationale as `PolicyRepository.createIssuanceArtifacts`); `reference` is an `invoice:<id>` pointer. **No maker/checker** (`roles-and-segregation-of-duties.md` — "record receipts" is a Finance single-actor duty; moving client money to the insurer is mechanical). Book-wide. `InvoiceView` gains `netRemittance` (`premium − commission`, computed server-side so the web never does money math — a `@code-reviewer` MINOR fix), `receipt` (`amount` / `method` / `receivedAt`) and `remittance` (`amount` / `insurerId` / `remittedAt`); `InvoiceRepository.findById` / list now `include` the cycle. Audit: a best-effort `CREATE` row for each `Receipt` / `Remittance` / `ClientFundsLedgerEntry` (amount as a fixed 3dp string + method / insurer / direction, no free text) + the engine `TRANSITION` rows. `apps/web/.../opportunities/[id]/`: the "Billing" block gains the three cycle actions (record receipt / reconcile / remit, `canCollect={isFinance}`) + a read-only "Collected …" / "Remitted to insurer …" display. **`/brain-gap` filed + pushed** (`ibms-brain` `2cfbe4f`): `finance-lifecycle.md` gains a "Collection (Process 32)" section. **`@code-reviewer` (mandatory — workflow state-machine + financial calc + a `$transaction` seam + Confidential data) → CHANGES REQUESTED, then re-reviewed → APPROVE.** All six mandatory lex checks pass bar the one BLOCKER (now fixed). **BLOCKER (fixed)**: "one receipt per invoice" was a read-then-create with no structural backstop — a retried / concurrent `POST /receipt` where request B lost the `INVOICED → COLLECTED` transition but reached `finishReceipt` before request A's `$transaction` committed would write **two** `Receipt` rows + **two** `in` `ClientFundsLedgerEntry` rows (double-booking the client's premium — `race-safe-invariants.md`). Fixed with `Receipt.invoiceId @unique` (migration `20260902220000`) so the already-written `P2002 → 409` handling becomes real, + a reload-and-resume on a byte-identical race (matching the remittance path and #31). **MINOR (fixed)**: `FinanceSection.tsx` computed the net remittance with naked JS float subtraction for a button label — replaced with a server-computed `InvoiceView.netRemittance`. **NIT (kept)**: the "stored remittance disagrees → 409" branch is unreachable given deterministic figures — kept as defence-in-depth. +25 api unit (`finance.config.spec.ts` 10 → 15, new `collection.service.spec.ts` 20); api unit **1160**. See README § Known gaps, Part C #32. | Run `npm run db:migrate:deploy` (migration `20260902220000`; checksum drift blocks `migrate:dev`, not `deploy`). No seed change. |

Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
