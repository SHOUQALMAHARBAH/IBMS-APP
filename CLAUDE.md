@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-02 | Part C #25 (Claim Documentation) landed — extends the `claim` module. **No migration of its own, no seed change** — the `ClaimDocument` / `Document` models, the `DocumentCategory` / `DataClassification` enums and the `claim.document` perm (`[CLAIMS_OFFICER]`) all pre-existed; migration `20260902180000` (the #24 `@@unique([claimId, toStatus])`) also backstops the #25 best-effort history-row write. `POST /claims/:id/documents` (`{ documents: [{ docType ∈ claim_form\|police_report\|medical_report\|photo\|invoice\|repair_estimate\|expert_report\|correspondence, classification, fileName, storageRef }] }`, `claim.document`/**Claims Officer**) — valid from `REGISTERED` onward (`NOTIFIED` → 422 "register it first"); `ClaimRepository.attachDocuments` writes one `Document` per file (`category: CLAIM`, `storageRef` pointer — never bytes) + one `ClaimDocument` join carrying `docType` in **one `$transaction`** (local exception). A `medical_report` MUST be `HIGHLY_CONFIDENTIAL` (`claims-lifecycle.md` — health data is classification-driven from first contact; 422 else). **The first attach best-effort advances `Claim REGISTERED → DOCUMENTATION_IN_PROGRESS`** via the engine + `ClaimRepository.recordStatusHistory` (idempotent `count()` pre-check + the `@@unique` backstop) — best-effort because `DOCUMENTATION_IN_PROGRESS` is a forward-progress marker, NOT a #20-style safety gate, so a failed advance is logged + retried on the next attach, never thrown; a later attach does not re-transition. The e2e asserts every `Claim` status move is an engine `TRANSITION` row. **The mandatory-document checklist per claim type** — `classifyInsuranceLine` (pure) maps the free-text `Policy.insuranceLine` to a broad line family (`property`/`motor`/`medical`/`liability`/`marine`/`other`) by keyword; `mandatoryDocTypesFor` maps each family (+ `police_report` for any third-party loss, + `claim_form` always) to a required `docType` set; `buildDocumentChecklist` derives `documentationComplete` + `missingMandatoryDocuments`. **Classifier + matrix are `ibms-app` drafted, unsourced** (Part 3.7 lists doc *types* only, no per-line matrix; no line taxonomy) — same status as `CLAIM_LARGE_THRESHOLD_JOD`/#16's 10%/2pp/#22's constants. `ClaimView` gains `documents` (no `storageRef`), `documentChecklist`, `documentationComplete`, `missingMandatoryDocuments`. Audit `CREATE ClaimDocument` = ids/`docType`/`category`/`classification` only, **never `fileName`/`storageRef`** (a claim doc filename can name an injured person — #18-19's `policyDocumentAuditSnapshot` rule). A claim `Document` carries no `policyId` (the `ClaimDocument` join is the canonical link). No maker/checker (single-actor Claims work). `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim checklist + single-file attach form (`canDocument={isClaims}`). **`@code-reviewer` (mandatory — workflow transition + Highly Confidential data) → APPROVE WITH MINORS, no blockers, no MAJOR, no lex violation** (all six mandatory lex checks pass). MINORs fixed: (1) the best-effort `REGISTERED → DOCUMENTATION_IN_PROGRESS` advance keys its resume off the **domain history row being absent** (`!statusHistory.some(h => h.toStatus === 'DOCUMENTATION_IN_PROGRESS')`), not `status === 'REGISTERED'` — a transition that committed but whose separate history write then threw is backfilled on the next attach (only the transition itself is skipped once `status` is already `DOCUMENTATION_IN_PROGRESS`); (2) `classifyInsuranceLine`'s `property` branch `\b(propert|…)\b` could never match the bare word "property" (`\b` between `t` and `y` fails) → `\bpropert\w*`; (3) `attachDocuments` now emits the `READ` sensitive-data-access audit row (ids/counts only) that `get`/`list` emit — its response echoes every file's `fileName` + `causeOfLoss` + third-party name. NITs: (1) the best-effort warn now says "will retry on the next attach"; (2) `CLAIM_INCLUDE.documents` gets a secondary `{ id: 'asc' }` sort (one `$transaction` → all `Document`s share `createdAt`). +5 spec tests for the three MINORs; api unit 980 (72 files); `claim.e2e-spec.ts` 6/6 isolated, full e2e 118/119 (the 1 fail = the known `enrollMfa` MFA-timing flake, this run on `cross-sell.e2e-spec.ts`). **`/brain-gap` filed + pushed** (`ibms-brain` `0dfa33f`): `claims-lifecycle.md` gains a Claim Documentation row + a medical-data sub-point. | Run `npm run db:migrate:deploy` if not already applied for #24 (migration `20260902180000`). No seed change. |
| 2026-09-02 | Part C #26 (Claim Assessment) landed — extends the `claim` module. **No migration, no seed change** (`claim.assess` perm `[CLAIMS_OFFICER]`, `Adjuster.surveyCompletedAt` / `investigationCompletedAt`, the `ClaimStatus` verdict values and the `WORKFLOW_TRANSITIONS.Claim` map `DOCUMENTATION_IN_PROGRESS → UNDER_ASSESSMENT → APPROVED\|PARTIALLY_APPROVED\|DECLINED` all pre-existed). Three endpoints, all `claim.assess`/**Claims Officer**: `POST /claims/:id/assessment/adjuster-progress` (`{ surveyCompletedAt?, investigationCompletedAt? }`) stamps the loss adjuster's completion timestamps — past-only (`parseHistoricalInstant`), no earlier than the loss date, **write-once per field** (identical re-send = no-op; different value = 409); `POST /claims/:id/assessment/submit` drives `Claim DOCUMENTATION_IN_PROGRESS → UNDER_ASSESSMENT`; `POST /claims/:id/assessment/decision` (`{ outcome ∈ APPROVED\|PARTIALLY_APPROVED\|DECLINED }`) drives `UNDER_ASSESSMENT → <outcome>`. **Every `Claim` status move goes through `WorkflowTransitionService.transition`** + writes a domain `ClaimStatusHistory` row (best-effort *after* the loud transition — `recordHistoryBestEffort`; the #24/#25 seam, reconciled by `backfillStatusHistory` on the next call). **`submit` is a HARD safety gate on the mandatory-document checklist** (`claims-lifecycle.md` — "the checklist is what gates the move to insurer assessment"), **recomputed from the loaded `ClaimDocument` rows** (never a stored `documentationComplete` snapshot — the #16 "re-derive at the decision point" generalisation); a 422 names the missing docTypes. Idempotent — a re-call once `UNDER_ASSESSMENT` backfills a missing history row without re-transitioning; anything past it is 422. **`decision` is gated on the adjuster having completed BOTH survey AND investigation** — a **drafted, unsourced** `ibms-app` rule (Part 3.7 tracks the data but does not say it blocks the verdict), same status as `CLAIM_LARGE_THRESHOLD_JOD` (#23) / the #25 checklist matrix / #16's 10%/2pp. The verdict is **write-once** (different outcome once recorded = 409; a disputed verdict routes to Complaint Management #42, not a status walk-back). **No maker/checker** — recording the insurer's verdict is single-actor Claims work, not the broker approving a payment (`maker-checker-segregation.md` § "what does NOT trigger this rule"; the second approver is at settlement #28). `ClaimView` gains `assessment` (`surveyCompletedAt` / `investigationCompletedAt` / `adjusterWorkComplete` / `readyForAssessment` / `outcome`). Audit: `UPDATE Adjuster` (ids + the two ISO timestamps, no claim narrative) + the engine `TRANSITION` rows; every assessment endpoint also emits the `READ` sensitive-data-access row `get`/`list` emit — **also retrofitted onto `register` (#24)** for consistency. **Also fixed here**: the #25 NIT-2 secondary `documents` sort had been an inline array literal under `as const` → a `readonly` tuple `Prisma.ClaimInclude` rejects, so `nest build` / `turbo typecheck` was RED on `0fc3922`; moved to a typed `CLAIM_DOCUMENTS_ORDER: Prisma.ClaimDocumentOrderByWithRelationInput[]` const, full turbo `build` + `typecheck` green again. `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim `ClaimAssessment` sub-block (adjuster stamps, "Submit for assessment" disabled until the checklist is complete, a verdict `<select>` once `UNDER_ASSESSMENT`), `canAssess={isClaims}`. **`@code-reviewer` (mandatory — workflow transitions + a state machine + Highly Confidential data) → APPROVE WITH MINORS, no blockers, no MAJOR, no lex violation** (all six mandatory checks pass — no money; every `Claim.status` move via the engine; no approval step and none required per `maker-checker-segregation.md`; no HC field logged; no `UNDER_ASSESSMENT` SLA; the one-history-row-per-status invariant is `@@unique([claimId, toStatus])`). Four MINORs (robustness gaps where `decideAssessment` / the `recordAdjusterProgress` repo / `backfillStatusHistory` didn't harden the crash/concurrency seams as thoroughly as `submitForAssessment` — integrity held via the engine's status-conditional write + the `@@unique`, so none merge-blocking) all fixed: (1) `decideAssessment` now backfills a missing `UNDER_ASSESSMENT` `ClaimStatusHistory` row (shared `concludeIdempotently` helper); (2) its `workflow.transition` is wrapped like `submitForAssessment`'s — a lost decision race gets a clean idempotent-200 / 409, not the engine's raw `ConflictException`; (3) `ClaimRepository.recordAdjusterProgress` returns `{ adjuster, wrote }` (per-field `updateMany` count) so the service 409s a losing concurrent writer with a different value; (4) `backfillStatusHistory` routes through `recordHistoryBestEffort` so a concurrent double-backfill's `@@unique` bounce is swallowed as "already reconciled". NIT: `isAssessmentOutcome` → `isAssessmentConcluded`. NIT noted for #28: `deriveAssessmentView.outcome` → `null` at `SETTLED` hides a settled claim's verdict in the web sub-block (`TODO(#28)` comment; verdict survives in `statusHistory`). +8 spec tests for the MINORs; api unit 1007. **`/brain-gap` filed + pushed** (`ibms-brain` `d1dba95`): `claims-lifecycle.md` gains a Process 26 bullet. | Run `npm run db:migrate:deploy` if not already applied (migration `20260902180000`, from #24). No new migration, no seed change. |
| 2026-09-02 | Part C #27 (Claim Follow-up) landed — extends the `claim` module. **Migration `20260902190000`** adds a partial `UNIQUE ("claimId") WHERE "resolvedAt" IS NULL` + `@@index([claimId])` on `ClaimFollowUpAlert` (the model, `Claim.followUpAlertThresholdDays` `@default(9)`, the `claim.followup.manage` perm `[CLAIMS_OFFICER]` all pre-existed). **No seed change.** **`ClaimFollowUpScheduler`** (`@Cron` 07:00 UTC, after the 06:00 RFQ sweep) + an on-demand **`POST /claims/follow-up-sweep`** (`claim.followup.manage`, counts only) both call `ClaimService.runFollowUpScan` — **two passes, per-row isolated** (`CrossSellDetectionScheduler` shape): **raise** a `ClaimFollowUpAlert` on every pre-verdict claim (`REGISTERED`/`DOCUMENTATION_IN_PROGRESS`/`UNDER_ASSESSMENT`) whose business-day `followUpAlertThresholdDays` has elapsed since its `REGISTERED` `ClaimStatusHistory.changedAt` and which has no open alert; **auto-resolve** open alerts whose claim has since moved past pre-verdict. `raiseFollowUpAlert` = `create` + `P2002` → "already alerted" (the partial `UNIQUE` is the race gate — a concurrent sweep is `skippedAlreadyAlerted`, not `failed`); resolve = conditional `updateMany` (0 rows = concurrent resolve). **One clock, from `REGISTERED`** (registration = submission to the insurer) — NOT reset at `UNDER_ASSESSMENT`. **Threshold per broad line family** (`motor` 7, `property` 10, `medical` 7, `liability`/`marine` 15, else 9 = the Part 3.7 example) via `followUpThresholdDaysFor` (`classifyInsuranceLine`), **drafted/unsourced** (same status as `CLAIM_LARGE_THRESHOLD_JOD` / #25 matrix / #16's 10%/2pp), **snapshotted onto `Claim.followUpAlertThresholdDays` at notification** (#23's `notify` now sets it) so a taxonomy change doesn't shift live claims. `isFollowUpDue` moved `rfq.config.ts` → shared **`common/follow-up.util.ts`** (`rfq.config.ts` re-exports). **`POST /claims/:id/follow-up-alerts/:alertId/resolve`** (`claim.followup.manage`) — manual resolve; **the claim's status is NOT touched** (an alert is an accountability nudge, not a lifecycle state); idempotent; wrong alert id on this claim → 404. **No maker/checker** (single-actor ops work). Audit `CREATE`/`UPDATE ClaimFollowUpAlert` = ids + threshold + clock timestamps + `resolvedBy` (`sweep`/`manual`), never claim narrative. `ClaimView` gains `followUp` (`followUpAlerts` / `followUpAlertOpen` / `followUpAlertThresholdDays` / `awaitingInsurerResponse` / `awaitingInsurerSince`). `apps/web/.../opportunities/[id]/`: the "Claims" section gains a "Run follow-up sweep" button + a per-claim open-alert display with "Resolve" (`canFollowUp={isClaims}`). **`@code-reviewer` (mandatory — a scheduled job + a migration + Highly Confidential data) → APPROVE WITH MINORS, no blocker, no MAJOR, no lex violation** (all six mandatory checks pass; the raise/resolve race handling called out as "a textbook application of `race-safe-invariants.md`"). Six MINORs (observability / maintainability / test-hygiene) fixed: (1) `findClaimsAwaitingInsurerResponse` return type declares the filtered `followUpAlerts: { where: { resolvedAt: null } }` shape; (2) a pre-verdict claim with **no `REGISTERED` history row** (the #24 seam) falls back to its earliest known instant + a `logger.warn` (still chased, ops-visible) instead of a silent skip; (3) a sweep that hits `FOLLOWUP_SWEEP_LIMIT` now `logger.warn`s instead of silently truncating; (4) `pdpl-sla-timers.md`'s Claim-follow-up registry row reconciled with the per-line table + flagged drafted (brain `155b233`); (5) the repo's `AWAITING_INSURER_STATUSES` exported + a spec asserts it equals the config's `CLAIM_AWAITING_INSURER_STATUSES`; (6) the e2e's raw `prisma.claim.update({ status: 'DECLINED' })` gained a deliberate-shortcut comment. NIT: the web `ClaimFollowUp` block dropped its dead multi-alert plural branch. +4 spec tests; api unit 1048. **`/brain-gap` filed + pushed** (`ibms-brain` `8618f29`, + the `155b233` reconcile): `claims-lifecycle.md` insurer-non-response row gains a Process 27 sub-point. | Run `npm run db:migrate:deploy` (migration `20260902190000`; checksum drift blocks `migrate:dev`, not `deploy`). No seed change. |
| 2026-09-02 | Part C #28 (Claim Settlement) landed — extends the `claim` module, closes the built stretch of Domain C at Process 28. **Migration `20260902200000`** adds ONLY `Settlement.brokerProcessedPayment BOOLEAN NOT NULL DEFAULT false` (`IF NOT EXISTS`, hand-authored + `migrate deploy` to `db` + `db-test`) — the "any claim payment the broker processes" trigger. **No seed change** (`Settlement` model, all four `Settlement` `Decimal` money fields already classified, the `Settlement_maker_checker_distinct` CHECK (`20260826091424`, `secondApproverUserId <> approvedByUserId`), the `WORKFLOW_TRANSITIONS.Claim` `APPROVED`/`PARTIALLY_APPROVED → SETTLED` edges, and the `claim.settle.approve` `[CLAIMS_OFFICER, MANAGER]` / `claim.settle.second-approve` `[MANAGER, FINANCE]` perms all pre-existed). `POST /claims/:id/settlement` (`{ approvedAmount, deductible, brokerProcessedPayment? }`, `claim.settle.approve`) — valid only from `APPROVED` / `PARTIALLY_APPROVED` (422 else). **Four DISTINCT figures, never collapsed**: `estimatedLoss` carried from `Claim.estimatedLoss`; `approvedAmount` + `deductible` the only inputs; **`netSettlement` ALWAYS `approvedAmount − deductible` computed server-side** (`subtractMoney`) — the DTO does not accept a `netSettlement` field. Hard bounds: `approvedAmount > 0`, `approvedAmount ≤ estimatedLoss` (422), `deductible ≥ 0`, `deductible ≤ approvedAmount` (422 — net can't go negative). **The recorder of the four figures IS the first approver** (`Settlement.approvedByUserId = actor`); recording is **write-once** — byte-identical re-`POST` (`compareMoney` on all figures + `brokerProcessedPayment`) = 200 no-op/resume, any changed figure = 409, concurrent create hits `claimId @unique` → `P2002` → 409. **Second approver required iff `approvedAmount ≥ CLAIM_LARGE_THRESHOLD_JOD`** (the **same drafted, unsourced** `'25000.000'` #23 uses for `isLargeClaim`, **re-derived from the LIVE approved amount** — never `Claim.isLargeClaim`, the notification-time snapshot) **OR `brokerProcessedPayment === true`**. Neither → `POST /settlement` drives `Claim → SETTLED` straight through; either → the claim holds at its verdict status until **`POST /claims/:id/settlement/second-approve`** (`claim.settle.second-approve`/**Manager or Finance**) — **maker/checker** `assertDifferentActors(approvedByUserId, actor)` (403) + the DB CHECK; status-conditional `recordSettlementSecondApproval` `updateMany` (0 rows → 409); a *different* second approver on an already-approved settlement → 409, the *same* one → idempotent `settleCore` resume; 422 if it doesn't actually need one. **`settleCore` structurally re-checks the second approval at the `→ SETTLED` write** — refuses to walk a claim whose live `Settlement` still needs a second approver while `secondApproverUserId IS NULL`, regardless of path (record + second-approve are separate writes and the engine map allows `→ SETTLED` unconditionally — the #22 "APPLY must re-check approval structurally" generalisation). Every `Claim` status move via `WorkflowTransitionService.transition` (+ a best-effort domain `ClaimStatusHistory` row, the #24–27 seam); the e2e asserts exactly **5** `TRANSITION` audit rows across the #23→#28 chain. **`Settlement` is NOT a `WorkflowTransitionService` entity** (no `status` — lifecycle = the parent `Claim`'s), same shape as `Adjuster` / `ClaimDocument` / `ThirdPartyClaimant` / `ClaimFollowUpAlert`. Audit `CREATE`/`APPROVE Settlement` = the four figures as fixed strings + `brokerProcessedPayment` + the maker/checker ids + `secondApproverRequired`, **never the claim narrative**; both endpoints also emit the `READ` `isSensitiveDataAccess` row `get`/`list` emit. `ClaimView` gains `settlement` (four figures + `brokerProcessedPayment` / `approvedByUserId` / `secondApproverUserId` / `secondApproverRequired` (re-derived in the view) / `settled`), or `null`. `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim `ClaimSettlement` sub-block — a four-figure record form + broker-processed checkbox (`canSettle={isClaims || isManager}`), a read-only four-figure display, and a "Second-approve settlement" button (`canSecondApproveSettlement={isManager || isFinance}`); `page.tsx` wires `isFinance` (`FINANCE_COLLECTIONS_OFFICER`). **`/brain-gap` filed + pushed** (`ibms-brain` `1999311`): `claims-lifecycle.md` — the four-figures row gains a Process 28 sub-point (net always computed; the two hard bounds; recorder = first approver; write-once) + the second-approver row gains the live-re-derived threshold / `brokerProcessedPayment` trigger / `assertDifferentActors` + DB CHECK pair / `settleCore` structural re-check. **`@code-reviewer` (mandatory — money arithmetic + maker/checker + Highly Confidential data + a migration) → APPROVE WITH MINORS, no blocker, no MAJOR, no lex violation** (all six mandatory checks pass; no crash/concurrency window to `SETTLED`-unapproved found). 2 MINORs fixed: (1) `recordSettlement`'s byte-identical re-post now resumes a stuck-but-fully-approved settle for **any** `claim.settle.approve` holder (`settleCore` is called whenever the second approval is satisfied — not required OR already recorded — not only when a second approver was never required), so recovery isn't bottlenecked on the one user who second-approved; (2) `secondApproveSettlement` no longer coalesces a null first approver to `'' === actor.id` (silent maker/checker pass) — a `Settlement` with a null `approvedByUserId` now 409s. NIT fixed: `isSettleableStatus(status)` helper (mirrors `isAssessmentConcluded`) replaces the `(CLAIM_SETTLEABLE_STATUSES as readonly string[]).includes(...)` cast at 3 sites. NIT noted, not taken: the web "Second-approve settlement" button still renders for the first-approver Manager (clicking → clear 403; hiding needs the user id threaded through `ClaimSection`, which the single-identity Playwright auth mock can't exercise). +2 unit tests; api unit 1073. See README § Known gaps, Part C #28. | Run `npm run db:migrate:deploy` (migration `20260902200000`; checksum drift blocks `migrate:dev`, not `deploy`). No seed change. |
| 2026-09-02 | Part C #29 (Claim Closure) landed — extends the `claim` module + a small new **`loss-ratio` module**. **No migration, no seed change** — the `claim.close` perm `[CLAIMS_OFFICER]`, `ClaimStatus.CLOSED`, the `WORKFLOW_TRANSITIONS.Claim` `SETTLED → CLOSED` / `DECLINED → CLOSED` edges, `Settlement.clientPaymentConfirmedAt`, and the `RenewalCase` / `LossRatio` models (migrated in `20260825124114`) all pre-existed. `POST /claims/:id/closure` (`{ clientPaymentConfirmedAt? }`, `claim.close`/**Claims Officer**) — **no maker/checker** (single-actor Claims work; the second approver is at settlement #28). **`SETTLED → CLOSED` is gated on the client's payment receipt**: `clientPaymentConfirmedAt` is body-supplied, stamped **write-once** on the `Settlement` (`ClaimRepository.confirmSettlementPayment` = status-conditional `updateMany`), past-only (`parseHistoricalInstant`) and **no earlier than the loss date** (a tighter "after the `Settlement` was recorded" bound deliberately NOT enforced — data-entry lag is normal, the #21 `deliveredAt` latitude). Closing without it → **422**; a *different* instant once recorded → **409**; a byte-identical re-close resumes a stuck close. **`DECLINED → CLOSED` is direct** — a `clientPaymentConfirmedAt` on a declined claim → **422**. Any other status → 422; an already-`CLOSED` claim = **200 no-op** that does **NOT** re-fire the recompute. **`closeCore`** drives `Claim (SETTLED \| DECLINED) → CLOSED` via `WorkflowTransitionService.transition` (+ a best-effort domain `ClaimStatusHistory` row — the #24–28 seam; a concurrent close normalises to an idempotent no-op) and **only the call that actually transitioned** best-effort triggers `LossRatioService.recomputeForPolicy` (logged, never thrown — closure has committed; the recompute is a downstream input, not a gate). The e2e asserts exactly **6** `TRANSITION` rows across the #23→#29 chain. **`LossRatioModule`** (`LossRatioService` + `LossRatioRepository`, `loss-ratio.config.ts`'s pure `computeLossRatio`) — `recomputeForPolicy(policyId)` upserts the `LossRatio` for the policy's open `RenewalCase` (`LossRatio.renewalCaseId @unique`); **the renewal module (Part 3.9) is NOT built, so no policy has a `RenewalCase` → it is a logged no-op today** (a standalone per-claim loss ratio is deliberately NOT created; the `CLOSED` `ClaimStatusHistory` row is the durable trigger record). `computeLossRatio` (pure): `periodClaims` = Σ `Settlement.netSettlement` over the policy's `SETTLED`/`CLOSED` claims (a `DECLINED` claim contributes 0), `periodPremium` = `Policy.issuedPremium ?? requestedPremium`, `ratio` = `periodClaims ÷ periodPremium` at 4 dp (`ROUND_HALF_UP`; zero premium → zero ratio). **The "period" is drafted/unsourced** — all-time for the policy; the renewal module will narrow it to the policy year (same status as `CLAIM_LARGE_THRESHOLD_JOD` etc.). `ClaimView` gains `closedAt` (the `CLOSED` `ClaimStatusHistory.changedAt`, or null) + `settlement.clientPaymentConfirmedAt`. Audit: the engine `TRANSITION` row + `UPDATE Settlement` (ids + the ISO timestamp, no narrative) when the payment is stamped + `UPDATE LossRatio` (ids + the three figures as fixed strings + trigger `claim-closed`) per real recompute; every closure also emits the `READ` `isSensitiveDataAccess` row `get`/`list` emit. `apps/web/.../opportunities/[id]/`: the "Claims" block gains a per-claim `ClaimClosure` sub-block (a "Client received the settlement payment on" date input + "Confirm payment & close claim" for `SETTLED`, a "Close claim" button for `DECLINED`, a read-only "Closed …" line once `CLOSED`), `canClose={isClaims}`. **`/brain-gap` filed + pushed** (`ibms-brain` `194888c`): `claims-lifecycle.md` — a new Claim Closure bullet + the Loss Ratio bullet extended. **`@code-reviewer` (mandatory — money arithmetic + a workflow transition + Highly Confidential data) → APPROVE WITH MINORS, no blocker, no MAJOR, all six mandatory lex checks pass** ("only the actual transitioner fires the recompute" confirmed). 3 MINORs fixed: (1) `computeLossRatio` clamps the ratio at `999.9999` (the `Decimal(7,4)` column max) + returns `ratioCapped` (into the result + audit + a `logger.warn`) so a huge all-time claims total can't make the upsert throw once renewal is wired; (2) `LossRatioService` audits **best-effort** (`recordAuditBestEffort` — `logger.error`, no rethrow) so an audit failure after the `LossRatio` row committed doesn't surface as a false "recompute did not run"; (3) the claim `#29` e2e now creates a `RenewalCase` so the closure drives a **real** recompute (`loadPolicyForRecompute` + `computeLossRatio` + `upsertLossRatio` + audit exercised against a real DB). NIT fixed: dropped "open `RenewalCase`" (1:1 with `Policy`; `TODO(renewal)` marks the renewal module owns any status predicate). NITs not taken: re-close-CLOSED-with-different-instant is a 200 no-op not 409 (brain sanctions it); the `.trim()` guard over `@IsISO8601`. +4 unit tests; api unit 1095. See README § Known gaps, Part C #29. | No migration, no seed change — `npm run db:migrate:deploy` already current at 34. |

Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
