@ibms-brain/CLAUDE.md

<!-- The import above pulls in ibms-brain's full rule set (meta/lex/, meta/context/,
     meta/designs/) via the ibms-brain/ git submodule. If that directory is empty, run
     `git submodule update --init --recursive` — see "Cloning this repo" below. Keep
     the import on line 1, same reason ibms-brain's own CLAUDE.md keeps @AGENTS.md on
     line 1: without it this content is dead weight while still read by other tools. -->

# CLAUDE.md — ibms-app

This is the first IBMS engineering repo. **Standards, mandatory rules, domain
knowledge, and architecture decisions live in `ibms-brain` (imported above via git
submodule at `ibms-brain/`), not here** — `ibms-brain/meta/lex/` (mandatory),
`ibms-brain/meta/context/` (domain knowledge), `ibms-brain/meta/designs/` (why decisions
were made). Read the relevant `meta/context/` file before touching an area, and the
`meta/lex/` rules before any non-trivial change — most of them (money-decimal-jod,
workflow-state-transitions, maker-checker-segregation, sensitive-data-handling,
pdpl-sla-timers) apply the moment real domain code lands here, which it has not yet.

## What's New

| Date | Change | Action required |
|------|--------|-----------------|
| 2026-09-05 | Part C #53-54 (Operational & Professional Indemnity Risk) landed — two checkboxes: "a generic risk register covering the six categories the source names (operational/cyber/financial/compliance/reputational) — Professional Indemnity gets its own, deeper table" and "track the broker's own Professional Indemnity policy (coverage limit, expiry, claims history) — a broker without valid PI cover is itself a licensing breach." **Genuinely no migration, no seed change** — `RiskRegisterItem`/`ProfessionalIndemnityPolicy`/`ProfessionalIndemnityRiskEvent` (Part 5/7.1 core schema) all pre-existed; `risk-register.manage` (`[COMPLIANCE_OFFICER, BRANCH_DEPARTMENT_MANAGER]`) / `pi-policy.manage` (`[COMPLIANCE_OFFICER]`) pre-seeded. `ProfessionalIndemnityRiskEvent` already had a real writer before this landing — `PolicyCheckingRepository.recordChecking` (Process 20) auto-logs one on a coverage discrepancy, in the same `$transaction` as the `PolicyChecking` row; this build gives those rows their first read surface, not their first writer. **`ProfessionalIndemnityPolicy` is deliberately NOT a fixed-id singleton like `BrokerLicense`** (#51) — no `issuedAt`/period-start field to define a validity window with, so a renewal is a brand-new row (preserving `claimsHistorySummary` per period) and "current" = `findFirst({orderBy:{expiresAt:'desc'}})`, the SAME definition `PolicyCheckingRepository.findLatestPiPolicyId` already relied on before `PiPolicyRepository` existed to give it a home — that method now delegates to the new repo instead of carrying its own copy (`PiPolicyRepository` provided directly by BOTH `ComplianceRiskModule` and `PolicyModule`, the `BrokerLicenseRepository` duplication-over-cross-import shape). New `apps/api/src/modules/compliance-risk/risk-register.{config,service,controller}.ts` + `pi-policy.{config,service,controller}.ts` + `pi-risk-event.{config,service,controller}.ts` + 3 repositories. `apps/web/` gains an **"Operational & PI risk"** screen (one page, three sections). **`@code-reviewer` (mandatory — a shared repository refactor touching a maker/checker-adjacent module + a new derived-"current" resolution over money/PII-adjacent data) → CHANGES REQUESTED → resolved: 1 BLOCKER + 1 MAJOR.** **BLOCKER**: `findFirst({orderBy:{expiresAt:'desc'}})` alone has no secondary sort key, and a tie is the LIKELY case, not an edge case — `parseCalendarDate` normalizes any plain-date input to an exact UTC midnight, so two officers entering the same renewal date (or a multi-insurer PI tower, or an accidental duplicate — no uniqueness guard exists on this table) tie to the millisecond, and Postgres gives no guarantee which tied row an unordered `ORDER BY ... LIMIT 1` returns, so `GET /pi-policy/current`, `list()`'s `isCurrent` flag, and the discrepancy auto-link could each independently pick a DIFFERENT "current" record; fixed by adding `id: 'desc'` as a deterministic secondary sort key on both `findCurrent()` and `findMany()`. **MAJOR**: the new `piRiskEventAuditSnapshot` embedded an auto-logged event's `description` verbatim into the audit row — but for an auto-logged event that text is `piRiskEventDescription()`'s exact coverage-figure diff (requested vs. issued amounts + policy number), the SAME content `policyCheckingAuditSnapshot` deliberately excludes from the sibling `PolicyChecking` audit row (`ibms-brain/meta/lex/sensitive-data-handling.md` — "metadata not body", the #12-19 precedent); fixed by redacting `description` to a placeholder whenever `sourcePolicyCheckingId` is set, leaving a MANUAL entry's own narrative verbatim (mitigationAction is never redacted — always fresh human-authored text either way). **2 MINORs**: a manual risk event auto-resolving to a currently-lapsed PI policy now gets a durable signal (`logger.warn` + `linkedPolicyWasLapsedAtLogTime` in the CREATE audit row, rather than a new N+1-prone view field on every future read); the claims-history update's lack of an optimistic-concurrency guard is now an explicit, stated trust-level decision (the `BrokerLicense.scopeOfAuthorization` reasoning) instead of an implicit one. **1 deferred MINOR**: none of the three models carry a `DataClassification` field — flagged as a real open gap needing an actual PCMS/`PRIV-STD-02` determination (`claimsHistorySummary` and a risk event's coverage detail plausibly name identifiable customers and settlement amounts, closer to `TransactionMonitoringAlert.detailText`'s `HIGHLY_CONFIDENTIAL` tier than to a bare authorization-scope label), not fixed by further stretching the `BrokerLicense` analogy — a `/brain-gap` filed for the day someone makes that call, not resolved by engineering guess in this pass. Verifying the tiebreak fix also caught a genuine cross-run test-monotonicity bug in the new e2e spec's own `nextFarFuture()` helper (needed because "current" resolution is book-wide with no per-test scoping possible, against a cumulative db-test): it originally grew its offset by a whole extra DAY per call within a run, which broke monotonicity ACROSS runs (a later-position call in an earlier run could out-rank an earlier-position call in a later run, since the wall-clock gap between any two runs is always under a day) — fixed by keeping the base offset FIXED and using only `Date.now()` (strictly increasing across runs) plus a same-run millisecond-counter tiebreak. **Verification**: +47 api unit → api unit **1752** (123 files, from 1705). New `test/operational-pi-risk.e2e-spec.ts` **4/4** (risk register CRUD + PI-exclusion validation; PI policy create/claims-history/current/list; a deliberate tie-on-`expiresAt` regression test proving 5 repeated `/current` reads all agree and `list()`'s `isCurrent` flag matches; manual PI risk event log — explicit and auto-resolved policy — + mitigation + list filters); `policy.e2e-spec.ts`'s pre-existing discrepancy-to-PI-risk-event auto-link re-verified unaffected by the repository refactor. Full api e2e suite green aside from the 2 documented chronic flakes (rbac confirmed passing cleanly in isolation; up-sell confirmed as a Prisma-engine-connection/timeout error — not an assertion failure — unrelated to this diff). Playwright `operational-pi-risk.spec.ts` 3/3; full Playwright suite **171/171** (from 168). `npm run typecheck`/`lint`/`build` (api + web) OK; `ibms-brain` `brain-doctor.sh` 0 errors; `prisma migrate status` clean (46, unchanged). | No migration, no seed change — `npm run db:migrate:deploy` already current at 46. |
| 2026-09-05 | Part D's Data Subject Request checkbox (M04, bundled under backlog Process #52 "Data Protection Compliance") landed — second of Part D's nine systems after M03 (Consent), both in `apps/api/src/modules/pdpl/`. **`DataSubjectRequest` (Part 4.1 core schema) pre-existed** with every field needed except the maker/checker closure pair; `WORKFLOW_TRANSITIONS.DataSubjectRequest` (RECEIVED -> IDENTITY_VERIFIED -> IN_PROGRESS -> {FULFILLED, PARTIALLY_FULFILLED, REJECTED} -> CLOSED, REJECTED also reachable from RECEIVED/IDENTITY_VERIFIED) and `SLA_REGISTRY`'s two DSR entries (`dsr_access_deletion` 15 business days, `dsr_correction_objection` 10, both with the pre-existing DPO-then-General-Manager two-stage escalation) also pre-existed — this module is their first real consumer. Migration `20260905120000` (46th) adds `processedByUserId`/`closedByUserId`/`rejectionReason`/`noOpenRetentionHoldConfirmedAt` + the `DataSubjectRequest_closure_maker_checker_distinct` CHECK + three list-filter indexes — no seed change (`dsr.log`/`dsr.handle`/`dsr.close`, all pre-seeded, `dsr.handle`/`dsr.close` both `[DATA_PROTECTION_OFFICER]`-only so closure segregates between two distinct DPO officers, not roles). **Closure needs sign-off from a DIFFERENT DPO officer** (`assertDifferentActors` + the CHECK, the `Complaint` shape) — `ibms-brain/meta/lex/maker-checker-segregation.md`'s own "Covered actions" table was silent on this despite the brain `CLAUDE.md`'s own summary already naming "DSR closure" as covered; fixed alongside this build. A DELETION request needs an explicit staff attestation (`confirmNoOpenRetentionHold`) before it can be marked fully FULFILLED — Retention & Disposal (M06) isn't built yet, so this is deliberately a staff attestation, not an automated check (the #48 `third_party_payment_source`-dormancy lesson applied up front, not discovered the hard way). The one ACCESS-only +15-business-day extension re-bases `slaDueAt`, additive to the existing due date. `apps/web/` gains a **"Data Subject Requests"** screen. **`@code-reviewer` (mandatory — a new `WorkflowTransitionService` entity + a new maker/checker pair + Highly-Confidential-adjacent PDPL rights data) → CHANGES REQUESTED → resolved: 1 BLOCKER + 2 MAJORs + 2 MINORs.** **BLOCKER**: the extension's write-once repository guard (`accessExtensionAppliedAt IS NULL`) never re-asserted `status` — a DSR that concluded (via a concurrent fulfil/reject) between the service's read and the repository's write could still have its deadline silently re-based and a fresh `SlaTimer` pair opened against an already-closed-out request; fixed by adding `status IN (RECEIVED, IDENTITY_VERIFIED, IN_PROGRESS)` to the same `where`, the `recordHandlerAssignment` shape three lines above it. **MAJOR 1**: the SLA re-basing on extension (`SlaTimerService` has no "update dueAt" method, only `startTimer`/`resolve`) was resolve-then-start, non-atomic — if `startTimer` failed after a successful `resolve`, the DSR ended up with ZERO open timers, a silent coverage gap; fixed by reordering to START-then-resolve with a new `SlaTimerService.resolve()` parameter (`createdBefore`, an optional `createdAt: {lt}` filter) so `resolve()`'s own `startsWith` match can't also catch the row `startTimer()` just created — every partial-failure outcome is now the safer of the two possible bad states (a harmless duplicate or a false-early alarm on a stale deadline), never a silent gap. **MAJOR 2**: `confirmNoOpenRetentionHold` was validated in-memory and then discarded — nothing persisted or audited which DPO officer attested "no retention hold applies" before a DELETION was closed as fully fulfilled; fixed by adding `noOpenRetentionHoldConfirmedAt`, stamped in the same write as `processedByUserId` and carried into the UPDATE audit snapshot. **2 MINORs**: the race-safe idempotent-retry-on-ConflictException pattern (present only on `start()`) applied uniformly across all six status-changing methods (`verifyIdentity`/`fulfil`/`partiallyFulfil`/`reject`/`close` too); the `receivedAt`-is-never-backdatable comment softened to not overclaim what it actually guarantees (staff can still receive a request and log it late — the design stops *disguising* a late log, not delaying one). DSR reads are audited (`isSensitiveDataAccess: true`, both `get()` and `list()`) — a deliberate departure from the Confidential-tier no-read-audit precedent (#33/#34/#41/#44/#45/#46/#51), reasoned as closer in kind to `Claim`/`TransactionMonitoringAlert`. **`/brain-gap` filed + pushed** (`ibms-brain` — new `data-subject-requests.md` context file; `maker-checker-segregation.md` gained the missing DSR row; `pcms-privacy-modules.md`'s "Where the code lives" updated; the brain `CLAUDE.md` What's New got a DSR row, 5-row cap dropped the #46 Customer Retention row). **Verification**: +~40 api unit (incl. the review-fix regression tests: extension start/resolve ordering both ways, the persisted attestation, a concurrent-race idempotency test per status-changing method) → api unit **1705** (117 files, from ~1665 pre-review-fix). New `test/dsr.e2e-spec.ts` **3/3** (full ACCESS lifecycle incl. the extension — asserts exactly 2 open + 2 resolved `SlaTimer` rows afterward, never 0 or 4; a DELETION lifecycle incl. the persisted attestation; reject-from-RECEIVED + book-wide list filters). Full api e2e suite green — all 33 files / 147 tests, no regression this run. New Playwright `dsr.spec.ts` 3/3; full Playwright suite **168/168** (from 165). `npm run typecheck`/`lint`/`build` (api + web) OK; `ibms-brain` `brain-doctor.sh` 0 errors; `prisma migrate status` clean (46). | Run `npm run db:migrate:deploy` (migration `20260905120000`; 46 migrations). No seed change. |
| 2026-09-05 | Part C #51 (Regulatory Compliance / CBJ) landed — two checkboxes: "automatically block new business issuance once the license lapses" and "a compliance calendar of regulatory obligations with owner, due date, and evidence-of-submission tracking." Also verified same-day: **#50 Conflict of Interest needed no separate build** — the backlog's own line reads "(covered under #16)", confirmed against the real, built `ConflictOfInterestDisclosure`/`RecommendationService` code; also fixed a pre-existing README misattribution of #50/#51's scope. **Genuinely no migration, no seed change** — `BrokerLicense`/`ComplianceCalendarItem` (both Part 7.1 core schema) pre-existed with every field needed; `license.manage`/`compliance-calendar.manage` (both `[COMPLIANCE_OFFICER]`) were pre-seeded ahead of time. **`BrokerLicense` is a true SINGLETON** — one row at a fixed id (`BROKER_LICENSE_SINGLETON_ID`), not a `findFirst()` guess; `POST /broker-license` 409s if one exists, `POST /broker-license/renew` 404s if none does, `POST /broker-license/mark-lapsed` is a manual override ahead of the calendar expiry (idempotent). **The lapse check is a pure LIVE recompute (`isBrokerLicenseCurrentlyLapsed`), not a scheduler-maintained flag** — deliberately: the #16 `@code-reviewer` MAJOR lesson ("a control that fires only when a human/sweep configured data in the right order is procedural, not structural") means `PolicyService.place()`'s block must be correct the instant `expiresAt` passes, so there is no `BrokerLicenseSweepScheduler` at all — nothing for one to keep in sync. **The gate is the literal first statement in `place()`**, before every other placement precondition; an unconfigured license (no row at all) is deliberately treated as **not blocked** (dozens of existing e2e/unit tests place a Policy with no license configured — this system assumes an already-licensed brokerage). `recordIssuance` (completing an already-placed policy) is NOT gated — only NEW placement is. New `apps/api/src/modules/compliance-risk/broker-license.{config,service,controller}.ts` + `compliance-calendar.{config,service,controller}.ts` + `repositories/broker-license.repository.ts` + `compliance-calendar.repository.ts`; `BrokerLicenseRepository` is ALSO provided directly by `PolicyModule` (the #49 `WatchlistEntryRepository` duplication-over-cross-import shape). `ComplianceCalendarItem.recordSubmission` is write-once (409 on a second attempt, race-safe status-conditional update) — a recurring obligation is a new row per cycle, the #41/#46 per-instance shape. **`parseCalendarDate` promoted from `policy.config.ts` to a new `common/calendar-date.util.ts`** (already a de facto shared utility via `endorsement.service.ts`'s cross-module import; `policy.config.ts` re-exports it, no call-site changes). `apps/web/` gains a **"Regulatory compliance"** screen (one page, two sections). **`@code-reviewer` (mandatory — a business-critical issuance-blocking gate + a novel global-singleton e2e-testing pattern) → CHANGES REQUESTED → resolved: 3 MAJORs.** **MAJOR 1**: `BrokerLicenseService.create()`'s `findCurrent() === null` pre-check alone leaves a real gap — two concurrent `POST /broker-license` calls both pass it before either writes, and while the fixed-id `@id` PK correctly stops a second row from ever existing, the loser's P2002 was unhandled, surfacing as a raw 500 instead of the clean 409 every other create-once resource in this codebase produces; fixed by wrapping `repo.create()` in the same `isUniqueViolation` catch-and-rethrow shape as `policy.service.ts`/`watchlist-sync.service.ts`. **MAJOR 2**: both new e2e specs mutate the real, shared `BrokerLicense` singleton (unlike every other fixture in this suite, scoped by unique generated ids) and rely on a `finally`-block restore to leave it active before the file ends — the restore's own success check only looked at `isCurrentlyLapsed` when the renew call returned 201, missing the more likely failure (renew itself returning non-201), which would have silently left the singleton lapsed with no signal, cascading into confusing unrelated 422s across every later e2e file that places a Policy; fixed by asserting the renew call's status unconditionally. **MAJOR 3**: the docs/bookkeeping obligations this table's own footer names (a dated What's New row, a pushed `ibms-brain` context file) hadn't been done yet at review time — this row is that fix. Restructuring the e2e fix away from `throw`-in-`finally` (ESLint's `no-unsafe-finally` — a throw there would have silently swallowed a genuine test-body failure) surfaced a MINOR too: the web form's `renew` only collected `licenseNumber`/`expiresAt`, and since `renew()` is a full-replace not a partial merge, using it would silently wipe any previously recorded `scopeOfAuthorization`/`issuedAt` — fixed by adding both fields to the form, pre-filled from the current record on load (adjusted during render per React's own recommended pattern, not a `useEffect`, to avoid clobbering an officer's in-progress edit on an unrelated reload). **Verification**: +33 api unit (`broker-license.config.spec.ts` 7, `broker-license.service.spec.ts` 11 incl. the P2002-race fix, `compliance-calendar.config.spec.ts` 4, `compliance-calendar.service.spec.ts` 8, `calendar-date.util.spec.ts` 5 — moved from `policy.config.spec.ts`, `policy.service.spec.ts` +4 for the license gate) → api unit **1651** (114 files, from 1617 pre-#51). New `test/regulatory-compliance.e2e-spec.ts` **2/2** (broker-license CRUD + permissions; compliance-calendar log/filter/record-submission) + `test/policy.e2e-spec.ts` **+1** (the real cross-module block: an active license places normally, a manually-lapsed one 422s a NEW placement, both singleton-mutating tests restore-and-verify before finishing). Full api e2e suite green aside from the two pre-existing chronic flakes (rbac/up-sell) plus one transient full-suite-contention failure (`audit.e2e-spec.ts`, confirmed passing in isolation) — no regression. New Playwright `regulatory-compliance.spec.ts` 3/3; full Playwright suite **165/165** (includes those 3). `npm run typecheck`/`lint`/`build` (api + web) OK. | No migration, no seed change. |
| 2026-09-05 | Part C #49 (Sanctions & PEP Screening) landed — the one checkbox: "Screen at onboarding + on any material change + a recurring batch against **updated lists**"; the first two legs were already built under #3-4, this finishes the third with REAL list data (two free, publicly published, no-API-key sanctions lists — OFAC SDN + UN Consolidated, both verified reachable live) instead of only `sample-watchlist.ts`'s fixture, and a cadence tied to a real observed list-refresh rate instead of an unsourced monthly guess. **New `WatchlistSource`/`WatchlistEntry`/`WatchlistSyncRun`** (migration `20260904140000`, 45th) — `WatchlistEntry` is the local sync cache (`sourceRecordId` = OFAC `ent_num`/UN `DATAID`, the upsert/prune key); `WatchlistSyncRun` is the sync job's own operational log, not an `AuditLogEntry`. **No seed change** — `sanctions-pep.screen` (`[COMPLIANCE_OFFICER]`) was pre-seeded, gating THREE endpoints: `POST /watchlist-sync/run`, `GET /watchlist-sync/status`, `POST /screening/recurring-batch`. New `apps/api/src/modules/compliance-risk/watchlist-sync.{config,service,controller,scheduler}.ts` + `watchlist-fetchers.ts` (the network boundary — two tiny injectable classes so the service never calls `fetch()` directly) + `repositories/watchlist-entry.repository.ts`. **`ScreeningService.run()` now checks TWO sources**: the fixture (dev/test only, unchanged) + the real synced list (every environment incl. production) — an exact match on `normalizeWatchlistName`'s canonical form (uppercase, punctuation stripped, tokens sorted — order-independent, NOT fuzzy/phonetic, documented). **`ScreeningBatchScheduler`'s cadence changed from a drafted monthly guess to every 4 hours** (the lists resync every 12h); its loop MOVED into `ScreeningService.runRecurringBatch()` so the scheduler and the new on-demand `POST /screening/recurring-batch` share identical logic — the #46/#48 shared-sweep shape. **Hand-rolled parsers, no new dependency** — a quoted-CSV parser for OFAC's mixed quoted/unquoted 12 columns, a scoped regex block/tag extractor for the UN XML — both unit-tested against real captured sample data. **Upsert-then-prune, chunked at 100 with bounded concurrency, no `$transaction`** — a non-transactional external cache refresh, not a financial/workflow write; OFAC alone is ~19,000 records. **`WatchlistEntryRepository` provided in BOTH `ComplianceRiskModule` and `CustomerModule` deliberately** (stateless `PrismaService` wrapper, safe twice) rather than exported/imported. **Neither unit nor e2e tests call the real endpoints** — the e2e spec stubs `globalThis.fetch` with fixture CSV/XML and drives the real endpoint through the full Nest app. `apps/web/` gains a **"Watchlist sync"** screen. **`@code-reviewer` (mandatory — a new external-network sync job + Highly-Confidential-adjacent data + a race-safe-invariants.md-shaped concurrency guard) → CHANGES REQUESTED → resolved: 4 BLOCKERs + 3 MINORs.** **BLOCKER 1** (concurrency): `POST /watchlist-sync/run` firing while the 12-hourly scheduler was mid-run (or two manual triggers) let two syncs of the SAME source interleave — one run's `pruneStale` could delete rows the other had just (re-)written under a different `syncRunId`; fixed with a hand-authored partial `UNIQUE (source) WHERE status='running'` on `WatchlistSyncRun` (`race-safe-invariants.md`'s established shape) — `createSyncRun`'s resulting P2002 is now caught and mapped to a benign `'skipped'` outcome, not an unhandled rejection. **BLOCKER 2** (plausibility): a 200 response carrying the wrong content (a WAF/interstitial page, a changed redirect target) parsed to near-zero records without ever throwing, and nothing stopped `pruneStale` from wiping the entire prior cache for that source on that basis; fixed with a floor check against the last successful sync's count (`WATCHLIST_MIN_ACCEPTABLE_RATIO = 0.5`, drafted) or an absolute floor with no prior sync (`WATCHLIST_MIN_ABSOLUTE_RECORDS = 10`, drafted) — a suspicious drop now fails the sync (existing cache untouched) instead of committing. **BLOCKER 3** (false-positive wildcard): `normalizeWatchlistName`'s original `[^A-Z0-9\s]` character class reduced ANY all-non-Latin-script name — Arabic, for this Jordan-based broker's own default `languagePreference: AR` — to `""`, and an empty `normalizedName` would collide with every other empty-string name; fixed by switching to Unicode-aware `\p{L}`/`\p{N}` (the `u` flag), plus a defense-in-depth empty-string refusal at BOTH the ingestion side (`WatchlistSyncService`, filtered before upsert, logged) and the match side (`ScreeningService.findRealWatchlistHit`, skipped before the query) as well as inside `WatchlistEntryRepository.findByNormalizedName` itself. **BLOCKER 4** (classification): `WatchlistEntry` shipped with no `DataClassification` field at all, reasoned in a code comment ("public government text, not IBMS customer data") rather than a `PRIV-STD-02` citation — `2026-08-pcms-source-of-truth.md` forbids exactly this; fixed by adding `classification DataClassification @default(HIGHLY_CONFIDENTIAL)` (conservative default, pending a real PCMS determination — `remarks` can carry a named individual's DOB and alleged-conduct text). **3 MINORs fixed**: `parseCsvLine` silently merged fields across an unterminated quote instead of rejecting the line (now returns `null`, treated as unparseable like a blank line); `ScreeningService.runRecurringBatch`'s catch block gained a comment pinning down exactly why logging `(err as Error).message` here is safe (every reachable failure is keyed on `customer.id`, never on a name or list content); the ADF-style single-short-token collision risk is now explicitly documented as an accepted, unfixed MINOR rather than left implicit. **`/brain-gap` filed + pushed** (`ibms-brain` — new `sanctions-pep-screening.md` context file; the brain `CLAUDE.md` What's New got a #49 row, 5-row cap dropped the #44 Customer Communication row). **Verification**: +59 api unit total (`watchlist-sync.config.spec.ts` 23, from 17 — Arabic-name + pure-punctuation normalization, the two plausibility constants, unterminated-quote cases in both `parseCsvLine` and `parseOfacSdnLine`; `watchlist-sync.service.spec.ts` 10, from 4 — P2002→skipped, a non-P2002 failure still throws, the plausibility floor both with and without a prior sync plus its exact boundary, an empty-normalized-name record filtered before upsert; `watchlist-sync.scheduler.spec.ts` 3 unchanged; `screening.service.spec.ts` +1 — an empty-normalized subject name never reaches the real-watchlist query; `screening-batch.scheduler.spec.ts` unchanged); api unit **1617** (109 files, from 1604, from 1575 pre-#49). Isolated `test/watchlist-sync.e2e-spec.ts` 1/1 (now also asserts the synced row's `classification` is `HIGHLY_CONFIDENTIAL`); full api e2e suite green (no regression); Playwright `watchlist-sync.spec.ts` 3/3 unaffected (no web changes in the review-fix pass); `npm run typecheck`/`lint` (api) OK; `ibms-brain` `brain-doctor.sh` 0 errors; `prisma migrate status` clean (45, same migration widened pre-commit). | Run `npm run db:migrate:deploy` (migration `20260904140000`; 45 migrations). No seed change. |
| 2026-09-04 | Part C #48 (AML/CFT Transaction Monitoring) landed — **opens Domain F (Compliance & Risk) beyond KYC**; new module `apps/api/src/modules/compliance-risk/`. Also verified same-day, immediately before starting #48: **backlog #47 KYC needed no separate build** — the backlog's own line reads "fully covered under #3–4", confirmed against the real, built `KycService`/`ScreeningService` code (`apps/api/src/modules/customer/`). **`TransactionMonitoringAlert` pre-existed with no application code ever writing to it — this is its first consumer.** **Migration `20260904130000` (44th) only widens it**: `sourceEntityType`/`sourceEntityId` (nullable — the triggering `Receipt` for the two event-scoped patterns; null for the two aggregate patterns), 3 indexes, and two race-safe uniqueness guards. **No seed change** — `aml.monitor`/`aml.escalate` (both `[COMPLIANCE_OFFICER]`, module `compliance-risk`) were seeded ahead of time (149 perms), kept as two distinct permissions on purpose. **Not a `WorkflowTransitionService` entity, no maker/checker** (the #42 `complaint.escalate` shape). **Detection** (`TransactionMonitoringSweepScheduler`, nightly 09:00 UTC after the 08:00 retention sweep, + on-demand `POST /transaction-monitoring-alerts/detect`) reads existing Finance/Endorsement data via four pure classifiers: `large_premium_payment` / `third_party_payment_source` scan every `Receipt` (an actual collected payment, #32) against a DRAFTED `AML_LARGE_PREMIUM_THRESHOLD_JOD='15000.000'` and whether its `PaymentChannel` (#38) belongs to a different customer than the one invoiced; `frequent_cancellations`/`frequent_refunds` count `Cancellation`/`Refund` per customer (via `Endorsement.policy.customerId`) in a DRAFTED rolling 90-day window against a threshold of 3. **`third_party_payment_source` is DORMANT in production** — `CollectionService.assertReceiptChannelUsable` (#38) already rejects any real `Receipt` whose channel mismatches the invoiced customer before one can exist, so this classifier can never fire outside a test that bypasses `CollectionService` directly (documented prominently, not architecturally changed — a business decision, not a code fix). **Race-safety**: a plain `@@unique([patternType, sourceEntityId])` for the event-scoped patterns (NULL `sourceEntityId` makes it inert for the two aggregate patterns AND for a manual log) + a hand-authored partial `UNIQUE ("customerId","patternType") WHERE status='open' AND "patternType" IN ('frequent_cancellations','frequent_refunds')` (the `UpSellRecommendation` shape) for the aggregate ones — scoped directly to `patternType`, NOT `sourceEntityId IS NULL` (a review BLOCKER: that predicate would have collided two unrelated manual `other`-pattern alerts, throwing an uncaught 500 — `create()` now catches `P2002` → a 409). Duplicate alerts are deliberately not treated as a hard invariant (unlike a double refund), so the sweep's own guard is pre-check + `P2002`→`skippedExisting`. **The suspicious-activity escalation path is TWO SEPARATE STEPS** (`POST /:id/escalate` then `POST /:id/report-to-authority`, the latter requiring the former first — 422 otherwise) — deliberately mirroring M03's consent-withdrawal request/confirm shape. **Record-keeping**: no delete endpoint exists on this model — the row + its audit trail IS the record; the retention *period* itself is undocumented/unsourced and not a tracked `SlaTimer` (no purge mechanism exists anywhere yet to guard against). `detailText` carries `NO_FULL_ACCOUNT_NUMBER` and is excluded from every audit row; `get()`/`list()` DO write a best-effort `READ` audit row (HIGHLY_CONFIDENTIAL — the `Claim`/`Crm` precedent, not the Confidential-tier no-audit one). `apps/web/` gains an **"AML monitoring"** screen. **`@code-reviewer` (mandatory — Highly Confidential AML data + financial reads + a new race-safe-invariant pattern) → CHANGES REQUESTED → resolved: 2 BLOCKERs** (the partial-index scoping above — fixed; `third_party_payment_source`'s dormancy above — documented, not architecturally changed, since relaxing `assertReceiptChannelUsable` would need its own product decision) **+ 1 MAJOR** (the missing `READ` audit — fixed) **+ 2 MINORs** (`escalate()` now checks the escalation flag before the `status` guard so a closed-but-escalated alert stays idempotent on retry; the sweep's `scanned` count now sums actual rows examined, not distinct customers). **`/brain-gap` filed + pushed** (`ibms-brain` — new `transaction-monitoring.md` context file; the brain `CLAUDE.md` What's New got a #48 row, 5-row cap dropped the #43 SLA Management row). **Verification**: +57 api unit (`transaction-monitoring.config.spec.ts` 23, `transaction-monitoring.service.spec.ts` 31 incl. the review-fix tests, `transaction-monitoring-sweep.scheduler.spec.ts` 3); api unit **1575** (106 files, from 1518). New `test/transaction-monitoring.e2e-spec.ts` 1/1 isolated (extended post-review: two independent manual `other` alerts both succeed, a manual aggregate-pattern duplicate 409s, re-escalating a closed-but-escalated alert stays idempotent, `READ` audit rows asserted); new Playwright `transaction-monitoring.spec.ts` 3/3; `npm run typecheck`/`lint`/`build` (api + web) OK; `ibms-brain` `brain-doctor.sh` 0 errors; `prisma migrate status` clean (44). | Run `npm run db:migrate:deploy` (migration `20260904130000`; 44 migrations). No seed change. |

Drop the oldest row once this table exceeds 5 entries (ibms-brain/meta/lex/workspace-updates.md).

## Cloning this repo

```bash
git clone --recurse-submodules https://github.com/SHOUQALMAHARBAH/IBMS-APP.git
# already cloned without it?
git submodule update --init --recursive
```

The submodule pins a specific `ibms-brain` commit — it does not auto-track `main`. Pull
in newer brain rules deliberately:

```bash
cd ibms-brain && git pull origin main && cd ..
git add ibms-brain && git commit -m "ibms-brain: sync to latest"
```

## What's here today

Infrastructure (Part A + Part B), plus Part C **Domain A, Processes 1–10** — Lead
Management (#1), Prospect Management (#2), Customer Acquisition/Onboarding (#3-4, with
*simulated* screening), Needs Assessment (#5), Risk Assessment (#6), Product
Recommendation / Program Design (#7), Cross-Selling (#8, a no-op until the Policy module
lands), Up-Selling (#9, flags a survey that outgrew the designed property Sum Insured),
Relationship Management / CRM (#10, logs every touchpoint as an `Interaction` and serves
the aggregated 360° customer timeline — policies/claims/complaints empty until Domains
B/C/E land). **Domain A is complete.** Part C **Domain B** has begun with RFQ / Market
Submission (#11) — a minimal `Opportunity` parent (created from a FINALIZED Insurance
Program) plus `RFQ`/`RFQInsurer` (one RFQ per line, insurer shortlist, per-insurer
response tracking, a nightly business-day follow-up alert sweep). Everything else —
Domain B #12–22, Domains C–H, and Parts D–G (PDPL, dashboards, bilingual UI, final
verification) — is not started. See root `README.md` § Scope status for the full picture
and § Known gaps for each built item's deferred edges.

## Common commands

```bash
npm install
cp .env.example .env
docker compose up -d db
npm run db:migrate:dev
npm run db:seed       # 11 roles + full permission grid — RBAC needs these to exist
npm run dev          # web:3000, api:4000
npm run lint
npm run typecheck
npm run test          # vitest, web + api

# api e2e — separate test DB (db-test), never the dev DB above. See README
# § Dev DB vs. test DB.
cp .env.test.example .env.test
docker compose up -d db-test
npm run db:test:migrate:dev
npm run test:e2e

npm run e2e           # playwright + axe-core, web
```

## Environment

- Node `20.19.0` — see `.nvmrc`. This satisfies Prisma 7's Node floor (≥20.19/22.12/24)
  but Prisma itself stays pinned at `6.19.3` in `package.json` — Node version no longer
  blocks a Prisma 7 install, so don't bump `prisma`/`@prisma/client` past 6.x without
  doing the driver-adapter (`@prisma/adapter-pg`) + `prisma.config.ts` migration first.
  See the Prisma note in root `README.md`.
- Docker required for Postgres locally and for building `apps/api`/`apps/web` images.

## Repo map

```
apps/web/     Next.js frontend
apps/api/     NestJS backend
packages/db/  Shared Prisma schema + client (@ibms/db)
ibms-brain/   Submodule — standards/rules/context (loaded via the import above)
.claude/      Agents/commands mirrored from ibms-brain + a subset of its hooks — see
              README.md § .claude/ — agents, commands, hooks
```

`@code-reviewer` and `@software-developer` (defined in `ibms-brain/meta/agents/`) and
`/brain-gap` are available in this repo via `.claude/`. Not yet wired here: the `git push`
evidence-gate hook (`enforce-evidence.sh`) and the domain-code hooks — see README.md for why.

## Before you write code

Same rule as ibms-brain: read `meta/context/` for the area, `meta/lex/` for what's
mandatory, cite the source document (PDPL / CBJ / ISO 27001/27701 / a specific
`PRIV-STD-*`/`PRIV-SOP-*`) in the PR when the change touches a regulatory obligation.
